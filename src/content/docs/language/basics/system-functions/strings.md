---
title: "Строковые функции"
sidebar:
  order: 11
---

# Строковые функции

в языке EME-L строковые функции обеспечивают манипуляцию текстом: поиск, замену, форматирование, преобразование регистра, кодировки, хеширование и работу с подстроками. в системе EME.WMS строковые функции используются для обработки данных штрих-кодов, формирования отчётов, валидации ввода и подготовки данных для EDI-обмена.

---

### is_regexp — проверка по регулярному выражению

проверяет, содержит ли строка подстроку, соответствующую регулярному выражению. При наличии третьего параметра заполняет массив найденными подстроками (до 10 элементов).

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Исходная строка для проверки |
| args[1] | String | Да | Шаблон регулярного выражения |
| args[2] | CIArray | Нет | Массив для сохранения найденных подстрок |

**Возвращает**: `Boolean` — результат проверки соответствия:
- `TRUE` — подстрока, соответствующая регулярному выражению, найдена
- `FALSE` — соответствие не найдено или исходная строка пуста

```EME-L
' Проверка формата email '
Result = is_regexp("user@example.com", "^[\w.-]+@[\w.-]+\.\w+$")
' Result = TRUE

' Извлечение групп совпадений '
Dim arr(10)
Found = is_regexp("Order #12345", "#(\d+)", arr)
' Found = TRUE, arr[0] = "12345"
```

---

### is_regexparr — поиск всех вхождений регулярного выражения

выполняет поиск всех вхождений регулярного выражения в строке с использованием синтаксиса QRegExp Qt и возвращает массив найденных подстрок.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Исходная строка для поиска |
| args[1] | String | Да | Регулярное выражение в синтаксисе QRegExp |
| args[2] | Boolean | Нет | Если `TRUE`, используется минимальный (ленивый) поиск. По умолчанию `FALSE` — жадный, максимальный поиск |

**Возвращает**: `CIArray` — массив найденных подстрок.

```EME-L
' Поиск всех чисел в строке '
Dim Numbers()
Numbers = is_regexparr("Order 100, Item 200, Total 300", "\d+")
' Numbers = ["100", "200", "300"]

' Ленивый поиск '
Dim Tags()
Tags = is_regexparr("<b>bold</b><i>italic</i>", "<.*>", TRUE)
' Tags = ["<b>", "</b>", "<i>", "</i>"] — при ленивом режиме
```

---

### is_regexparrpos — поиск вхождений с позициями

выполняет поиск всех вхождений регулярного выражения в строке и возвращает массив найденных подстрок, а также заполняет переданный массив позициями вхождений.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Исходная строка для поиска |
| args[1] | String | Да | Регулярное выражение в синтаксисе QRegExp |
| args[2] | CIArray | Да | Массив для сохранения позиций найденных вхождений |
| args[3] | Boolean | Нет | Если `TRUE`, используется минимальный поиск. По умолчанию `FALSE` |
| args[4] | Boolean | Нет | Если `FALSE`, регистр не учитывается. По умолчанию `TRUE` |
| args[5] | Integer | Нет | Индекс захватываемой группы. По умолчанию 0 |

**Возвращает**: `CIArray` — массив найденных подстрок.

```EME-L
' Поиск с сохранением позиций '
Dim Results()
Dim Positions()
Results = is_regexparrpos("ABC def GHI", "[A-Z]{3}", Positions)
' Results = ["ABC", "GHI"], Positions = [0, 8]
```

---

### is_replacepos — замена по позиции

заменяет часть строки, начиная с указанной позиции, на заданную подстроку.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Исходная строка |
| args[1] | Integer | Да | Начальная позиция замены, отсчёт с 0 |
| args[2] | Integer | Да | Количество заменяемых символов |
| args[3] | String | Да | Строка для вставки на место заменяемого фрагмента |

**Возвращает**: `String` — изменённая строка.

```EME-L
' Замена части строки '
Result = is_replacepos("Hello World", 6, 5, "EME-L")
' Result = "Hello EME-L"

' Вставка без удаления '
Result = is_replacepos("ABC", 1, 0, "123")
' Result = "A123BC"
```

---

### is_formatted — проверка формата строки

проверяет соответствие строки заданному формату по спецификации функции `scanf`.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Исходная строка для проверки |
| args[1] | String | Да | Форматная строка в спецификации scanf |

**Возвращает**: `Boolean` — результат проверки:
- `TRUE` — строка полностью соответствует формату
- `FALSE` — строка не соответствует формату

```EME-L
' Проверка формата даты '
Result = is_formatted("2024-03-15", "%d-%d-%d")
' Result = TRUE

' Проверка числа '
Result = is_formatted("123.45", "%f")
' Result = TRUE
```

---

### is_masked — проверка по маске символов

проверяет соответствие строки заданной маске. В маске в фигурных скобках `{}` задаётся допустимый набор символов, в круглых скобках `()` определяется собственно маска. Доступна только маска `*` — все символы должны быть из допустимого набора.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Исходная строка для проверки |
| args[1] | String | Да | Строка маски с описанием допустимых символов и шаблона |

**Возвращает**: `Boolean` — результат проверки:
- `TRUE` — строка соответствует маске
- `FALSE` — строка не соответствует маске или маска задана неверно

```EME-L
' Проверка, что строка содержит только цифры '
Result = is_masked("12345", "{ '0'-'9' }(*)")
' Result = TRUE

' Проверка на буквы и цифры '
Result = is_masked("A1B2", "{ 'A'-'Z', '0'-'9' }(*)")
' Result = TRUE
```

---

### is_length — длина строки или массива

возвращает длину строки в символах или количество элементов в бинарном массиве.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String или Array | Да | Исходная строка или бинарный массив (VT_ARRAY | VT_UI1) |

**Возвращает**: `Integer` — длина строки в символах или количество элементов массива.

```EME-L
' Длина строки '
Len = is_length("Hello")
' Len = 5

' Длина пустой строки '
Len = is_length("")
' Len = 0
```

---

### is_char — преобразование символа и кода

выполняет преобразование между символом и его кодом. Если аргумент — строка, возвращает код первого символа (0-255). Если аргумент — целое число, возвращает строку из одного или нескольких символов с указанным кодом.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String или Integer | Да | Исходная строка или целочисленный код символа |
| args[1] | Integer | Нет | Количество символов в результирующей строке при преобразовании числа в строку. По умолчанию 1 |

**Возвращает**:
- При строковом аргументе — `Integer` — код первого символа (диапазон 0-255)
- При числовом аргументе — `String` — строка из указанного количества символов

```EME-L
' Код символа '
Code = is_char("A")
' Code = 65

' Символ по коду '
Symbol = is_char(65)
' Symbol = "A"

' Несколько символов по коду '
Symbols = is_char(65, 3)
' Symbols = "AAA"

' Проверка обратимости '
Result = is_char(is_char("X"))
' Result = "X"
```

---

### is_charw — UNICODE-символ по коду

создаёт строку из одного UNICODE-символа по указанному числовому коду.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Числовой код UNICODE-символа |

**Возвращает**: `String` — строка из одного UNICODE-символа.

```EME-L
' Символ сердца '
Heart = is_charw(9829)
' Heart = "♥"

' Знак евро '
Euro = is_charw(8364)
' Euro = "€"
```

---

### is_atof — строка в вещественное число

преобразует строку в число с плавающей точкой. Если аргумент уже является числом, возвращает его как вещественное.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String или Real | Да | Строковое представление числа или числовое значение |

**Возвращает**: `Real` — число с плавающей точкой.

```EME-L
' Преобразование строки '
Value = is_atof("3.14159")
' Value = 3.14159

' Число остаётся числом '
Value = is_atof(42)
' Value = 42.0
```

---

### is_atol — строка в целое число

преобразует строку в целое число. Если аргумент уже является числом, возвращает его как целое.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String или Integer | Да | Строковое представление числа или числовое значение |

**Возвращает**: `Integer` — целое число.

```EME-L
' Преобразование строки '
Value = is_atol("12345")
' Value = 12345

' Отрицательное число '
Value = is_atol("-100")
' Value = -100
```

---

### is_atoi64 — строка в 64-битное целое

преобразует строку в 64-битное целое число. Если аргумент уже является числом, возвращает его как int64.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String или Integer | Да | Строковое представление числа или числовое значение |

**Возвращает**: `Integer` — 64-битное целое число.

```EME-L
' Большое число '
Value = is_atoi64("9223372036854775807")
' Value = 9223372036854775807
```

---

### is_in_full — число прописью

возвращает целую часть числа прописью на русском языке.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Real | Да | Исходное вещественное число |

**Возвращает**: `String` — строка с целой частью числа прописью.

```EME-L
' Число прописью '
Text = is_in_full(1234.56)
' Text = "одна тысяча двести тридцать четыре"
```

---

### is_trim_token — удаление токенов из строки

удаляет из строки указанное количество токенов, разделённых заданным символом. При отрицательном количестве удаление выполняется с конца строки.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Исходная строка |
| args[1] | String | Да | Символ-разделитель токенов |
| args[2] | Integer | Да | Количество удаляемых токенов. Если < 0 — удаление с конца строки |

**Возвращает**: `String` — строка после удаления указанных токенов.

```EME-L
' Удаление первых двух токенов '
Result = is_trim_token("a;b;c;d", ";", 2)
' Result = "c;d"

' Удаление последнего токена '
Result = is_trim_token("a;b;c;d", ";", -1)
' Result = "a;b;c"
```

---

### is_upper — верхний регистр

преобразует все символы строки в верхний регистр.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Исходная строка |

**Возвращает**: `String` — строка в верхнем регистре.

```EME-L
' Преобразование в верхний регистр '
Result = is_upper("Hello World")
' Result = "HELLO WORLD"

' Русские буквы '
Result = is_upper("Привет")
' Result = "ПРИВЕТ"
```

---

### is_lower — нижний регистр

преобразует все символы строки в нижний регистр.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Исходная строка |

**Возвращает**: `String` — строка в нижнем регистре.

```EME-L
' Преобразование в нижний регистр '
Result = is_lower("Hello World")
' Result = "hello world"

' Русские буквы '
Result = is_lower("ПРИВЕТ")
' Result = "привет"
```

---

### is_PascalCase2snake_case — преобразование регистра

преобразует строку из стиля PascalCase в стиль snake_case. Перед каждой заглавной буквой (кроме первой) вставляется символ подчёркивания, все символы преобразуются в нижний регистр.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Исходная строка в стиле PascalCase |

**Возвращает**: `String` — строка, преобразованная к стилю snake_case.

```EME-L
' Преобразование '
Result = is_PascalCase2snake_case("HelloWorld")
' Result = "hello_world"

' С аббревиатурой '
Result = is_PascalCase2snake_case("XMLParser")
' Result = "x_m_l_parser"
```

---

### is_format — форматирование строки

формирует строку по заданному формату, аналогично функции `printf`. Поддерживаются аргументы типа строка, целое число и вещественное число.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Форматная строка в спецификации printf |
| args[1] | String/Integer/Real | Нет | Значение для подстановки |
| args[2] | String/Integer/Real | Нет | Значение для подстановки |
| ... | ... | Нет | Дополнительные значения |

**Возвращает**: `String` — отформатированная строка.

```EME-L
' Форматирование числа '
Result = is_format("Value: %d", 42)
' Result = "Value: 42"

' Несколько аргументов '
Result = is_format("Name: %s, Age: %d", "Ivan", 30)
' Result = "Name: Ivan, Age: 30"

' Вещественное число с точностью '
Result = is_format("Pi = %.2f", 3.14159)
' Result = "Pi = 3.14"
```

---

### is_piece — извлечение части строки

возвращает заданное количество символов с начала или с конца строки. Также поддерживает работу с бинарными массивами VT_ARRAY | VT_UI1.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String или Array | Да | Исходная строка или бинарный массив |
| args[1] | Integer | Да | Количество символов. Если положительное — с начала строки, если отрицательное — с конца строки |

**Возвращает**:
- При строковом аргументе — `String` — подстрока указанной длины
- При массиве — бинарный массив указанной длины (VT_ARRAY | VT_UI1)

```EME-L
' Первые 5 символов '
Result = is_piece("Hello World", 5)
' Result = "Hello"

' Последние 5 символов '
Result = is_piece("Hello World", -5)
' Result = "World"
```

---

### is_fmoney — форматирование в денежный формат

форматирует число в денежный формат ЕМЕ с разделителями тысяч. По умолчанию используется 2 знака после запятой.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Real | Да | Исходное вещественное число |
| args[1] | Integer | Нет | Количество знаков после запятой. По умолчанию 2 |

**Возвращает**: `String` — строка с числом в денежном формате.

```EME-L
' Денежный формат '
Result = is_fmoney(1234567.89)
' Result = "1`234`567.89"

' Без копеек '
Result = is_fmoney(1234567, 0)
' Result = "1`234`567"
```

---

### is_trim_spaces — удаление всех пробелов

удаляет все пробельные символы (код 32) из строки.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Исходная строка |

**Возвращает**: `String` — строка без пробелов.

```EME-L
' Удаление пробелов '
Result = is_trim_spaces("  Hello   World  ")
' Result = "HelloWorld"
```

---

### is_trim_left — удаление символов слева

удаляет указанные символы с левого края строки. Если удаляемые символы не указаны, удаляются пробельные символы (whitespace).

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Исходная строка |
| args[1] | String | Нет | Символы для удаления слева. Если не указаны — удаляются whitespace |

**Возвращает**: `String` — строка после удаления символов слева.

```EME-L
' Удаление whitespace '
Result = is_trim_left("   Hello")
' Result = "Hello"

' Удаление указанных символов '
Result = is_trim_left("xxxHello", "x")
' Result = "Hello"
```

---

### is_trim_right — удаление символов справа

удаляет указанные символы с правого края строки. Если удаляемые символы не указаны, удаляются пробельные символы (whitespace).

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Исходная строка |
| args[1] | String | Нет | Символы для удаления справа. Если не указаны — удаляются whitespace |

**Возвращает**: `String` — строка после удаления символов справа.

```EME-L
' Удаление whitespace '
Result = is_trim_right("Hello   ")
' Result = "Hello"

' Удаление указанных символов '
Result = is_trim_right("Helloxxx", "x")
' Result = "Hello"
```

---

### is_trim_all — удаление символов с обоих концов

удаляет указанные символы с обоих концов строки. Если удаляемые символы не указаны, удаляются пробельные символы (whitespace).

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Исходная строка |
| args[1] | String | Нет | Символы для удаления с обоих концов. Если не указаны — удаляются whitespace |

**Возвращает**: `String` — строка после удаления символов с обоих концов.

```EME-L
' Удаление whitespace '
Result = is_trim_all("   Hello   ")
' Result = "Hello"

' Удаление указанных символов '
Result = is_trim_all("xxHelloxx", "x")
' Result = "Hello"
```

---

### is_left — символы с начала строки

возвращает заданное количество символов с начала строки.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Исходная строка |
| args[1] | Integer | Да | Количество символов для извлечения |

**Возвращает**: `String` — подстрока указанной длины с начала строки.

```EME-L
' Первые 3 символа '
Result = is_left("Hello", 3)
' Result = "Hel"
```

---

### is_right — символы с конца строки

возвращает заданное количество символов с конца строки.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Исходная строка |
| args[1] | Integer | Да | Количество символов для извлечения |

**Возвращает**: `String` — подстрока указанной длины с конца строки.

```EME-L
' Последние 3 символа '
Result = is_right("Hello", 3)
' Result = "llo"
```

---

### is_mid — подстрока из середины

возвращает подстроку из середины строки, начиная с указанной позиции. Если количество не указано, возвращает все символы до конца строки.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Исходная строка |
| args[1] | Integer | Да | Начальная позиция подстроки, отсчёт с 0 |
| args[2] | Integer | Нет | Количество символов для извлечения. Если не указано — возвращаются все символы до конца строки |

**Возвращает**: `String` — подстрока из середины исходной строки.

```EME-L
' Подстрока с позиции 2 '
Result = is_mid("Hello", 2)
' Result = "llo"

' Подстрока 3 символа с позиции 1 '
Result = is_mid("Hello", 1, 3)
' Result = "ell"
```

---

### is_clear_shortname — очистка названия

очищает строку от лишних элементов: удаляет текст внутри и вокруг двойных кавычек, а также указанные подстроки (например, ООО, ЗАО и т.п.). Удаляет пробелы по краям.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Исходная строка с полным названием |
| args[1] | String | Нет | Подстрока для удаления |
| args[2] | String | Нет | Подстрока для удаления |
| ... | String | Нет | Дополнительные подстроки для удаления |

**Возвращает**: `String` — очищенная строка.

```EME-L
' Удаление кавычек и юридических форм '
Result = is_clear_shortname('"ООО Ромашка"', "ООО", "ОАО", "ЗАО")
' Result = "Ромашка"
```

---

### is_wildcmp — сравнение с wildcard-маской

выполняет сравнение строки с маской, содержащей wildcard-символы `*` и `?`.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Шаблон с wildcard-символами: `*` (любое количество символов), `?` (один любой символ) |
| args[1] | String | Да | Строка для сравнения с маской |

**Возвращает**: `Integer` — результат сравнения:
- `1` — строка соответствует маске
- `0` — строка не соответствует маске

```EME-L
' Проверка расширения файла '
Result = is_wildcmp("*.txt", "document.txt")
' Result = 1

' Один символ '
Result = is_wildcmp("file?.log", "file1.log")
' Result = 1

' Не соответствует '
Result = is_wildcmp("*.pdf", "document.txt")
' Result = 0
```

---

### is_find — поиск подстроки

ищет первое вхождение подстроки в строке, начиная с указанной позиции.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Исходная строка для поиска |
| args[1] | String | Да | Искомая подстрока |
| args[2] | Integer | Нет | Позиция, с которой начинать поиск. По умолчанию 0 |
| args[3] | Boolean | Нет | Если `TRUE`, регистр учитывается. По умолчанию `TRUE` |

**Возвращает**: `Integer` — индекс первого найденного вхождения или `-1`, если не найдено.

```EME-L
' Простой поиск '
Pos = is_find("Hello World", "World")
' Pos = 6

' Поиск с позиции '
Pos = is_find("ababab", "ab", 2)
' Pos = 2

' Поиск без учёта регистра '
Pos = is_find("Hello World", "world", 0, FALSE)
' Pos = 6

' Не найдено '
Pos = is_find("Hello", "xyz")
' Pos = -1
```

---

### is_rfind — обратный поиск подстроки

ищет последнее вхождение подстроки в строке, начиная с указанной позиции с конца.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Исходная строка для поиска |
| args[1] | String | Да | Искомая подстрока |
| args[2] | Integer | Нет | Позиция с конца для начала поиска. По умолчанию `-1` — конец строки |
| args[3] | Boolean | Нет | Если `TRUE`, регистр учитывается. По умолчанию `TRUE` |

**Возвращает**: `Integer` — индекс последнего найденного вхождения или `-1`, если не найдено.

```EME-L
' Обратный поиск '
Pos = is_rfind("ababab", "ab")
' Pos = 4

' С позиции '
Pos = is_rfind("ababab", "ab", 3)
' Pos = 2
```

---

### is_count_CRC — контрольная сумма CRC32

вычисляет CRC32 контрольную сумму строки.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Исходная строка |

**Возвращает**: `Integer` — значение CRC32 контрольной суммы.

```EME-L
' Контрольная сумма '
Crc = is_count_CRC("Hello World")
' Crc = целочисленное значение CRC32
```

---

### is_replace — замена подстрок

заменяет все вхождения указанных подстрок в строке на новые подстроки. При более чем 4 аргументах используется умная замена CSmartReplace.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Исходная строка |
| args[1] | String | Да | Подстрока для замены |
| args[2] | String | Да | Подстрока для вставки |
| args[3] | String | Нет | Дополнительная подстрока для замены |
| args[4] | String | Нет | Дополнительная подстрока для вставки |
| ... | String | Нет | Дополнительные пары замена/вставка |

**Возвращает**: `String` — изменённая строка.

```EME-L
' Простая замена '
Result = is_replace("Hello World", "World", "EME-L")
' Result = "Hello EME-L"

' Несколько замен '
Result = is_replace("a-b-c", "-", "_", "a", "x")
' Result = "x_b_c"
```

---

### is_replace_ex — замена запрещённых символов

заменяет указанные символы в строке на заданную подстроку. По умолчанию заменяет запрещённые символы в имени файла на символ подчёркивания.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Исходная строка |
| args[1] | String | Нет | Строка с символами для замены. По умолчанию `|"`/\<>*\|:` |
| args[2] | String | Нет | Строка для замены. По умолчанию `_` |

**Возвращает**: `String` — изменённая строка.

```EME-L
' Корректировка имени файла '
Result = is_replace_ex("file:name?.txt")
' Result = "file_name_.txt"

' Замена других символов '
Result = is_replace_ex("a,b,c", ",", ";")
' Result = "a;b;c"
```

---

### is_correct_filename — корректное имя файла

приводит строку к корректному имени файла, удаляя недопустимые символы.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Исходная строка с предполагаемым именем файла |

**Возвращает**: `String` — корректное имя файла.

```EME-L
' Корректировка имени '
Result = is_correct_filename("report:2024/03.txt")
' Result = "report202403.txt"
```

---

### is_replace_and_save_position — замена с сохранением позиций

заменяет указанные символы в строке и сохраняет в массиве позиции заменённых символов. В массив записываются пары: символ, позиция.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Исходная строка |
| args[1] | String | Нет | Строка с символами для замены. По умолчанию `|"`/\<>*\|:` |
| args[2] | String | Нет | Строка для замены. По умолчанию `_` |
| args[3] | CIArray | Да | Массив для сохранения информации о заменах |

**Возвращает**: `String` — изменённая строка.

```EME-L
' Замена с сохранением позиций '
Dim Positions()
Result = is_replace_and_save_position("a:b/c", "", "", Positions)
' Result = "a_b_c"
' Positions = [":", 1, "/", 3]
```

---

### is_insert_simbols_from_array — вставка символов из массива

вставляет символы внутрь строки в соответствии с переданным массивом. Массив содержит пары: символ, позиция вставки. Символы вставляются, а не заменяют существующие.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Исходная строка |
| args[1] | CIArray | Да | Массив с парами символ-позиция |

**Возвращает**: `String` — изменённая строка.

```EME-L
' Вставка разделителей '
Dim Arr()
Arr[0] = "-"
Arr[1] = 2
Arr[2] = "-"
Arr[3] = 5
Result = is_insert_simbols_from_array("123456", Arr)
' Result = "12-34-56"
```

---

### is_UTF2WIN — UTF-8 в Windows-1251

конвертирует строку из кодировки UTF-8 в кодировку Windows-1251 (ANSI).

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Исходная строка в кодировке UTF-8 |

**Возвращает**: `String` — строка в кодировке Windows-1251.

```EME-L
' Конвертация из UTF-8 '
WinStr = is_UTF2WIN(Utf8Str)
```

---

### is_WIN2UTF — Windows-1251 в UTF-8

конвертирует строку из кодировки Windows-1251 (ANSI) в кодировку UTF-8. При передаче объекта CIStringBuilder преобразует содержимое объекта и возвращает изменённый объект.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String или Object | Да | Исходная строка в кодировке Windows-1251 или объект CIStringBuilder |

**Возвращает**:
- При передаче строки — `String` — преобразованная строка в кодировке UTF-8
- При передаче CIStringBuilder — ссылка — модифицированный объект CIStringBuilder с содержимым в UTF-8

```EME-L
' Конвертация строки '
Utf8Str = is_WIN2UTF(WinStr)

' Конвертация объекта '
is_WIN2UTF(StringBuilder)
```

---

### is_load_random_names — генерация случайных строк

генерирует указанное количество случайных строк заданного типа и добавляет их в массив.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Количество генерируемых значений |
| args[1] | String | Да | Тип генерируемых значений: Фамилия, Фирма, ФИО, Адрес, Имя |
| args[2] | CIArray | Да | Массив для сохранения сгенерированных значений |

**Возвращает**: пустое значение.

```EME-L
' Генерация случайных имён '
Dim Names()
is_load_random_names(10, "ФИО", Names)
' Names содержит 10 случайных ФИО
```

---

### is_show_text_diff_by_html_color — выделение отличий HTML-цветом

сравнивает две строки и выделяет отличия в новой строке указанным HTML-цветом. Изменённые и новые символы оборачиваются в HTML-тег `<font>` с указанным цветом.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Исходная строка для сравнения |
| args[1] | String | Да | Изменённая строка |
| args[2] | String | Нет | Цвет выделения в формате HTML, например `#FF0000`. По умолчанию красный |

**Возвращает**: `String` — новая строка с HTML-выделением отличий.

```EME-L
' Выделение отличий '
Result = is_show_text_diff_by_html_color("Hello World", "Hello EME-L")
' Result содержит HTML с выделенным "EME-L"
```

---

### is_atofmoney — денежный формат в число

преобразует строку в денежном формате ЕМЕ в число с плавающей точкой. Удаляет разделители тысяч и преобразует десятичный разделитель в точку.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Строка с числом в денежном формате |
| args[1] | Real | Нет | Значение, возвращаемое при ошибке парсинга. По умолчанию 0 |

**Возвращает**: `Real` — число с плавающей точкой или значение по умолчанию при ошибке.

```EME-L
' Преобразование денежного формата '
Value = is_atofmoney("1`234`567.89")
' Value = 1234567.89

' С запятой '
Value = is_atofmoney("1 234,56")
' Value = 1234.56
```

---

### is_split — разбиение строки

разбивает строку на подстроки по указанному разделителю и записывает результат в массив. Массив не очищается перед добавлением.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Исходная строка для разбиения |
| args[1] | String | Да | Символ или строка-разделитель |
| args[2] | CIArray | Да | Массив для сохранения результатов |
| args[3] | Boolean | Нет | Если `TRUE`, разделитель считается целой строкой. По умолчанию `FALSE` (разделитель — любой символ из строки) |

**Возвращает**: `Integer` — количество добавленных элементов.

```EME-L
' Разбиение по точке с запятой '
Dim Parts()
Count = is_split("a;b;c;d", ";", Parts)
' Parts = ["a", "b", "c", "d"], Count = 4

' Разбиение с полным совпадением '
Dim Items()
Count = is_split("a::b::c", "::", Items, TRUE)
' Items = ["a", "b", "c"], Count = 3
```

---

### is_join — объединение массива в строку

собирает элементы массива в строку, вставляя между ними указанный разделитель.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | CIArray | Да | Массив элементов для объединения |
| args[1] | String | Нет | Строка-разделитель между элементами. По умолчанию пробел |
| args[2] | Integer | Нет | Начальный индекс в массиве. По умолчанию 0 |
| args[3] | Integer | Нет | Конечный индекс (не включается). По умолчанию размер массива |

**Возвращает**: `String` — объединённая строка.

```EME-L
' Объединение массива '
Dim Arr()
Arr[0] = "Hello"
Arr[1] = "World"
Result = is_join(Arr, " ")
' Result = "Hello World"

' С разделителем '
Result = is_join(Arr, "-")
' Result = "Hello-World"
```

---

### is_strncmp — сравнение строк с учётом регистра

выполняет лексикографическое сравнение первых N символов двух строк с учётом регистра.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Первая строка для сравнения |
| args[1] | String | Да | Вторая строка для сравнения |
| args[2] | Integer | Нет | Количество символов для сравнения. По умолчанию длина второй строки |

**Возвращает**: `Integer` — результат сравнения:
- `< 0` — строка1 меньше строки2
- `0` — строки равны
- `> 0` — строка1 больше строки2

```EME-L
' Сравнение '
Result = is_strncmp("Hello", "Helium", 3)
' Result = 0 — первые 3 символа равны

' Различие '
Result = is_strncmp("ABC", "XYZ", 2)
' Result < 0
```

---

### is_strnicmp — сравнение строк без учёта регистра

выполняет лексикографическое сравнение первых N символов двух строк без учёта регистра.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Первая строка для сравнения |
| args[1] | String | Да | Вторая строка для сравнения |
| args[2] | Integer | Нет | Количество символов для сравнения. По умолчанию длина второй строки |

**Возвращает**: `Integer` — результат сравнения:
- `< 0` — строка1 меньше строки2
- `0` — строки равны
- `> 0` — строка1 больше строки2

```EME-L
' Сравнение без регистра '
Result = is_strnicmp("Hello", "HELLO", 5)
' Result = 0
```

---

### is_strcmp — полное сравнение строк

выполняет лексикографическое сравнение двух строк с учётом регистра.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Первая строка для сравнения |
| args[1] | String | Да | Вторая строка для сравнения |

**Возвращает**: `Integer` — результат сравнения:
- `< 0` — строка1 меньше строки2
- `0` — строки равны
- `> 0` — строка1 больше строки2

```EME-L
' Полное сравнение '
Result = is_strcmp("Hello", "Hello")
' Result = 0

' Различие '
Result = is_strcmp("ABC", "XYZ")
' Result < 0
```

---

### is_stemplate — шаблонизация строки

формирует строку из шаблона, заменяя ключевые слова на соответствующие значения. Поддерживает передачу объектов CIParameters и CIMap в качестве источников пар ключ-значение.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Строка шаблона с ключевыми словами |
| args[1] | Boolean | Нет | Если `TRUE`, не найденные ключи пропускаются без ошибки. По умолчанию `TRUE` |
| args[2] | String | Нет | Ключевое слово для замены |
| args[3] | String | Нет | Значение для подстановки |
| ... | ... | Нет | Дополнительные пары ключ-значение |

**Возвращает**: `String` — сформированная строка.

```EME-L
' Простая шаблонизация '
Result = is_stemplate("Hello, {name}!", TRUE, "name", "World")
' Result = "Hello, World!"

' С несколькими параметрами '
Result = is_stemplate("{greeting}, {name}!", TRUE, "greeting", "Hi", "name", "EME-L")
' Result = "Hi, EME-L!"
```

---

### is_urlencode — кодирование URL

кодирует строку в формат URL (percent-encoding) с использованием UTF-8.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Исходная строка для кодирования |

**Возвращает**: `String` — закодированная URL-строка.

```EME-L
' Кодирование строки '
Encoded = is_urlencode("Hello World!")
' Encoded = "Hello%20World%21"

' Русские символы '
Encoded = is_urlencode("Привет")
' Encoded = URL-encoded UTF-8
```

---

### is_urldecode — декодирование URL

декодирует строку из формата URL (percent-encoding) в исходный вид.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Закодированная URL-строка |

**Возвращает**: `String` — декодированная строка.

```EME-L
' Декодирование '
Decoded = is_urldecode("Hello%20World%21")
' Decoded = "Hello World!"
```

---

### is_hex — строка в HEX

преобразует строку в HEX-представление, кодируя каждый байт шестнадцатеричным значением.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Исходная строка для преобразования |
| args[1] | String | Нет | Формат вывода HEX-значений. По умолчанию `%02X` |

**Возвращает**: `String` — строка в HEX-представлении.

```EME-L
' HEX-представление '
Result = is_hex("ABC")
' Result = "414243"

' С нижним регистром '
Result = is_hex("ABC", "%02x")
' Result = "414243"
```

---

### is_fromhex — HEX в строку

преобразует строку из HEX-представления обратно в исходную строку.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | HEX-строка для преобразования |
| args[1] | String | Нет | Формат чтения HEX-значений. По умолчанию `%02X` |

**Возвращает**: `String` — декодированная строка.

```EME-L
' Декодирование HEX '
Result = is_fromhex("414243")
' Result = "ABC"
```

---

### is_simplified — упрощение строки

удаляет пробельные символы с начала и конца строки, а также заменяет последовательности пробельных символов в середине одним пробелом. Аналог `QString::simplified()` из Qt.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Исходная строка |

**Возвращает**: `String` — упрощённая строка.

```EME-L
' Упрощение '
Result = is_simplified("  Hello    World  ")
' Result = "Hello World"

' С табуляцией и переносами '
Result = is_simplified("  Line1\t\t\n\n  Line2  ")
' Result = "Line1 Line2"
```

---

### is_base64 — кодирование Base64

кодирует строку в формат Base64.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Исходная строка для кодирования |

**Возвращает**: `String` — строка в формате Base64.

```EME-L
' Кодирование '
Encoded = is_base64("Hello World")
' Encoded = "SGVsbG8gV29ybGQ="
```

---

### is_frombase64 — декодирование Base64

декодирует строку из формата Base64.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Строка в формате Base64 |
| args[1] | Integer | Нет | Атрибут типа данных для возвращаемого значения |

**Возвращает**:
- Без атрибута — `String` — декодированные данные как строка
- С атрибутом — тип определяется значением атрибута: бинарные данные в указанном формате

```EME-L
' Декодирование '
Decoded = is_frombase64("SGVsbG8gV29ybGQ=")
' Decoded = "Hello World"
```

---

### is_frombase64URL — декодирование Base64URL

декодирует строку из формата Base64URL.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Строка в формате Base64URL |
| args[1] | Integer | Нет | Атрибут типа данных для возвращаемого значения |

**Возвращает**:
- Без атрибута — `String` — декодированные данные как строка
- С атрибутом — тип определяется значением атрибута: бинарные данные в указанном формате

```EME-L
' Декодирование Base64URL '
Decoded = is_frombase64URL("SGVsbG8gV29ybGQ")
' Decoded = "Hello World"
```

---

### is_letter — проверка буквы

проверяет, является ли символ буквой. Принимает строку (проверяет первый символ) или числовой код.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String или Integer | Да | Тестируемый символ (строка) или его код (целое число) |

**Возвращает**: `Boolean` — результат проверки:
- `TRUE` — символ является буквой
- `FALSE` — символ не является буквой

```EME-L
' Проверка символа '
Result = is_letter("A")
' Result = TRUE

' Проверка кода '
Result = is_letter(65)
' Result = TRUE

' Проверка цифры '
Result = is_letter("1")
' Result = FALSE
```

---

### is_all_letters — проверка букв в строке

проверяет наличие букв в строке. В режиме по умолчанию проверяет, что все символы — буквы. В альтернативном режиме проверяет наличие хотя бы одной буквы.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Тестируемая строка |
| args[1] | Boolean | Нет | Если `TRUE`, проверяет что все символы — буквы. Если `FALSE`, проверяет наличие хотя бы одной буквы. По умолчанию `TRUE` |

**Возвращает**: `Boolean` — результат проверки:
- `TRUE` — в строке есть буквы (в соответствии с режимом)
- `FALSE` — в строке нет букв или не все символы являются буквами

```EME-L
' Все символы — буквы '
Result = is_all_letters("Hello")
' Result = TRUE

' Не все буквы '
Result = is_all_letters("Hello123")
' Result = FALSE

' Хотя бы одна буква '
Result = is_all_letters("123A456", FALSE)
' Result = TRUE

' Пустая строка '
Result = is_all_letters("")
' Result = FALSE
```

---

### is_digit — проверка цифры

проверяет, является ли символ десятичной цифрой. Принимает строку (проверяет первый символ) или числовой код.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String или Integer | Да | Тестируемый символ (строка) или его код (целое число) |

**Возвращает**: `Boolean` — результат проверки:
- `TRUE` — символ является цифрой
- `FALSE` — символ не является цифрой

```EME-L
' Проверка цифры '
Result = is_digit("5")
' Result = TRUE

' Проверка буквы '
Result = is_digit("A")
' Result = FALSE
```

---

### is_check_ccd_code — исправление раскладки клавиатуры

преобразует сканированный код из русской раскладки клавиатуры в английскую. Если текущая раскладка уже английская, возвращает исходный код без изменений.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Сканированный строковый код |

**Возвращает**: `String` — преобразованный код в английской раскладке.

```EME-L
' Исправление сканированного кода '
FixedCode = is_check_ccd_code(ScannedCode)
```

---

### is_SHA1Sum — хеш SHA1

вычисляет SHA1 хеш-сумму строки для безопасного хранения и сравнения.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Исходная строка для хеширования |

**Возвращает**: `String` — SHA1 хеш-сумма в виде шестнадцатеричной строки.

```EME-L
' Хеширование пароля '
Hash = is_SHA1Sum("my_password")
' Hash = "5baa61e4c9b93f3f0682250b6cf8331b7ee68fd8"
```

---

### is_hexstring2integer — HEX в число

преобразует строку с шестнадцатеричным числом в целое число. При длине более 8 символов возвращает 64-битное целое число.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Строка с шестнадцатеричным числом |

**Возвращает**: `Integer` — целое число, соответствующее шестнадцатеричной строке.

```EME-L
' Малое HEX-число '
Value = is_hexstring2integer("FF")
' Value = 255

' Большое HEX-число '
Value = is_hexstring2integer("FFFFFFFFFF")
' Value = 1099511627775 (int64)
```

---

### is_calc_crc_ean13 — контрольный символ EAN-13

вычисляет контрольный символ для штрих-кода EAN-13 и возвращает строку EAN-14.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Строка с кодом EAN-13 (должна содержать 13 цифр) |

**Возвращает**: `String` — строка EAN-14 (исходный код + контрольный символ) или `"ERROR"` при ошибке.

```EME-L
' Вычисление контрольного символа '
Ean14 = is_calc_crc_ean13("4601234567890")
' Ean14 = "4601234567890X" (где X — контрольный символ)

' Неверная длина '
Result = is_calc_crc_ean13("123")
' Result = "ERROR"
```

---

### is_split_EMEL — разбиение кода EME-L

разбивает строку на лексемы по правилам синтаксиса EME-L и записывает результат в массив. Комментарии в массив не попадают.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Исходная строка с кодом EME-L |
| args[1] | CIArray | Да | Массив для сохранения лексем |
| args[2] | Boolean | Да | `TRUE`, если строка начинается внутри многострочного комментария |

**Возвращает**: `Boolean` — `TRUE`, если по окончании разбора находимся внутри многострочного комментария.

```EME-L
' Разбиение кода '
Dim Tokens()
InComment = is_split_EMEL("Var = 10 + 20", Tokens, FALSE)
' Tokens содержит лексемы: ["Var", "=", "10", "+", "20"]
```

---

### is_utf8_bom — проверка BOM UTF-8

проверяет наличие BOM (Byte Order Mark) сигнатуры UTF-8 в начале строки. При вызове без параметров возвращает саму сигнатуру BOM.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Нет | Строка для проверки. Необязательный параметр |

**Возвращает**:
- Без аргументов — `String` — сигнатура BOM (`"\xEF\xBB\xBF"`)
- С аргументом — `Boolean` — результат проверки:
  - `TRUE` — строка начинается с BOM-сигнатуры UTF-8
  - `FALSE` — строка не содержит BOM-сигнатуру

```EME-L
' Получение BOM '
Bom = is_utf8_bom()
' Bom = "\xEF\xBB\xBF"

' Проверка строки '
HasBom = is_utf8_bom(Utf8String)
' HasBom = TRUE или FALSE
```

---

### is_split_csv — разбиение CSV

разбивает строку на поля согласно стандарту CSV (RFC 4180), начиная с указанной позиции. Поддерживает поля в кавычках и экранирование кавычек удвоением.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | CIString | Да | Объект строки CIString |
| args[1] | Integer | Да | Начальная позиция для разбора |
| args[2] | String | Да | Символ-разделитель полей (строка длиной 1 символ) |
| args[3] | CIArray | Да | Массив для сохранения разобранных полей |

**Возвращает**: `Integer` — позиция символа переноса строки или `-1`, если его нет.

```EME-L
' Разбор CSV-строки '
Dim Fields()
LinePos = is_split_csv(CsvString, 0, ";", Fields)
' Fields содержит разобранные поля
```

---

### is_find_w — поиск в UTF-16

ищет первое вхождение подстроки в строке UTF-16, начиная с указанной позиции.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Исходная строка UTF-16 |
| args[1] | String | Да | Искомая подстрока UTF-16 |
| args[2] | Integer | Нет | Позиция, с которой начинать поиск. По умолчанию 0 |

**Возвращает**: `Integer` — индекс первого найденного вхождения или `-1`, если не найдено.

```EME-L
' Поиск в UTF-16 '
Pos = is_find_w(Utf16String, Substring)
' Pos = позиция найденной подстроки
```

---

### is_left_w — символы с начала UTF-16

возвращает заданное количество символов с начала строки UTF-16.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Исходная строка UTF-16 |
| args[1] | Integer | Да | Количество символов для извлечения |

**Возвращает**: `String` — подстрока указанной длины с начала строки UTF-16.

```EME-L
' Первые символы UTF-16 '
Result = is_left_w(Utf16String, 5)
```

---

### is_mid_w — подстрока из середины UTF-16

возвращает подстроку из середины строки UTF-16, начиная с указанной позиции. Если количество не указано, возвращает все символы до конца строки.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Исходная строка UTF-16 |
| args[1] | Integer | Да | Начальная позиция подстроки, отсчёт с 0 |
| args[2] | Integer | Нет | Количество символов для извлечения. Если не указано — возвращаются все символы до конца строки |

**Возвращает**: `String` — подстрока из середины строки UTF-16.

```EME-L
' Подстрока UTF-16 '
Result = is_mid_w(Utf16String, 2, 5)
```

---

**Всего функций:** 68

См. также: [Ядро языка](./core) — базовые is-функции, [Файловая система](./filesystem) — работа с путями и файлами, [Дата и время](./datetime) — форматирование и парсинг дат.
