---
title: "Профилирование, отладка и прочее"
sidebar:
  order: 16
  label: "Профилирование, отладка и прочее"
---

## Профилирование, отладка и прочее

is-функции группы Debug отвечают за отладку, профилирование, диагностику, экспорт/импорт данных, управление консолью вывода и вспомогательные операции разработки. в системе EME.WMS эти функции используются при разработке метапроектов, тестировании и диагностике проблем.

---

## Профилирование

### is_prf — профилирование

функция для профилирования кода. Аналог ядерных функций профилирования.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Тип: 0 — reset_prf, 1 — start_prf, 2 — stop_prf, 3 — get_prf |
| args[1] | Integer | Нет | Номер таймера (0-31). Для args[0]=0 не нужен |

**Возвращает**: при args[0]=3 — время в мс. В остальных случаях — ничего.

```EME-L
' Запуск таймера 0 '
is_prf(1, 0)

' Остановка '
is_prf(2, 0)

' Получить время '
time = is_prf(3, 0)
```

---

### is_profile_point — профилируемая точка

в языке EME-L функция `is_profile_point` регистрирует профилируемую точку в метапроектном профайлере. Измеряет время от начала текущего метода или от предыдущей точки до себя. Для учёта точек в диалоге EMEProfiler необходимо установить флаг Point.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Имя профилируемой точки |

**Возвращает**: пустое значение.

```EME-L
' Отметить точку входа в метод '
is_profile_point("Вход_в_Обработку")

' ... код обработки ... '

' Отметить точку выхода '
is_profile_point("Выход_из_Обработки")
```

---

### is_profile_name — идентификатор профилирования

в системе EME.WMS функция `is_profile_name` задаёт прикладной идентификатор результатов профилирования. Добавляется к системному идентификатору через точку.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Прикладное имя профилирования |

**Возвращает**: пустое значение.

```EME-L
' Задать имя для сессии профилирования '
is_profile_name("Подтверждение приказа на подборку")

' Теперь результаты будут помечены этим именем '
```

---

## Отладка и ошибки

### is_breakpoint — точка останова

в языке EME-L функция `is_breakpoint` выводит диагностическое сообщение «Точка останова» через механизм smart_error. При каждом вызове переключает внутренний флаг, позволяя пропускать чередующиеся вызовы.

**Параметры**: нет.

**Возвращает**: пустое значение.

```EME-L
' Установить точку останова для отладки '
is_breakpoint()
' Выведено сообщение "Точка останова" '
```

---

### is_error — вывод ошибки

в EME БД функция `is_error` выводит сообщение об ошибке указанного типа.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Тип ошибки: 0 — stealth_error (скрытая), 1 — smart_error (с сообщением), 2 — user_error (пользовательская), 3 — soft_error (программная), 4 — developer_error (ошибка разработчика) |
| args[1] | String | Да | Текст сообщения об ошибке |

**Возвращает**: пустое значение.

```EME-L
' Вывести сообщение об ошибке с диалогом '
is_error(1, "Неверное значение поля")

' Скрытая ошибка — без диалога '
is_error(0, "Ошибка в фоновой операции")

' Ошибка разработчика с трассировкой '
is_error(4, "Нулевой указатель в методе X")
```

---

### is_catastrophic_log — запись в системный лог

в системе EME.WMS функция `is_catastrophic_log` записывает текстовое сообщение в системный лог (catastrf.log или буферизованный лог в зависимости от конфигурации).

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Текст сообщения для записи в лог |

**Возвращает**: пустое значение.

```EME-L
' Записать событие в системный лог '
is_catastrophic_log("Начало импорта данных")

' Записать ошибку '
is_catastrophic_log("Ошибка подключения к серверу БД")
```

---

### is_log — синоним is_catastrophic_log

в языке EME-L функция `is_log` является синонимом `is_catastrophic_log`. Записывает текстовое сообщение в системный лог.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Текст сообщения для записи в лог |

**Возвращает**: пустое значение.

```EME-L
' Записать в лог через синоним '
is_log("Проверка завершена успешно")
```

---

### is_post_console_out — вывод в журнал мозгового компьютера

в EME БД функция `is_post_console_out` выводит сообщение в журнал (консоль) мозгового компьютера.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Текст сообщения для вывода |

**Возвращает**: пустое значение.

```EME-L
' Вывести сообщение в консоль мозгового компьютера '
is_post_console_out("Обработка записи Клиенты, строка 5")
```

---

### is_debug — проверка debug-сборки

в языке EME-L функция `is_debug` возвращает признак сборки текущего модуля в режиме отладки (_DEBUG).

**Параметры**: нет.

**Возвращает**: `Boolean` — `TRUE` если модуль собран в режиме отладки, иначе `FALSE`.

```EME-L
' В debug-режиме включить профилирование операций '
If (is_debug())
    is_profile_name("Отладочная_сессия")
    is_profile_point("Старт_обработки")
End If
```

---

### is_debug_mode — проверка отладочного режима EME-L

в системе EME.WMS функция `is_debug_mode` возвращает признак включения отладочного режима в EME-L.

**Параметры**: нет.

**Возвращает**: `Boolean` — `TRUE` если отладка включена, иначе `FALSE`.

```EME-L
' В отладочном режиме выводить диагностику в консоль '
If (is_debug_mode())
    is_add_string_to_output("LOG", "Отладка: запуск обработки записи %s", "Клиенты")
    is_profile_point("Начало_обработки")
End If
```

---

### is_ui_test_mode — режим тестирования UI

возвращает TRUE, если программа в режиме тестирования интерфейса. В этом режиме база последовательно запускает все пункты меню. Прикладной код может использовать для отключения браузеров и MessageBox.

**Параметры**: нет.

**Возвращает**: `Boolean` — `TRUE` если включён режим тестирования.

```EME-L
' В режиме тестирования UI не показывать MessageBox '
If (is_ui_test_mode())
    showMsg = FALSE
End If
```

---

## Экспорт и импорт данных

### is_eximport — экспорт/импорт массива

в языке EME-L функция `is_eximport` выполняет экспорт или импорт одномерного массива в бинарный файл.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Каталог файла |
| args[1] | String | Да | Имя файла |
| args[2] | Reference | Да | Массив данных (CIArray) |
| args[3] | Integer | Да | Направление: 1 — экспорт, -1 — импорт |

**Возвращает**: пустое значение.

```EME-L
' Экспорт массива в файл '
Arr = Object("Array")
Arr.Add("Элемент1")
Arr.Add("Элемент2")
is_eximport("C:\\Temp", "data.bin", Arr, 1)

' Импорт массива из файла '
Arr2 = Object("Array")
is_eximport("C:\\Temp", "data.bin", Arr2, -1)
```

---

### is_eximport2 — экспорт/импорт именованных данных v2

в EME БД функция `is_eximport2` выполняет экспорт/импорт именованных данных версии 2. Записывает массив имён и связанных с ними данных в бинарный файл.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Каталог файла |
| args[1] | String | Да | Имя файла |
| args[2] | Integer | Да | Направление: 1 — экспорт, -1 — импорт |
| args[3] | Reference | Да | Массив наименований (CIArray) |
| args[4] | Reference | Да | Массив значений (CIArray) |
| args[5] | Integer | Да | Количество значений в data на одно имя в names |

**Возвращает**: `Boolean` — `TRUE` при успехе, иначе `FALSE`.

```EME-L
' Экспорт именованных данных '
Names = Object("Array")
Names.Add("Параметр1")
Names.Add("Параметр2")
Data = Object("Array")
Data.Add("Значение1")
Data.Add("Значение2")
Result = is_eximport2("C:\\Temp", "config.bin", 1, Names, Data, 1)
' Result = TRUE '
```

---

### is_eximport3 — экспорт/импорт именованных данных v3

в языке EME-L функция `is_eximport3` выполняет экспорт/импорт именованных данных версии 3. Формат файла включает номер версии (101) и поддерживает обратную совместимость.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Каталог файла |
| args[1] | String | Да | Имя файла |
| args[2] | Integer | Да | Направление: 1 — экспорт, -1 — импорт |
| args[3] | Reference | Да | Массив наименований (CIArray) |
| args[4] | Reference | Да | Массив значений (CIArray) |
| args[5] | Integer | Да | Количество значений в data на одно имя в names |

**Возвращает**: `Integer` — количество значений на одно имя при успехе, 0 при ошибке.

```EME-L
' Импорт данных версии 3 '
Names = Object("Array")
Data = Object("Array")
Count = is_eximport3("C:\\Temp", "config_v3.bin", -1, Names, Data, 1)
' Count = 1 — импорт выполнен '
```

---

### is_merge_EMEL — слияние EME-L кода

в системе EME.WMS функция `is_merge_EMEL` запускает внешнюю утилиту сравнения/слияния (TortoiseMerge) для объединения конструктора и методов EME-L класса.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Имя класса |
| args[1] | String | Да | Конструктор из базы данных |
| args[2] | String | Да | Методы из базы данных |
| args[3] | Reference | Да | Ссылка на строку конструктора из импорта (изменяется) |
| args[4] | Reference | Да | Ссылка на строку методов из импорта (изменяется) |
| args[5] | String | Да | Командная строка для запуска merge-утилиты |

**Возвращает**: пустое значение.

```EME-L
' Объединить код класса через внешний merge '
MineCtor = ""
MineMethods = ""
is_merge_EMEL("COrder", TheirsCtor, TheirsMethods, MineCtor, MineMethods, "TortoiseMerge.exe /base:%s /mine:%s")
```

---

## Консоль вывода

### is_add_string_to_output — вывод в консоль

в языке EME-L функция `is_add_string_to_output` выводит форматированное сообщение в окно системных сообщений (консоль вывода). Работает аналогично printf.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Тип вывода: "LOG" — журнал, "TRAN" — транзакции, "ERROR" — ошибки, "USER_ACTION" — действия пользователя |
| args[1] | String | Да | Строка формата сообщения |
| args[2+] | Various | Нет | Дополнительные аргументы для подстановки |

**Возвращает**: пустое значение.

```EME-L
' Вывести сообщение в журнал '
is_add_string_to_output("LOG", "Обработано %d записей", 42)

' Вывести ошибку '
is_add_string_to_output("ERROR", "Ошибка в файле %s", "data.txt")
```

---

### is_clear_output_console — очистка консоли

в EME БД функция `is_clear_output_console` очищает окно системных сообщений (консоль вывода).

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Нет | Тип очищаемой вкладки: "LOG", "TRAN", "ERROR", "USER_ACTION". Без параметра — очищает все |

**Возвращает**: пустое значение.

```EME-L
' Очистить все вкладки консоли '
is_clear_output_console()

' Очистить только журнал '
is_clear_output_console("LOG")
```

---

### is_show_output_console — показать/скрыть консоль

в языке EME-L функция `is_show_output_console` показывает или скрывает окно системных сообщений (консоль вывода).

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Boolean | Да | `TRUE` — показать консоль, `FALSE` — скрыть |

**Возвращает**: пустое значение.

```EME-L
' Показать консоль вывода '
is_show_output_console(TRUE)

' Скрыть консоль '
is_show_output_console(FALSE)
```

---

## Информация о системе

### is_project_list — список проектов

в системе EME.WMS функция `is_project_list` заполняет переданный массив списком проектов системы.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Reference | Да | Массив для заполнения (CIArray) |

**Возвращает**: пустое значение.

```EME-L
' Получить список проектов '
Projects = Object("Array")
is_project_list(Projects)
' Projects содержит имена проектов '
```

---

### is_get_type — тип значения

в языке EME-L функция `is_get_type` возвращает строковое представление типа переданного значения.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Various | Да | Произвольное значение для определения типа |

**Возвращает**: `String` — имя типа данных (например, "VT_BSTR", "VT_I4", "VT_R8").

```EME-L
' Определить тип переменной '
TypeName = is_get_type("Строка")
' TypeName = "VT_BSTR" '

TypeName = is_get_type(42)
' TypeName = "VT_I4" '
```

---

### is_help_file — генерация справки

в EME БД функция `is_help_file` сохраняет текст справки указанного класса в HTML-файл.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Имя класса для получения справки |
| args[1] | String | Да | Имя целевого HTML-файла |

**Возвращает**: пустое значение.

```EME-L
' Сохранить справку по классу в HTML '
is_help_file("COrder", "C:\\Temp\\COrder_help.html")
```

---

### is_object_ptr — указатель на объект

в системе EME.WMS функция `is_object_ptr` возвращает числовое представление указателя на объект EME-L.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Reference | Да | Объект EME-L |

**Возвращает**: `Integer` — числовое значение указателя на объект.

```EME-L
' Получить указатель на объект '
Obj = Object("Array")
Ptr = is_object_ptr(Obj)
' Ptr = числовой адрес объекта '
```

---

## Диагностика памяти

### is_mem_dump — дамп памяти

записывает в катастроф-лог информацию об использовании памяти.

**Параметры**: нет.

**Возвращает**: ничего.

```EME-L
is_mem_dump()
```

---

### is_SYS_mem_dump — дамп пула SYS

выполняет дамп пула SYS.

**Параметры**: нет.

**Возвращает**: пустое значение.

```EME-L
is_SYS_mem_dump()
```

---

### is_memory_leak — создание утечки памяти

в языке EME-L функция `is_memory_leak` создаёт утечку памяти указанного размера для тестирования. Выделяет блок памяти без последующего освобождения.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Размер выделяемого блока памяти в байтах |

**Возвращает**: пустое значение.

```EME-L
' Создать утечку памяти 1 КБ для тестирования '
is_memory_leak(1024)
```

---

### is_mem_usagestat — статистика памяти

в системе EME.WMS функция `is_mem_usagestat` собирает статистику использования памяти в debug-сборке. Позволяет определить утечки памяти на участке кода.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Режим: 1 — начать учёт, -1 — завершить учёт и вывести дамп |
| args[1] | String | Нет | Имя анализируемого файла |
| args[2] | Integer/String | Нет | Номер строки или имя модуля |
| args[3] | Integer | Нет | Размер таблицы учёта |

**Возвращает**: пустое значение.

```EME-L
' Начать учёт памяти '
is_mem_usagestat(1)

' ... код для анализа ... '

' Завершить учёт и вывести дамп '
is_mem_usagestat(-1)
```

---

## Тестирование и диагностика

### is_lost_fields — проверка совместимости метапроекта

в языке EME-L функция `is_lost_fields` проверяет совместимость метапроекта с данными. Ищет поля, используемые классами CEMERec, но отсутствующие в базе, а также несоответствия типов и дублирующиеся методы.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Reference | Да | Массив для заполнения отсутствующими полями (CIArray). Формат: имя_класса\|имя_записи\|имя_метода\|имя_поля |
| args[1] | Reference | Нет | Массив для ошибок типов (CIArray). Формат: имя_класса\|имя_записи\|тип_метода\|тип_поля |
| args[2] | Reference | Нет | Массив для дубликатов методов (CIArray). Формат: имя_класса\|имя_метода |

**Возвращает**: пустое значение.

```EME-L
' Проверить совместимость метапроекта '
Missing = Object("Array")
TypeErrors = Object("Array")
Dups = Object("Array")
is_lost_fields(Missing, TypeErrors, Dups)

' Если Missing не пуст — есть отсутствующие поля '
If (Missing.GetSize() > 0)
    is_error(1, "Обнаружены несоответствия метапроекта")
End If
```

---

### is_switch_workstation_mode — переключение режима работы

в EME БД функция `is_switch_workstation_mode` переключает режим работы станции между монопольным и рабочей станцией. При переключении в режим рабочей станции останавливает фоновые потоки, при обратном переключении возобновляет их.

**Параметры**: нет.

**Возвращает**: `Boolean` — `TRUE` при успешном переключении, иначе `FALSE`.

```EME-L
' Переключить режим работы '
Result = is_switch_workstation_mode()
' Result = TRUE — режим переключён '
```

---

### is_run_robot — запуск робота

в языке EME-L функция `is_run_robot` запускает робота для автоматического тестирования диалогов. После завершения работы робот посылает уведомление в вызвавший диалог.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Имя робота: "RobotOpenDialogs", "RobotStaffPosition", "RobotStaff" |
| args[1] | Integer | Нет | Пошаговая задержка в мс. По умолчанию 500 |
| args[2] | Integer | Нет | Таймаут выполнения в секундах. По умолчанию 500 |
| args[3] | Integer | Нет | Признак скрытия диалога. По умолчанию 0 |

**Возвращает**: пустое значение.

```EME-L
' Запустить робота для тестирования диалогов '
is_run_robot("RobotOpenDialogs", 1000, 600, 0)
```

---

### is_server_log — лог сервера БД

в системе EME.WMS функция `is_server_log` получает лог сервера базы данных и сохраняет его в файл.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Нет | Имя файла для сохранения. По умолчанию "DUMP\\CATASTRF.LOG" |

**Возвращает**: пустое значение.

```EME-L
' Получить лог сервера в файл по умолчанию '
is_server_log()

' Сохранить в указанный файл '
is_server_log("server_log.txt")
```

---

### is_vfs_test — тест виртуальной файловой системы

в языке EME-L функция `is_vfs_test` запускает юнит-тесты виртуальной файловой системы (VFS).

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Нет | Номер теста. По умолчанию тест целостности таблицы размещения |
| args[1] | Integer | Нет | Дополнительный параметр теста. По умолчанию 0 |
| args[2] | Integer | Нет | Предыдущее значение для проверки. По умолчанию 0 |

**Возвращает**: пустое значение.

```EME-L
' Запустить тест VFS по умолчанию '
is_vfs_test()

' Запустить конкретный тест '
is_vfs_test(5)
```

---

## Трассировка и внутренние

### is_OLE_error_trace — трассировка OLE

в EME БД функция `is_OLE_error_trace` включает или отключает трассировку ошибок OLE в системном логе.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Boolean | Да | `TRUE` — включить трассировку, `FALSE` — отключить |

**Возвращает**: пустое значение.

```EME-L
' Включить трассировку OLE '
is_OLE_error_trace(TRUE)

' Отключить '
is_OLE_error_trace(FALSE)
```

---

### is_trace_exist — объект трассировки

в языке EME-L функция `is_trace_exist` возвращает ранее созданный объект трассировки по его имени. Внимание: гарантия сохранности возвращаемого объекта не обеспечивается, так как CTraceManager использует автоматические указатели, которые нельзя экспортировать в EME-L.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Софтовое имя диалога трассировки |

**Возвращает**: ссылка на объект трассировки или пустое значение, если объект не найден.

```EME-L
' Получить объект трассировки '
TraceObj = is_trace_exist("МояТрассировка")
```

---

### is_resolve_double_keys — разрешение задвоенных ключей

в системе EME.WMS функция `is_resolve_double_keys` запускает механизм устранения задвоенных ключей в базе данных.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Reference | Да | Массив задвоенных ключей (CIArray) |

**Возвращает**: пустое значение.

```EME-L
' Разрешить задвоенные ключи '
Keys = Object("Array")
Keys.Add(12345)
Keys.Add(67890)
is_resolve_double_keys(Keys)
```

---

### is_OLEAUT32_error — тестовое исключение OLEAUT32

в языке EME-L функция `is_OLEAUT32_error` генерирует принудительное исключение OLEAUT32 для тестирования обработки ошибок. При вызове происходит аварийное завершение/исключение.

**Параметры**: нет.

**Возвращает**: пустое значение (фактически управление не возвращается).

```EME-L
' ВНИМАНИЕ: генерирует исключение! '
' Использовать только для тестирования обработки ошибок '
is_OLEAUT32_error()
```

---

## Планировщик и события

### is_eventer_tick — запуск планировщика

запускает процедуры планировщика.

**Параметры**: нет.

**Возвращает**: `Integer` — результат отправки WM_TIMER, 0 при ошибке.

```EME-L
is_eventer_tick()
```

---

### is_force_standard_export — стандартный экспорт

Инициирует принудительный стандартный экспорт данных. Вызов QueryExport.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Boolean | Нет | `TRUE` — выдавать предупреждение перед экспортом (по умолчанию `TRUE`) |

**Возвращает**: `Integer` — результат запроса на экспорт. При отсутствии поддержки семафоров возвращает 0.

```EME-L
is_force_standard_export(TRUE)
```

---

## Прочее

### is_generate_NSI_script — генерация инсталляционного скрипта

генерирует инсталляционный скрипт.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Нет | Постфикс имени скрипта |
| args[1] | Integer | Нет | 0 — использовать текущий каталог, 1 — стандартные папки |
| args[2] | String | Нет | Путь к папке данных |
| args[3] | String | Нет | Путь к папке метапроекта |
| args[4] | String | Нет | Путь к папке ядра. При передаче 6 параметров игнорируется |
| args[5] | String | Нет | Имя выходного файла |
| args[6] | String | Нет | Путь к файлу лицензии |

**Возвращает**: ничего.

```EME-L
is_generate_NSI_script()
```

---

### is_OLE_serverbusydelay — задержка Server Busy

устанавливает задержку появления диалога OLE Server Busy.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Нет | Задержка в мс (по умолчанию 5000) |

**Возвращает**: `Boolean` — `TRUE` при успехе, `FALSE` если фильтр недоступен.

```EME-L
is_OLE_serverbusydelay(10000)
```

---

### is_get_data_path — путь к папке данных

возвращает путь к папке данных текущего проекта. Вызов GetProjectDataDir.

**Параметры**: нет.

**Возвращает**: `String` — путь к папке данных.

```EME-L
path = is_get_data_path()
```

---

### robot — ссылка на робота

возвращает ссылку на текущего выполняющегося робота (CIRobot). Глобальный объект, доступный из EME-L без явной передачи параметра.

**Параметры**: нет.

**Возвращает**: ссылку на объект робота (ссылка).

```EME-L
r = robot()
```

---

### is_enable_dataslice — включение срезов

включает режим рабочей станции для работы со срезами данных.

**Параметры**: нет.

**Возвращает**: `Boolean` — `TRUE` при успехе, `FALSE` если условия не выполнены.

```EME-L
' Включить режим работы со срезами данных '
result = is_enable_dataslice()
```

---

### is_restart_as_server_request — запрос на переключение сервера

отправляет на резервный сервер запрос на переключение в режим сервера БД. IP адрес берёт из настройки рабочей станции.

**Параметры**: нет.

**Возвращает**: `String` — текст ошибки или пусто при успехе.

```EME-L
err = is_restart_as_server_request()
```

---

### is_sys_import — системный импорт

выполняет "ручной" системный импорт по файлам из указанной папки.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Путь к папке с файлами импорта |

**Возвращает**: `Integer` — результат выполнения (код EEXPORTRESULT).

```EME-L
result = is_sys_import("C:\\import\\")
```

---

См. также: [EME_L_Syntax.md](EME_L_Syntax.md) — синтаксис EME-L, [EME_L_IS_Functions.md](EME_L_IS_Functions.md) — is-функции групп Bit/Browser/Context, [EME_L_Classes.md](EME_L_Classes.md) — объектная модель.
