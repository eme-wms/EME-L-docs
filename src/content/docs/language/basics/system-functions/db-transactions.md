---
title: "База данных: транзакции и модификация"
sidebar:
  order: 14
  label: "База данных: транзакции и модификация"
---

## База данных: транзакции и модификация

is-функции группы DB (транзакции и модификация) отвечают за управление серверными и локальными транзакциями, модификацию строк и записей, сжатие файлов базы данных и служебные операции. в системе EME.WMS эти функции обеспечивают атомарность операций, контроль целостности данных и оптимизацию хранения.

---

### is_transaction — управление серверной транзакцией

в языке EME-L функция `is_transaction` открывает, закрывает или отменяет серверную транзакцию. Является основной функцией для управления транзакциями в многопользовательском режиме.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Нет | Направление: 1 — открыть, -1 — закрыть, 0 — отменить. Если не указан, только проверяет состояние |
| args[1] | String / Boolean | Нет | При открытии: название транзакции (6-63 символа). При закрытии: `FALSE` — не бросать исключение при ошибке |
| args[2] | Integer / String | Нет | При открытии: приоритет транзакции: 0/"LOW"/"НИЗ" — низкий, 1/"NORMAL"/"НОРМ" — нормальный (по умолчанию), 2/"HIGH"/"ВЫС" — высокий |
| args[3] | Integer | Нет | При открытии: таймаут транзакции в миллисекундах. По умолчанию не ограничен |
| args[4] | Boolean | Нет | При открытии: `TRUE` — бросать исключение при таймауте на рабочей станции. По умолчанию `FALSE` |

**Возвращает**: `Boolean` — `TRUE` если транзакция открыта после выполнения операции, иначе `FALSE`.

```EME-L
' Открыть транзакцию с названием и нормальным приоритетом '
is_transaction(1, "Создание заказа")

' Открыть с высоким приоритетом и таймаутом 30 секунд '
is_transaction(1, "Срочное обновление", "HIGH", 30000)

' Закрыть транзакцию '
is_transaction(-1)

' Отменить транзакцию '
is_transaction(0)

' Проверить состояние транзакции '
IsOpen = is_transaction()
' IsOpen = TRUE '
```

---

### is_open_transaction — условное открытие транзакции

в системе EME.WMS функция `is_open_transaction` открывает серверную транзакцию, если нет другой открытой.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Название транзакции |

**Возвращает**: `Boolean` — `TRUE` если транзакция открыта, иначе `FALSE`.

```EME-L
' Открыть транзакцию, если нет другой '
Opened = is_open_transaction("Обновление данных")
' Opened = TRUE '

' Повторный вызов — транзакция уже открыта '
Opened = is_open_transaction("Другая транзакция")
' Opened = FALSE '
```

---

### is_open_master_transaction — открытие мастер-транзакции

в языке EME-L функция `is_open_master_transaction` открывает мастер-транзакцию в рабочем разделе. В мастер-разделе ничего не делает.

**Параметры**: нет.

**Возвращает**: пустое значение.

```EME-L
' Открыть мастер-транзакцию '
is_open_master_transaction()
' Мастер-транзакция открыта '
```

---

### is_abort_transaction — аварийное завершение транзакции

в EME БД функция `is_abort_transaction` аварийно завершает текущую транзакцию. Не нарушает парность открытия и закрытия транзакций.

**Параметры**: нет.

**Возвращает**: пустое значение.

```EME-L
' Аварийно завершить транзакцию '
is_abort_transaction()
' Транзакция аварийно завершена '
```

---

### is_local_transaction — управление локальной транзакцией

в языке EME-L функция `is_local_transaction` открывает или закрывает локальную транзакцию.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Нет | Направление: 1 — открыть, -1 — закрыть |

**Возвращает**: пустое значение.

```EME-L
' Открыть локальную транзакцию '
is_local_transaction(1)

' Закрыть локальную транзакцию '
is_local_transaction(-1)
```

---

### is_emulate_transaction — эмуляция транзакции

в системе EME.WMS функция `is_emulate_transaction` открывает или закрывает эмулированную транзакцию без уведомления сервера.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Направление: 1 — открыть, -1 — закрыть и отменить |

**Возвращает**: пустое значение.

```EME-L
' Открыть эмулированную транзакцию '
is_emulate_transaction(1)

' Закрыть и отменить эмуляцию '
is_emulate_transaction(-1)
```

---

### is_enable_multiple_transactions — снятие ограничения на транзакции

в языке EME-L функция `is_enable_multiple_transactions` снимает ограничение на количество подряд выполняемых серверных транзакций в текущем стеке.

**Параметры**: нет.

**Возвращает**: пустое значение.

```EME-L
' Разрешить множественные транзакции в стеке '
is_enable_multiple_transactions()
' Ограничение снято '
```

---

### is_modify_record — пометка всех строк записи

в EME БД функция `is_modify_record` помечает все строки указанных записей как модифицированные. Автоматически открывает и закрывает серверную транзакцию.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0+] | String / Integer | Да | Номер или имя записи. Можно указать несколько записей |

**Возвращает**: `Boolean` — `TRUE`.

```EME-L
' Пометить все строки записи Клиенты как модифицированные '
is_modify_record("Клиенты")

' Пометить несколько записей '
is_modify_record("Клиенты", "Заказы", "Товары")
```

---

### is_unmodify_record — снятие пометок модификации

в языке EME-L функция `is_unmodify_record` снимает пометки модификации со всех строк указанных записей.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0+] | String / Integer / Reference | Да | Номер, имя записи или объект массива с номерами/именами |

**Возвращает**: `Boolean` — `TRUE`.

```EME-L
' Снять пометки с записи Клиенты '
is_unmodify_record("Клиенты")

' Снять пометки через массив '
RecArray = Object("Array")
RecArray.Add("Клиенты")
RecArray.Add("Заказы")
is_unmodify_record(RecArray)
```

---

### is_modify_line — пометка строки

в системе EME.WMS функция `is_modify_line` помечает строку записи как модифицированную.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String / Integer | Да | Имя или номер записи |
| args[1] | Integer | Да | Номер строки |

**Возвращает**: пустое значение.

```EME-L
' Пометить строку 5 записи Клиенты '
is_modify_line("Клиенты", 5)
' Строка помечена '
```

---

### is_unmodify_lines — снятие флагов с группы строк

в языке EME-L функция `is_unmodify_lines` снимает флаг модификации с группы строк записи.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String / Integer | Да | Имя или номер записи |
| args[1] | Integer | Да | Номер первой строки |
| args[2] | Integer | Да | Количество строк, максимум 100000 |

**Возвращает**: `Integer` — количество сброшенных флагов модификации.

```EME-L
' Снять флаги с 100 строк, начиная со строки 0 '
Count = is_unmodify_lines("Клиенты", 0, 100)
' Count = 95 - снято 95 флагов '
```

---

### is_delete_record_line — удаление строки

в EME БД функция `is_delete_record_line` удаляет строку записи из базы данных на уровне ядра.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String / Integer | Да | Имя или номер записи |
| args[1] | Integer | Да | Номер строки |

**Возвращает**: `Boolean` — `TRUE` если строка удалена, иначе `FALSE`.

```EME-L
' Удалить строку 5 записи Клиенты '
Result = is_delete_record_line("Клиенты", 5)
' Result = TRUE '
```

---

### is_delete_records — удаление всех строк в записях

в языке EME-L функция `is_delete_records` удаляет все строки в указанных записях и обнуляет ссылки на них.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0+] | String / Integer / Reference | Да | Номер, имя записи или массив имён и номеров |

**Возвращает**: пустое значение.

```EME-L
' Удалить все строки в записи Клиенты '
is_delete_records("Клиенты")

' Удалить строки в нескольких записях '
is_delete_records("Клиенты", "Заказы")

' Через массив '
RecArray = Object("Array")
RecArray.Add("Клиенты")
is_delete_records(RecArray)
```

---

### is_compress_records — сжатие файлов записей

в системе EME.WMS функция `is_compress_records` выполняет физическое сжатие файлов записей базы данных за счет удалённых строк.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Режим: 0 — сохранить физическую последовательность, 1 — оптимизированное сжатие |
| args[1] | Reference | Да | Массив номеров или имён записей для сжатия |

**Возвращает**: `Boolean` — `TRUE` в случае успеха.

```EME-L
' Сжать записи с оптимизацией '
RecArray = Object("Array")
RecArray.Add("Клиенты")
RecArray.Add("Заказы")
Result = is_compress_records(1, RecArray)
' Result = TRUE '
```

---

### is_prune_record — сжатие одной записи

в языке EME-L функция `is_prune_record` сжимает файл одной записи после удаления строк.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Режим сжатия |
| args[1] | Integer | Да | Номер записи |

**Возвращает**: `Integer` — 0.

```EME-L
' Сжать запись 10 '
Result = is_prune_record(0, 10)
' Result = 0 '
```

---

### is_compress_vlf — сжатие поля переменной длины

в EME БД функция `is_compress_vlf` выполняет сжатие поля переменной длины (VLF) записи.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String / Integer | Да | Имя или номер записи |
| args[1] | String / Integer | Да | Имя или номер поля |

**Возвращает**: `Integer` или пустое значение — результат сжатия.

```EME-L
' Сжать VLF-поле Данные записи Документы '
Result = is_compress_vlf("Документы", "Данные")
' Result = 0 '
```

---

### is_empty_vlf — опустошение VLF-поля

в языке EME-L функция `is_empty_vlf` опустошает поле переменной длины (VLF) в указанной строке записи.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String / Integer | Да | Имя или номер записи |
| args[1] | String / Integer | Да | Имя или номер поля |
| args[2] | Integer | Да | Номер строки |

**Возвращает**: пустое значение.

```EME-L
' Опустошить VLF-поле в строке 5 '
is_empty_vlf("Документы", "Данные", 5)
' Поле опустошено '
```

---

### is_ws_align_data — выравнивание данных с сервера

в системе EME.WMS функция `is_ws_align_data` выполняет выравнивание данных с сервера на текущую рабочую станцию.

**Параметры**: нет.

**Возвращает**: `Boolean` — `TRUE` если выравнивание выполнено успешно, иначе `FALSE`.

```EME-L
' Выполнить выравнивание данных '
Result = is_ws_align_data()
' Result = TRUE '
```

---

### is_disable_align_data — отключение выравнивания

в языке EME-L функция `is_disable_align_data` отключает или управляет автоматическим выравниванием данных с сервера.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Нет | Флаг: 1 — отключить, -1 — включить, 0 — вернуть состояние |

**Возвращает**: пустое значение или `Integer` — текущее состояние выравнивания.

```EME-L
' Отключить автоматическое выравнивание '
is_disable_align_data()

' Включить обратно (старая версия) '
State = is_disable_align_data(-1)
' State = 0 - выравнивание включено '
```

---

### is_reverse_db_data — реверс данных в БД

в EME БД функция `is_reverse_db_data` производит реверс данных в полях базы данных для тестового выравнивания. Работает только в монопольном режиме.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Тип полей: "VAR" — переменной длины, "FIX" — фиксированной, "ALL" — все |
| args[1] | Integer | Нет | Начальная запись. По умолчанию 0 |
| args[2] | Integer | Нет | Конечная запись. По умолчанию последняя |

**Возвращает**: пустое значение.

```EME-L
' Реверс всех данных '
is_reverse_db_data("ALL")

' Реверс полей переменной длины с записи 10 по 50 '
is_reverse_db_data("VAR", 10, 50)
```

---

### is_set_header_correction — коррекция заголовка БД

в языке EME-L функция `is_set_header_correction` включает или выключает режим прямой записи в заголовок базы данных.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | 1 — включить коррекцию, 0 — выключить |

**Возвращает**: пустое значение.

```EME-L
' Включить коррекцию заголовка перед транзакцией '
is_set_header_correction(1)
is_transaction(1, "Коррекция структуры")
' ... операции с заголовком ... '
is_transaction(-1)
is_set_header_correction(0)
```

---

### is_set_check_transaction — параметры проверки транзакции

в системе EME.WMS функция `is_set_check_transaction` устанавливает параметры текущей записи и строки для проверки транзакции в метапроекте при закрытии.

**Параметры**: нет.

**Возвращает**: пустое значение.

```EME-L
' Установить параметры проверки транзакции '
is_set_check_transaction()
' Параметры установлены '
```
