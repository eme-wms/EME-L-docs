---
title: "Диспетчер терминалов"
sidebar:
  order: 21
  label: "Диспетчер терминалов"
---

## Перетаскивание и привязка приказов

### is_drag_drop_terminals_dispatcher — перетаскивание объектов в диспетчере терминалов

Реализует перетаскивание объектов в диспетчере терминалов. Привязывает или отвязывает приказы от терминалов.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Объект-источник перетаскивания (ссылка) |
| args[1] | Integer | Нет | Объект-приемник перетаскивания (ссылка) |
| args[2] | Integer | Нет | Карта для записи ошибок (ссылка) |

**Возвращает**: `String` — пустая строка при успешном выполнении, или строка с описанием ошибки.

```EME-L
' Перетащить приказ на терминал '
result = is_drag_drop_terminals_dispatcher(orderRef, terminalRef)
' result = "" при успехе, или текст ошибки '

' Перетащить с записью ошибок в карту '
errors = Object("Map")
result = is_drag_drop_terminals_dispatcher(orderRef, terminalRef, errors)
```

---

### is_add_del_terminals_dispatcher — привязка или отвязка приказа к терминалу

Реализует привязку или отвязку приказа к терминалу в диспетчере.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Ссылка на приказ в очереди (целое число) |
| args[1] | Integer | Нет | Ссылка на терминал. NULL_REF для отвязки (целое число) |
| args[2] | Integer | Нет | Карта для записи ошибок (ссылка) |

**Возвращает**: `String` — пустая строка при успехе, или строка с описанием ошибки.

```EME-L
' Привязать приказ к терминалу '
result = is_add_del_terminals_dispatcher(queueOrderRef, terminalRef)
' result = "" при успешной привязке '

' Отвязать приказ от терминала '
result = is_add_del_terminals_dispatcher(queueOrderRef, NULL_REF)
' result = "" при успешной отвязке '
```

---

## Работа с терминалами

### is_get_valid_terminal_ref — получить ссылку на терминал

Возвращает ссылку на терминал для указанного объекта.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Объект диспетчера терминалов (ссылка) |

**Возвращает**: `Integer` — ссылка на терминал, или NULL_REF если терминал не назначен.

```EME-L
' Получить ссылку на терминал объекта '
terminalRef = is_get_valid_terminal_ref(objRef)
' terminalRef = ссылка на терминал, или NULL_REF '
```

---

### is_set_valid_terminal_ref — установить ссылку на терминал

Устанавливает ссылку на терминал для указанного объекта.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Объект диспетчера терминалов (ссылка) |
| args[1] | Integer | Да | Ссылка на терминал (целое число) |

**Возвращает**: `Integer` — предыдущая ссылка на терминал.

```EME-L
' Назначить терминал объекту '
prevTerminal = is_set_valid_terminal_ref(objRef, terminalRef)
' prevTerminal = предыдущая ссылка на терминал '
```

---

## Управление очередью приказов

### is_delete_queue_order — удалить приказ из диспетчера

Удаляет приказ из диспетчера терминалов.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Объект для удаления (ссылка) |
| args[1] | Integer | Нет | Массив для сохранения ссылок удаленных строк (ссылка) |

**Возвращает**: `String` — пустая строка при успешном удалении, или строка с описанием ошибки.

```EME-L
' Удалить приказ из диспетчера '
result = is_delete_queue_order(orderRef)
' result = "" при успешном удалении '

' Удалить с сохранением ссылок удаленных строк '
deletedLines = Object("Array")
result = is_delete_queue_order(orderRef, deletedLines)
```

---

### is_exist_queue_order_with_terminal_for_delete — проверить наличие приказов для удаления

Проверяет наличие приказов, привязанных к терминалу, которые необходимо удалить.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Объект диспетчера терминалов (ссылка) |

**Возвращает**: `Integer` — 1 если есть приказы для удаления, 0 если таких приказов нет.

```EME-L
' Проверить, есть ли приказы для удаления '
hasOrders = is_exist_queue_order_with_terminal_for_delete(objRef)
' hasOrders = 1 если есть приказы, 0 если нет '
```

---

### is_get_first_queue_priority — получить приоритет очереди

Возвращает значение внутренней переменной приоритета очереди для указанного объекта.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Объект диспетчера терминалов (ссылка) |

**Возвращает**: `Float` — значение приоритета очереди. Если приоритет не инициализирован, возвращает 0.0.

```EME-L
' Получить приоритет очереди объекта '
priority = is_get_first_queue_priority(objRef)
' priority = значение приоритета, или 0.0 '
```

---

### is_set_first_queue_priority — установить приоритет очереди

Устанавливает значение приоритета очереди для указанного объекта.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Объект диспетчера терминалов (ссылка) |
| args[1] | Float | Да | Новое значение приоритета (вещественное число) |

**Возвращает**: `Float` — предыдущее значение приоритета.

```EME-L
' Установить высокий приоритет очереди '
prevPriority = is_set_first_queue_priority(objRef, 100.0)
' prevPriority = предыдущее значение приоритета '
```

---

### is_set_orders_type — установить тип отображаемых приказов

Устанавливает тип отображаемых приказов для пачки заказов.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Объект пачки заказов (ссылка) |
| args[1] | Integer | Да | Тип приказов для отображения (целое число) |

**Возвращает**: пустое значение.

```EME-L
' Установить тип приказов для пачки '
is_set_orders_type(batchRef, 2)
```

---

## Интегратор

### is_get_text_info_integrator — текстовая информация элемента

Возвращает текстовую информацию об элементе для отображения в интеграторе.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Объект диспетчера терминалов (ссылка) |

**Возвращает**: `String` — текстовая информация об элементе.

```EME-L
' Получить текстовую информацию для интегратора '
textInfo = is_get_text_info_integrator(objRef)
' textInfo = "Заказ №123 — Склад А" (пример) '
```

---

### is_get_base_icon_color_integrator — цвет иконки элемента

Возвращает цвет иконки элемента для отображения в интеграторе.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Объект диспетчера терминалов (ссылка) |

**Возвращает**: `Integer` — цвет иконки. 0 если цвет не задан.

```EME-L
' Получить цвет иконки для интегратора '
color = is_get_base_icon_color_integrator(objRef)
' color = RGB-значение цвета, или 0 '
```

---

### is_get_parent_text_info_integrator — текстовая информация родителя

Возвращает текстовую информацию о родительском элементе для отображения в интеграторе.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Объект диспетчера терминалов (ссылка) |

**Возвращает**: `String` — текстовая информация о родительском элементе.

```EME-L
' Получить текстовую информацию родителя '
parentText = is_get_parent_text_info_integrator(objRef)
' parentText = "Пачка заказов №45" (пример) '
```

---

### is_get_parent_base_icon_color_integrator — цвет иконки родителя

Возвращает цвет иконки родительского элемента для отображения в интеграторе.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Объект диспетчера терминалов (ссылка) |

**Возвращает**: `Integer` — цвет иконки родителя. 0 если цвет не задан.

```EME-L
' Получить цвет иконки родителя '
parentColor = is_get_parent_base_icon_color_integrator(objRef)
' parentColor = RGB-значение цвета родителя, или 0 '
```

---

### is_get_parent_record_for_integrator — запись родителя

Возвращает номер записи родительского объекта для отображения в интеграторе.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Объект диспетчера терминалов (ссылка) |

**Возвращает**: `Integer` — номер записи родителя, или NULL_RECORD если родитель не определен.

```EME-L
' Получить запись родителя для интегратора '
parentRec = is_get_parent_record_for_integrator(objRef)
' parentRec = номер записи родителя, или NULL_RECORD '
```

---

### is_get_parent_line_for_integrator — строка родителя

Возвращает номер строки родительского объекта для отображения в интеграторе.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Объект диспетчера терминалов (ссылка) |

**Возвращает**: `Integer` — номер строки родителя, или NULL_REF если родитель не определен.

```EME-L
' Получить строку родителя для интегратора '
parentLine = is_get_parent_line_for_integrator(objRef)
' parentLine = номер строки родителя, или NULL_REF '
```

---

## Заполнение ссылок и массивов

### is_fill_refs_for_integrator — заполнить карты ссылками

Заполняет переданные карты ссылками на документы, пачки и заказы из очереди приказов.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Карта для ссылок на документы (ссылка) |
| args[1] | Integer | Да | Резервный параметр (ссылка) |
| args[2] | Integer | Да | Карта для ссылок на пачки заказов (ссылка) |
| args[3] | Integer | Да | Карта для ссылок на заказы на отгрузку (ссылка) |
| args[4] | Integer | Да | Карта для ссылок на пачки заказов типа 2 (ссылка) |
| args[5] | Integer | Да | Карта для ссылок на пачки заказов типа 3 (ссылка) |
| args[6] | Integer | Да | Карта для ссылок на пачки заказов типа 4 (ссылка) |
| args[7] | Integer | Да | Опция фильтрации типов приказов (целое число) |

**Возвращает**: пустое значение.

```EME-L
' Заполнить карты ссылками из очереди '
docRefs = Object("Map")
reserved = Object("Map")
batchRefs = Object("Map")
shipRefs = Object("Map")
batch2 = Object("Map")
batch3 = Object("Map")
batch4 = Object("Map")
is_fill_refs_for_integrator(docRefs, reserved, batchRefs, shipRefs, batch2, batch3, batch4, 0)
```

---

### is_check_both_double_stack_queue — проверить оба double-stack приказа

Проверяет, что оба приказа double-stack присутствуют в переданном массиве.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Массив объектов для проверки (ссылка) |

**Возвращает**: `Boolean` — TRUE если оба приказа double-stack найдены, FALSE в противном случае.

```EME-L
' Проверить наличие обоих double-stack приказов '
bothPresent = is_check_both_double_stack_queue(ordersArray)
' bothPresent = TRUE если оба приказа найдены '
```

---

### is_undefine_array_to_queue_oders_array — заполнить массив строками приказов

Заполняет переданный массив строками приказов на основе массива объектов различных типов.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Исходный массив объектов (ссылка) |
| args[1] | Integer | Да | Выходной массив для строк приказов (ссылка) |

**Возвращает**: `Integer` — количество заполненных элементов в выходном массиве.

```EME-L
' Заполнить массив строками приказов '
sourceArray = Object("Array")
' ... заполнение sourceArray ... '
queueStrings = Object("Array")
count = is_undefine_array_to_queue_oders_array(sourceArray, queueStrings)
' count = количество элементов в queueStrings '
```

---
