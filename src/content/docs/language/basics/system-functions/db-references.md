---
title: "База данных: ссылки"
sidebar:
  order: 13
  label: "База данных: ссылки"
---

## База данных: ссылки

is-функции группы DB (ссылки) отвечают за работу со ссылочными полями, мягкими ссылками, массивами ссылок, классификаторами и цепочками записей в языке EME-L. в системе EME.WMS эти функции используются для подсчета ссылок, навигации по иерархии, поиска связанных строк и управления ссылочной целостностью.

---

### is_ref_to — количество ссылок на строку

в языке EME-L функция `is_ref_to` возвращает общее количество ссылок на указанную строку записи.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Строка с номерами записей и полей для пропуска или учета при подсчете |
| args[1] | Integer | Нет | Если 0 — пропускать указанные поля; если не 0 — учитывать только указанные |
| args[2] | String / Integer | Нет | Имя или номер записи для поиска ссылок. По умолчанию базовая запись |
| args[3] | Integer | Нет | Номер строки для поиска ссылок. По умолчанию текущая строка |

**Возвращает**: `Integer` — общее количество ссылок.

```EME-L
' Подсчитать все ссылки на текущую строку '
Count = is_ref_to()
' Count = 5 '

' Пропустить поля (5,1) и (5,2) при подсчете '
Count = is_ref_to("(5,1),(5,2)", 0)
' Count = 3 '

' Учитывать только указанные поля '
Count = is_ref_to("(10,3)", 1, "Заказы", 5)
' Count = 2 '
```

---

### is_exist_ref_to — проверка наличия ссылок

в системе EME.WMS функция `is_exist_ref_to` проверяет наличие хотя бы одной ссылки на указанную строку записи.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Строка с номерами записей и полей для пропуска или учета |
| args[1] | Integer | Нет | Если 0 — пропускать; если не 0 — учитывать только указанные |
| args[2] | String / Integer | Нет | Имя или номер записи. По умолчанию базовая запись |
| args[3] | Integer | Нет | Номер строки. По умолчанию текущая строка |

**Возвращает**: `Boolean` — `TRUE` если ссылка существует, иначе `FALSE`.

```EME-L
' Проверить наличие ссылок на текущую строку '
HasRefs = is_exist_ref_to()
' HasRefs = TRUE '

' Проверить с пропуском определенных полей '
HasRefs = is_exist_ref_to("(5,1)", 0)
' HasRefs = FALSE - после исключения ссылок нет '
```

---

### is_referenceed_line — поиск строки по ссылке

в языке EME-L функция `is_referenceed_line` ищет строку в указанной записи, которая ссылается на заданную строку.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String / Integer | Да | Имя или номер записи-источника ссылки |
| args[1] | String / Integer | Да | Имя или номер поля ссылки |
| args[2] | Integer | Нет | Номер строки, с которой начинать поиск. По умолчанию 0 |
| args[3] | Integer | Нет | Номер строки, на которую должна указывать ссылка. По умолчанию текущая строка |

**Возвращает**: `Integer` — номер найденной строки или `NULL_REF` если не найдена.

```EME-L
' Найти строку в записи Заказы, поле Клиент, ссылающуюся на текущую строку '
Line = is_referenceed_line("Заказы", "Клиент")
' Line = 10 '

' Найти со строки 5, ссылающуюся на строку 20 '
Line = is_referenceed_line("Заказы", "Клиент", 5, 20)
' Line = 15 '
```

---

### is_ref_to_child — ссылки на дочерние строки

в EME БД функция `is_ref_to_child` возвращает количество ссылок на дочерние строки базовой записи.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Нет | Имя поля первой строки в родительской записи. По умолчанию "@Первая строка" |
| args[1] | String | Нет | Имя поля заголовка в дочерней записи. По умолчанию "@Заголовок" |
| args[2] | String | Нет | Имя поля сортировки в дочерней записи. По умолчанию "Сортировка" |
| args[3] | String | Да | Строка с номерами записей и полей для пропуска или учета |
| args[4] | Integer | Да | Если 0 — пропускать; если не 0 — учитывать только указанные |

**Возвращает**: `Integer` — количество ссылок на дочерние строки.

```EME-L
' Подсчитать ссылки на дочерние строки с параметрами по умолчанию '
Count = is_ref_to_child("@Первая строка", "@Заголовок", "Сортировка", "", 0)
' Count = 3 '

' С учетом только определенных полей '
Count = is_ref_to_child("", "", "", "(10,3)", 1)
' Count = 2 '
```

---

### is_ref_rec — номер ссылаемой записи

в языке EME-L функция `is_ref_rec` возвращает номер записи, на которую указывает ссылочное поле.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String / Integer | Да | Имя или номер записи |
| args[1] | String / Integer | Да | Имя или номер поля |

**Возвращает**: `Integer` — номер ссылаемой записи.

```EME-L
' Получить номер записи, на которую ссылается поле Клиент '
RecNo = is_ref_rec("Заказы", "Клиент")
' RecNo = 15 '
```

---

### is_vvr_rec — номер записи из мягкой ссылки

в системе EME.WMS функция `is_vvr_rec` извлекает номер записи из мягкой ссылки.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Reference | Да | Значение мягкой ссылки |

**Возвращает**: `Integer` — номер записи.

```EME-L
' Извлечь номер записи из мягкой ссылки '
RecNo = is_vvr_rec(SoftRef)
' RecNo = 10 '
```

---

### is_vvr_line — номер строки из мягкой ссылки

в языке EME-L функция `is_vvr_line` извлекает номер строки из мягкой ссылки.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Reference | Да | Значение мягкой ссылки |

**Возвращает**: `Integer` — номер строки.

```EME-L
' Извлечь номер строки из мягкой ссылки '
LineNo = is_vvr_line(SoftRef)
' LineNo = 5 '
```

---

### is_vvr — формирование мягкой ссылки

в EME БД функция `is_vvr` формирует мягкую ссылку по записи и номеру строки.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String / Integer | Да | Имя или номер записи |
| args[1] | Integer | Да | Номер строки |
| args[2] | Integer | Нет | Игнорируется |

**Возвращает**: `Reference` — мягкая ссылка.

```EME-L
' Создать мягкую ссылку на строку 5 записи Клиенты '
SoftRef = is_vvr("Клиенты", 5)
' SoftRef = мягкая ссылка '

' Извлечь данные из ссылки '
RecNo = is_vvr_rec(SoftRef)
' RecNo = 10 '
LineNo = is_vvr_line(SoftRef)
' LineNo = 5 '
```

---

### is_in_ref_array — проверка значения в массиве ссылок

в языке EME-L функция `is_in_ref_array` проверяет наличие заданного значения в массиве ссылок.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String / Integer | Нет | Имя записи, физический номер или положение объекта. По умолчанию базовая запись |
| args[1] | String / Integer | Да | Имя или номер поля массива ссылок |
| args[2] | Integer | Нет | Номер строки. По умолчанию текущая строка |
| args[3] | Integer | Да | Искомое значение ссылки |

**Возвращает**: `Boolean` — `TRUE` если значение найдено, иначе `FALSE`.

```EME-L
' Проверить наличие ссылки 15 в массиве Товары '
Found = is_in_ref_array("Заказы", "Товары", 5, 15)
' Found = TRUE '

' Проверить в текущей строке '
Found = is_in_ref_array("Заказы", "Товары", , 20)
' Found = FALSE '
```

---

### is_show_author — данные из записи по ссылке массива

в системе EME.WMS функция `is_show_author` возвращает строку, составленную из содержимого трех полей записи, на которую указывает массив ссылок.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String / Integer | Да | Имя записи, физический номер или положение объекта |
| args[1] | Integer | Нет | Номер строки. По умолчанию текущая строка |
| args[2] | String / Integer | Да | Имя или номер поля массива ссылок |
| args[3] | Integer | Нет | Индекс элемента массива. -1 — первая ссылка |
| args[4] | String / Integer | Да | Имя или номер первого поля в целевой записи |
| args[5] | String / Integer | Да | Имя или номер второго поля |
| args[6] | String / Integer | Да | Имя или номер третьего поля |

**Возвращает**: `String` — склеенная строка из трех полей.

```EME-L
' Получить ФИО автора из массива ссылок '
Author = is_show_author("Документы", 5, "Авторы", 0, "Фамилия", "Имя", "Отчество")
' Author = "Иванов Иван Иванович" '
```

---

### is_linked — проверка ветви классификатора

в языке EME-L функция `is_linked` проверяет, лежат ли две строки в одной ветви классификатора.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Номер записи |
| args[1] | Integer | Да | Номер рекурсивного поля |
| args[2] | Integer | Да | Номер первой строки |
| args[3] | Integer | Да | Номер второй строки |

**Возвращает**: `Boolean` — `TRUE` если строки в одной ветви, иначе `FALSE`.

```EME-L
' Проверить, находятся ли строки 5 и 10 в одной ветви '
Linked = is_linked(15, 3, 5, 10)
' Linked = TRUE - обе строки в одной ветви классификатора '

' Проверить разные ветви '
Linked = is_linked(15, 3, 5, 20)
' Linked = FALSE '
```

---

### is_prev_line — поиск предыдущей строки

в EME БД функция `is_prev_line` ищет строку с максимальным значением поля, которое при этом меньше заданного.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Any | Да | Опорное значение для сравнения |
| args[1] | String / Integer | Да | Имя или номер записи |
| args[2] | String / Integer | Да | Имя или номер поля для поиска |
| args[3] | String / Integer | Да | Имя или номер поля фильтрации |
| args[4] | Any | Да | Значение для фильтрации |

**Возвращает**: `Integer` — номер предыдущей строки или `NULL_REF`.

```EME-L
' Найти строку с кодом меньше 100, но максимальным '
Line = is_prev_line(100, "Товары", "Код", "Группа", 1)
' Line = 15 - строка с кодом 95 '
```

---

### is_ref_to_subitems — ссылки на дочерние строки классификатора

в языке EME-L функция `is_ref_to_subitems` возвращает наличие ссылок на дочерние строки классификатора.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String / Integer | Да | Имя или номер записи |
| args[1] | Integer | Да | Номер родительской строки |
| args[2] | String / Integer | Да | Имя или номер рекурсивного поля |
| args[3] | String | Да | Строка с номерами записей и полей для пропуска или учета |
| args[4] | Integer | Да | Если 0 — пропускать; если не 0 — учитывать только указанные |

**Возвращает**: `Integer` — 1 если ссылки существуют, иначе 0.

```EME-L
' Проверить наличие дочерних элементов у строки 5 '
HasChildren = is_ref_to_subitems("Классификатор", 5, "Родитель", "", 0)
' HasChildren = 1 '
```

---

### is_check_chain — проверка цепочки

в системе EME.WMS функция `is_check_chain` проверяет все строки цепочки на соответствие заданному условию.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String / Integer | Да | Имя или номер записи |
| args[1] | String / Integer | Да | Имя или номер поля-ссылки на начало цепочки |
| args[2] | Integer | Да | Номер строки-заголовка цепочки |
| args[3] | String | Да | Текстовое условие для проверки каждой строки |
| args[4] | Integer | Да | Режим: 0 — все удовлетворяют, 1 — хотя бы один, 2 — хотя бы один не удовлетворяет, 3 — все не удовлетворяют |

**Возвращает**: `Boolean` — результат проверки в зависимости от режима.

```EME-L
' Проверить, что все строки цепочки имеют статус 1 '
AllActive = is_check_chain("Документы", "Следующий", 0, "is_data(\"Документы\", is_arg(1), \"Статус\") = 1", 0)
' AllActive = TRUE '

' Проверить, есть ли хотя бы одна строка со статусом 2 '
HasPending = is_check_chain("Документы", "Следующий", 0, "is_data(\"Документы\", is_arg(1), \"Статус\") = 2", 1)
' HasPending = FALSE '
```

---

### is_find_reference_field — поиск поля-ссылки

в языке EME-L функция `is_find_reference_field` находит поле-ссылку из одной записи в другую.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Номер записи-источника |
| args[1] | Integer | Да | Номер записи-цели |
| args[2] | Integer | Да | Номер поля, с которого начинать поиск |
| args[3] | Integer | Да | Режим обработки ошибки. `EXCEPT_MODE` — вызывает исключение если не найдено |

**Возвращает**: `Integer` — номер найденного поля или `NULL_FIELD`.

```EME-L
' Найти поле-ссылку из записи 10 в запись 15 '
FieldNo = is_find_reference_field(10, 15, 0, 0)
' FieldNo = 3 '

' С выбросом исключения если не найдено '
FieldNo = is_find_reference_field(10, 15, 0, 1)
' Если не найдено — ошибка '
```

---

### is_restore_refs — восстановление ссылок по ключам

в EME БД функция `is_restore_refs` восстанавливает ссылки `NULL_REF` в заданном поле записи по сохраненным ключам.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Номер записи |
| args[1] | Integer | Да | Номер поля-ссылки |

**Возвращает**: `Boolean` — `TRUE` если восстановление выполнено или транзакция не открыта, иначе `FALSE`.

```EME-L
' Восстановить ссылки в поле 5 записи 10 '
Result = is_restore_refs(10, 5)
' Result = TRUE '
```

---

### is_relink_ref — замена ссылок

в языке EME-L функция `is_relink_ref` заменяет ссылки в указанном поле записи с одного значения на другое.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String / Integer | Да | Имя или номер записи |
| args[1] | String / Integer | Да | Имя или номер поля |
| args[2] | Integer | Да | Старая ссылка для замены |
| args[3] | Integer | Да | Новая ссылка |

**Возвращает**: `Boolean` — `TRUE` если все ссылки изменены или значение не найдено, иначе `FALSE`.

```EME-L
' Заменить все ссылки с 15 на 20 в поле Клиент '
Result = is_relink_ref("Заказы", "Клиент", 15, 20)
' Result = TRUE '
```

---

### is_redirect_referencies — перенаправление ссылок

в системе EME.WMS функция `is_redirect_referencies` перенаправляет ссылки с одной строки записи на другую.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String / Integer | Да | Имя или номер записи, на которую есть ссылки |
| args[1] | Integer | Да | Номер строки, с которой нужно убрать ссылки |
| args[2] | Integer | Да | Номер строки, на которую нужно перенаправить ссылки |

**Возвращает**: `Integer` — количество исправленных строк.

```EME-L
' Перенаправить все ссылки со строки 5 на строку 10 записи Клиенты '
Count = is_redirect_referencies("Клиенты", 5, 10)
' Count = 3 - исправлено 3 ссылки '
```

---

### is_clean_refs — обнуление ссылок

в языке EME-L функция `is_clean_refs` обнуляет ссылки на записи, удаленные через `KillRecord`.

**Параметры**: нет.

**Возвращает**: пустое значение.

```EME-L
' Обнулить ссылки на удаленные записи '
is_clean_refs()
' Ссылки обнулены '
```

---

### is_root — корневой элемент дерева или классификатора

в EME БД функция `is_root` возвращает номер строки корневого элемента дерева или классификатора. Работает в двух режимах в зависимости от количества аргументов.

| Параметр (режим 1 - дерево) | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Имя органа управления с деревом |

| Параметр (режим 2 - классификатор) | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String / Integer | Да | Имя или номер записи классификатора |
| args[1] | String / Integer | Да | Имя или номер рекурсивного поля |
| args[2] | Integer | Да | Номер строки для начала обхода |

**Возвращает**: `Integer` — номер строки корневого элемента. `NULL_REF` (-1) — если текущий элемент уже является корневым или при ошибке.

```EME-L
' Получить корневой элемент дерева '
RootLine = is_root("TreeControl")
' RootLine = 0 '

' Получить корень классификатора для строки 20 '
RootLine = is_root("Классификатор", "Родитель", 20)
' RootLine = 0 - корень дерева '
```

---

### is_parent — родительский элемент дерева

в языке EME-L функция `is_parent` возвращает номер строки родительского элемента дерева для текущего элемента.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Имя органа с деревом |

**Возвращает**: `Integer` — номер строки родителя или `NULL_REF` (-1) если текущий элемент корневой (у корневого элемента нет родителя).

```EME-L
' Получить родителя текущего элемента в дереве '
ParentLine = is_parent("TreeControl")
' ParentLine = 5 '

' Корневой элемент '
ParentLine = is_parent("TreeControl")
' ParentLine = NULL_REF - корневой элемент не имеет родителя '
```
