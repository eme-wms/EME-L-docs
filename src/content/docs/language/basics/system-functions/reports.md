---
title: "Работа с отчётами"
sidebar:
  order: 13
  label: "Работа с отчётами"
---

## Работа с отчётами

### is_cell_data — содержимое ячейки отчёта

в языке EME-L функция `is_cell_data` возвращает содержимое ячейки отчёта с учётом её атрибутов форматирования. Если имя ячейки не указано, используется текущая ячейка из контекста выполнения. Функция доступна только в контексте ячейки отчёта (`rs_cell_`) или банда (`rs_band_`).

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Нет | Имя ячейки отчёта для чтения данных. По умолчанию используется текущая ячейка из контекста |

**Вызов без аргументов**: возвращает данные текущей ячейки отчёта.

**Возвращает**: содержимое ячейки — `String`, `Integer`, `Real`, `Date`, `Time` или `Reference` в зависимости от типа ячейки.

```EME-L
' Получить данные текущей ячейки '
Value = is_cell_data()

' Получить данные ячейки по имени '
CellValue = is_cell_data("TotalSum")
```

---

### is_cell_data_ex — чтение и запись ячейки в вертикальном банде

в системе EME.WMS функция `is_cell_data_ex` читает или записывает данные ячейки, находящейся в вертикальном банде сформированного или строящегося отчёта. Позволяет обращаться к ячейкам по номеру строки вертикального и горизонтального бандов.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Нет | Имя ячейки отчёта. По умолчанию используется текущая ячейка из контекста |
| args[1] | Integer | Нет | Номер строки в вертикальном банде. По умолчанию текущая строка вертикального банда |
| args[2] | Integer | Нет | Номер строки в горизонтальном банде. По умолчанию текущая строка банда |
| args[3] | Any | Нет | Значение для записи в ячейку. Если задано, выполняется запись вместо чтения |

**Вызов без аргументов**: читает данные текущей ячейки.

**Возвращает**:
- При чтении (параметр «значение» не задан): содержимое ячейки — `String`, `Integer`, `Real`, `Date`, `Time` или `Reference`
- При записи (параметр «значение» задан): пустое значение

```EME-L
' Прочитать данные текущей ячейки '
Value = is_cell_data_ex()

' Прочитать ячейку из строки 3 вертикального банда '
Value = is_cell_data_ex("Price", 3)

' Записать значение в ячейку '
is_cell_data_ex("Total", 1, 1, 1500.50)
```

---

### is_line_sum — сумма по колонкам вертикального банда

в языке EME-L функция `is_line_sum` вычисляет сумму значений ячейки по всем колонкам вертикального банда. Ячейка должна находиться внутри вертикального банда и иметь числовой тип (целое или вещественное). Функция доступна только в контексте ячейки отчёта (`rs_cell_`).

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Нет | Имя ячейки отчёта, расположенной внутри вертикального банда. По умолчанию используется имя текущей ячейки из контекста |

**Возвращает**: `Real` или `Integer` — сумму значений ячейки по колонкам вертикального банда. Если ячейка не найдена или вертикальный банд пуст, возвращает 0.

```EME-L
' Сумма по колонкам для текущей ячейки '
Total = is_line_sum()

' Сумма по колонкам для указанной ячейки '
Total = is_line_sum("Amount")
```

---

### is_band_line — номер строки банда

в системе EME.WMS функция `is_band_line` возвращает номер текущей строки указанной области (банда) отчёта. Для вертикального банда возвращает номер строки внутри банда. Для горизонтального банда возвращает текущую строку базы данных.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Нет | Имя банда (области) отчёта. По умолчанию возвращает номер текущей строки из контекста |

**Вызов без аргументов**: возвращает номер текущей строки из контекста отчёта (`param->line`).

**Возвращает**: `Integer` — номер строки области. Для вертикального банда — номер строки внутри банда. Для горизонтального банда — текущую строку базы данных. Если область не найдена, генерируется ошибка.

```EME-L
' Получить номер текущей строки '
LineNo = is_band_line()

' Получить номер строки банда "Details" '
BandLine = is_band_line("Details")
```

---

### is_band_column — индекс вертикального банда

в языке EME-L функция `is_band_column` возвращает индекс вертикального банда для текущей ячейки отчёта. Функция доступна только в контексте ячейки отчёта (`rs_cell_`).

**Параметры**: нет.

**Возвращает**: `Integer` — индекс вертикального банда. Если ячейка не находится в вертикальном банде, возвращает 0. В противном случае возвращает номер колонки (строки) вертикального банда.

```EME-L
' Получить индекс вертикального банда '
ColIndex = is_band_column()
```

---

### is_page_orientation — ориентация страницы

в системе EME.WMS функция `is_page_orientation` возвращает ориентацию страницы текущего отчёта.

**Параметры**: нет.

**Возвращает**: `Integer` — ориентацию страницы:
- 1 — книжная (портретная)
- 2 — альбомная (ландшафтная)

```EME-L
' Получить ориентацию страницы '
Orientation = is_page_orientation()
' Orientation = 1 (книжная) '

' Проверить, альбомная ли ориентация '
If (is_page_orientation() = 2)
    Msg = "Альбомная ориентация"
End If
```

---

### is_report_line — номер базовой строки БД отчёта

в языке EME-L функция `is_report_line` возвращает номер текущей базовой строки базы данных отчёта.

**Параметры**: нет.

**Возвращает**: `Integer` — номер базовой строки БД отчёта.

```EME-L
' Получить номер базовой строки отчёта '
DbLine = is_report_line()
```

---

### is_cell_exist — проверка существования ячейки

в системе EME.WMS функция `is_cell_exist` проверяет существование ячейки в отчёте без вывода сообщений об ошибках. В отличие от `is_cell_data`, не генерирует ошибку при отсутствии ячейки.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Имя проверяемой ячейки отчёта |

**Возвращает**: `Integer` — признак существования ячейки:
- 1 (true) — ячейка существует в отчёте
- 0 (false) — ячейка не найдена

```EME-L
' Проверить, существует ли ячейка '
If (is_cell_exist("TotalSum"))
    Value = is_cell_data("TotalSum")
End If

' Проверить несуществующую ячейку '
Exists = is_cell_exist("NonExistent")
' Exists = 0 '
```

---

### is_report_param — значение параметра отчёта

в языке EME-L функция `is_report_param` возвращает значение параметра отчёта, переданного при начале формирования отчёта. Параметры передаются через диалог отчёта или программно при запуске формирования.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Имя параметра отчёта |
| args[1] | Any | Нет | Значение, возвращаемое если параметр не найден. Если параметр найден, используется как подсказка типа: найденное значение приводится к типу данного аргумента. По умолчанию пустая строка |

**Вызов с одним аргументом**: возвращает значение параметра или пустую строку, если параметр не найден.

**Возвращает**: значение параметра — тип зависит от аргумента «значение_по_умолчанию». Если параметр не найден и отчёт не задан, возвращает значение по умолчанию или пустую строку.

```EME-L
' Получить значение параметра отчёта '
StartDate = is_report_param("StartDate")

' Получить параметр с значением по умолчанию '
EndDate = is_report_param("EndDate", is_date())

' Получить числовой параметр с подсказкой типа '
Count = is_report_param("Count", 0)
```

---

### is_export_pdf — экспорт отчёта в PDF

в системе EME.WMS функция `is_export_pdf` экспортирует текущий отчёт в файл формата PDF. Доступна только в контексте отчёта.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Полный путь к файлу для сохранения PDF |
| args[1] | String | Нет | Размер страницы: "A4" (по умолчанию), "A3", "A5" |
| args[2] | Integer | Нет | Ориентация страницы: -1 — из настроек отчёта (по умолчанию), 0 — портретная, 1 — альбомная |

**Вызов с одним аргументом**: экспортирует отчёт в PDF с размером A4 и ориентацией из настроек отчёта.

**Возвращает**: `Boolean` — результат экспорта:
- true (1) — экспорт выполнен успешно
- false (0) — экспорт не выполнен (ядро не поддерживает PDF или ошибка экспорта)

```EME-L
' Экспорт отчёта в PDF с настройками по умолчанию '
Result = is_export_pdf("C:\\Reports\\output.pdf")

' Экспорт с указанным размером и ориентацией '
Result = is_export_pdf("C:\\Reports\\output.pdf", "A3", 1)

' Проверить результат экспорта '
If (is_export_pdf("C:\\Reports\\output.pdf") = 1)
    Msg = "Экспорт выполнен успешно"
End If
```

---

### is_report_name — имя отчёта

в языке EME-L функция `is_report_name` возвращает имя текущего отчёта из контекста.

**Параметры**: нет.

**Возвращает**: `String` — имя отчёта. Если отчёт не задан в контексте, возвращает пустую строку.

```EME-L
' Получить имя текущего отчёта '
Name = is_report_name()

' Проверить имя отчёта '
If (is_report_name() = "SalesReport")
    Msg = "Отчёт о продажах"
End If
```

---

### is_report_query_line — текущая строка запроса отчёта

в системе EME.WMS функция `is_report_query_line` возвращает текущую строку запроса (query line) из контекста отчёта.

**Параметры**: нет.

**Возвращает**: `Integer` — номер текущей строки запроса. Если отчёт не задан в контексте, возвращает пустую ссылку (`NULL_REF`).

```EME-L
' Получить текущую строку запроса '
QueryLine = is_report_query_line()
```

---

### is_band_number_of_lines — количество строк в банде

в языке EME-L функция `is_band_number_of_lines` возвращает количество строк в указанной области (банде) отчёта. Во время разворачивания банда может использоваться как номер строки по порядку.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Нет | Имя банда (области) отчёта. По умолчанию используется текущая область из контекста |

**Вызов без аргументов**: возвращает количество строк в текущей области.

**Возвращает**: `Integer` — количество строк в области. Если область не найдена, генерируется ошибка.

```EME-L
' Получить количество строк в текущем банде '
Count = is_band_number_of_lines()

' Получить количество строк в банде "Details" '
Count = is_band_number_of_lines("Details")
```

---

### is_page_number — номер страницы отчёта

в системе EME.WMS функция `is_page_number` возвращает текущий или максимальный номер страницы отчёта.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Boolean | Нет | Признак возврата максимального номера страницы. Если true, возвращает общее количество страниц. По умолчанию false |

**Вызов без аргументов**: возвращает номер текущей страницы.

**Возвращает**: `Integer` — номер страницы отчёта, начиная с 1. Если параметр true, возвращает максимальный (общий) номер страницы.

```EME-L
' Получить номер текущей страницы '
Page = is_page_number()

' Получить общее количество страниц '
TotalPages = is_page_number(TRUE)

' Вывести номер страницы '
PageInfo = "Страница " + is_string(is_page_number()) + " из " + is_string(is_page_number(TRUE))
```

---

### is_edit_report — открытие редактора шаблонов отчётов

в языке EME-L функция `is_edit_report` открывает редактор шаблонов отчётов для указанного отчёта. Функция доступна только пользователю с правами администратора.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Имя отчёта, как оно хранится в базе данных |

**Возвращает**: пустое значение. Если у пользователя нет прав администратора, функция не выполняет действий.

```EME-L
' Открыть редактор для отчёта '
is_edit_report("SalesReport")

' Открыть редактор с проверкой прав '
If (is_admin())
    is_edit_report("InvoiceTemplate")
End If
```

---
