---
title: "Дата и время"
sidebar:
  order: 10
  label: "Дата и время"
---

## Дата и время

в языке EME-L функции группы DateTime отвечают за работу с датами, временем, календарными периодами и часовыми поясами. в системе EME.WMS эти функции используются для форматирования дат, расчета периодов, преобразования между временными зонами и извлечения компонентов даты/времени.

---

### is_dos_date — текущая системная дата

в языке EME-L функция `is_dos_date` возвращает текущую системную дату.

**Параметры**: нет.

**Возвращает**: `Date` — текущая системная дата.

```EME-L
' Получить текущую дату '
Today = is_dos_date()
```

---

### is_dos_time — текущее системное время

в системе EME.WMS функция `is_dos_time` возвращает текущее системное время.

**Параметры**: нет.

**Возвращает**: `Time` — текущее системное время.

```EME-L
' Получить текущее время '
Now = is_dos_time()
```

---

### is_datetime — преобразование к дате/времени

в языке EME-L функция `is_datetime` преобразует значение к типу дата/время.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | DateTime / String | Да | Исходное значение для преобразования |

**Возвращает**: `DateTime` — значение типа дата/время.

```EME-L
' Преобразовать строку в дату/время '
Dt = is_datetime("01.03.2025")
```

---

### is_fdatetime — форматирование даты/времени

в EME БД функция `is_fdatetime` форматирует дату/время в строку согласно заданному формату или выполняет обратное преобразование строки в дату/время.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | DateTime / String | Да | Дата/время для форматирования, или текстовая строка для обратного преобразования |
| args[1] | String | Нет | Строка формата вывода даты/времени. При обратном преобразовании задает формат входной строки. Если не указан, дата выводится прописью в формате DD месяца YYYY г. (по умолчанию) |

**Возвращает**: `String` — отформатированная строка даты/времени, или `DateTime` при обратном преобразовании.

```EME-L
' Форматировать дату прописью: 15 марта 2025 г. '
TextDate = is_fdatetime(Dt)

' Форматировать с явным форматом '
Formatted = is_fdatetime(Dt, "%d.%m.%Y %H:%M")

' Обратное преобразование строки в дату '
Dt = is_fdatetime("15.03.2025", "%d.%m.%Y")
```

---

### is_leap_year — проверка високосного года

в языке EME-L функция `is_leap_year` определяет, является ли указанный год високосным.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Год для проверки |

**Возвращает**: `Integer` — 1, если год високосный; 0 — если невисокосный.

```EME-L
' Проверить, високосный ли 2024 год '
Leap = is_leap_year(2024)
' Leap = 1 '

' Проверить 1900 год '
Leap = is_leap_year(1900)
' Leap = 0 - 1900 не делится на 400 '

' Проверить 2000 год '
Leap = is_leap_year(2000)
' Leap = 1 - 2000 делится на 400 '
```

---

### is_strmonth — название месяца

в системе EME.WMS функция `is_strmonth` возвращает название месяца на русском языке.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Номер месяца от 1 до 12 |
| args[1] | Integer | Нет | Режим склонения и регистра: 1 — именительный падеж строчные (по умолчанию), 0 — родительный падеж, 2 — именительный с заглавной буквы |

**Возвращает**: `String` — название месяца.

```EME-L
' Именительный падеж '
Month = is_strmonth(3)
' Month = "март" '

' Родительный падеж '
Month = is_strmonth(3, 0)
' Month = "марта" '

' С заглавной буквы '
Month = is_strmonth(3, 2)
' Month = "Март" '
```

---

### is_start_date_week — дата начала недели

в языке EME-L функция `is_start_date_week` возвращает дату начала недели (понедельник) для указанной даты.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Date | Да | Исходная дата |
| args[1] | Integer | Нет | Формат возвращаемого значения: 0 — строка DD.MM.YYYY, иное — дата (по умолчанию) |

**Возвращает**: `Date` или `String` — дата начала недели.

```EME-L
' Дата начала недели для 15 марта 2025 (суббота) '
StartWeek = is_start_date_week(Dt)
' StartWeek = 10.03.2025 - понедельник '

' Вернуть строкой '
StartStr = is_start_date_week(Dt, 0)
' StartStr = "10.03.2025" '
```

---

### is_final_date_week — дата конца недели

в EME БД функция `is_final_date_week` возвращает дату конца недели (воскресенье) для указанной даты.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Date | Да | Исходная дата |
| args[1] | Integer | Нет | Формат возвращаемого значения: 0 — строка DD.MM.YYYY, иное — дата (по умолчанию) |

**Возвращает**: `Date` или `String` — дата конца недели.

```EME-L
' Дата конца недели для 15 марта 2025 '
EndWeek = is_final_date_week(Dt)
' EndWeek = 16.03.2025 - воскресенье '
```

---

### is_day_of_week — номер дня недели

в языке EME-L функция `is_day_of_week` возвращает номер дня недели для указанной даты.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Date | Да | Исходная дата |

**Возвращает**: `Integer` — номер дня недели: 1 — понедельник, 7 — воскресенье.

```EME-L
' День недели для 15 марта 2025 (суббота) '
DayNum = is_day_of_week(Dt)
' DayNum = 6 '
```

---

### is_number_week_of_year — номер недели в году

в системе EME.WMS функция `is_number_week_of_year` возвращает номер недели в году. Первая неделя года может быть неполной.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Date | Да | Исходная дата |

**Возвращает**: `Integer` — номер недели в году, начиная с 1.

```EME-L
' Номер недели для 15 марта 2025 '
WeekNum = is_number_week_of_year(Dt)
```

---

### is_week — номер недели (полная первая неделя)

в языке EME-L функция `is_week` возвращает номер недели в году. Неполная неделя в начале года считается полной.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Date | Да | Исходная дата |

**Возвращает**: `Integer` — номер недели в году, начиная с 1.

```EME-L
' Номер недели (неполная неделя считается полной) '
WeekNum = is_week(Dt)
```

---

### is_start_date_week_of_number — дата начала недели по номеру

в EME БД функция `is_start_date_week_of_number` возвращает дату начала недели по номеру недели и году.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Номер недели в году |
| args[1] | Integer | Да | Год |

**Возвращает**: `Date` — дата начала указанной недели.

```EME-L
' Дата начала 10-й недели 2025 года '
StartDate = is_start_date_week_of_number(10, 2025)
```

---

### is_day_count_from_Christ — дни от базовой даты

в языке EME-L функция `is_day_count_from_Christ` возвращает количество дней, прошедших с определенной базовой даты (отсчет от Рождества).

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Date | Да | Исходная дата |

**Возвращает**: `Integer` — количество дней.

```EME-L
' Количество дней от базовой даты '
Days = is_day_count_from_Christ(Dt)
```

---

### is_start_date_month — дата начала месяца

в системе EME.WMS функция `is_start_date_month` возвращает дату начала месяца (первое число) для указанной даты.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Date | Да | Исходная дата |
| args[1] | Integer | Нет | Формат возвращаемого значения: 0 — строка DD.MM.YYYY, иное — дата (по умолчанию) |

**Возвращает**: `Date` или `String` — дата начала месяца.

```EME-L
' Первое число месяца '
StartMonth = is_start_date_month(Dt)
' StartMonth = 01.03.2025 '

' Строкой '
StartStr = is_start_date_month(Dt, 0)
' StartStr = "01.03.2025" '
```

---

### is_final_date_month — дата конца месяца

в языке EME-L функция `is_final_date_month` возвращает дату конца месяца (последнее число) для указанной даты.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Date | Да | Исходная дата |
| args[1] | Integer | Нет | Формат возвращаемого значения: 0 — строка DD.MM.YYYY, иное — дата (по умолчанию) |

**Возвращает**: `Date` или `String` — дата конца месяца.

```EME-L
' Последнее число марта 2025 '
EndMonth = is_final_date_month(Dt)
' EndMonth = 31.03.2025 '

' Последнее число февраля 2024 (високосный) '
EndFeb = is_final_date_month(FebDt)
' EndFeb = 29.02.2024 '
```

---

### is_start_date_decade — дата начала декады

в EME БД функция `is_start_date_decade` возвращает дату начала декады (1-е, 11-е или 21-е число) для указанной даты.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Date | Да | Исходная дата |
| args[1] | Integer | Нет | Формат возвращаемого значения: 0 — строка DD.MM.YYYY, иное — дата (по умолчанию) |

**Возвращает**: `Date` или `String` — дата начала декады.

```EME-L
' Начало декады для 15 марта (третья декада) '
StartDecade = is_start_date_decade(Dt)
' StartDecade = 21.03.2025 '

' Начало декады для 5 марта (первая декада) '
StartDecade = is_start_date_decade(EarlyDt)
' StartDecade = 01.03.2025 '
```

---

### is_final_date_decade — дата конца декады

в языке EME-L функция `is_final_date_decade` возвращает дату конца декады (10-е, 20-е или последнее число месяца) для указанной даты.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Date | Да | Исходная дата |
| args[1] | Integer | Нет | Формат возвращаемого значения: 0 — строка DD.MM.YYYY, иное — дата (по умолчанию) |

**Возвращает**: `Date` или `String` — дата конца декады.

```EME-L
' Конец декады для 15 марта '
EndDecade = is_final_date_decade(Dt)
' EndDecade = 20.03.2025 '

' Конец третьей декады февраля 2024 '
EndDecade = is_final_date_decade(FebDt)
' EndDecade = 29.02.2024 '
```

---

### is_start_date_quarter — дата начала квартала

в системе EME.WMS функция `is_start_date_quarter` возвращает дату начала квартала для указанной даты.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Date | Да | Исходная дата |
| args[1] | Integer | Нет | Формат возвращаемого значения: 0 — строка DD.MM.YYYY, иное — дата (по умолчанию) |

**Возвращает**: `Date` или `String` — дата начала квартала.

```EME-L
' Начало квартала для 15 марта (Q1) '
StartQuarter = is_start_date_quarter(Dt)
' StartQuarter = 01.01.2025 '

' Начало квартала для 20 июня (Q2) '
StartQ2 = is_start_date_quarter(SummerDt)
' StartQ2 = 01.04.2025 '
```

---

### is_final_date_quarter — дата конца квартала

в языке EME-L функция `is_final_date_quarter` возвращает дату конца квартала для указанной даты.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Date | Да | Исходная дата |
| args[1] | Integer | Нет | Формат возвращаемого значения: 0 — строка DD.MM.YYYY, иное — дата (по умолчанию) |

**Возвращает**: `Date` или `String` — дата конца квартала.

```EME-L
' Конец квартала для 15 марта (Q1) '
EndQuarter = is_final_date_quarter(Dt)
' EndQuarter = 31.03.2025 '

' Конец квартала для 20 июня (Q2) '
EndQ2 = is_final_date_quarter(SummerDt)
' EndQ2 = 30.06.2025 '
```

---

### is_parsing_date — разбор строки даты

в EME БД функция `is_parsing_date` преобразует текстовую строку даты в значение типа дата/время.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Строка даты для разбора |
| args[1] | String | Нет | Формат входной строки: ddmmyyyy (по умолчанию), yyyymmdd, yyyy-mm-dd, dd-mm-yyyy, dd.mm.yyyy, ddmmyy |
| args[2] | Integer | Нет | Формат возвращаемого значения: 0 — строка DD.MM.YYYY, иное — дата (по умолчанию) |

**Возвращает**: `Date` или `String` — разобранная дата.

```EME-L
' Разбор даты в формате ddmmyyyy '
Dt = is_parsing_date("15032025")

' Разбор даты в формате yyyymmdd '
Dt = is_parsing_date("20250315", "yyyymmdd")

' Разбор даты с дефисами '
Dt = is_parsing_date("15-03-2025", "dd-mm-yyyy")

' Вернуть строкой '
Str = is_parsing_date("15032025", "ddmmyyyy", 0)
' Str = "15.03.2025" '
```

---

### is_parsing_time — разбор строки времени

в языке EME-L функция `is_parsing_time` преобразует текстовую строку времени в значение типа время.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Строка времени для разбора |
| args[1] | String | Нет | Формат входной строки: hhmm (по умолчанию), hhmmss, hh:mm:ss, hh:mm:ss.zzz |

**Возвращает**: `Time` — значение типа время.

```EME-L
' Разбор времени в формате hhmm '
Tm = is_parsing_time("1430")

' Разбор времени с секундами '
Tm = is_parsing_time("143045", "hhmmss")

' Разбор времени с двоеточиями '
Tm = is_parsing_time("14:30:45", "hh:mm:ss")
```

---

### is_quarter_number — номер квартала

в системе EME.WMS функция `is_quarter_number` возвращает номер квартала для указанной даты.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Date | Да | Исходная дата |

**Возвращает**: `Integer` — номер квартала от 1 до 4.

```EME-L
' Квартал для 15 марта '
Q = is_quarter_number(Dt)
' Q = 1 '

' Квартал для 20 июня '
Q = is_quarter_number(SummerDt)
' Q = 2 '
```

---

### is_year — год из даты

в языке EME-L функция `is_year` возвращает год из значения даты/времени.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | DateTime | Да | Дата/время |

**Возвращает**: `Integer` — год.

```EME-L
' Извлечь год '
Year = is_year(Dt)
' Year = 2025 '
```

---

### is_shortyear — короткий год

в EME БД функция `is_shortyear` возвращает последние две цифры года.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | DateTime | Да | Дата/время |

**Возвращает**: `Integer` — последние две цифры года от 0 до 99.

```EME-L
' Последние две цифры года '
Short = is_shortyear(Dt)
' Short = 25 '
```

---

### is_month — месяц из даты

в языке EME-L функция `is_month` возвращает номер месяца из значения даты/времени.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | DateTime | Да | Дата/время |

**Возвращает**: `Integer` — номер месяца от 1 до 12.

```EME-L
' Извлечь месяц '
Month = is_month(Dt)
' Month = 3 '
```

---

### is_day — день месяца

в системе EME.WMS функция `is_day` возвращает день месяца из значения даты/времени.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | DateTime | Да | Дата/время |

**Возвращает**: `Integer` — день месяца от 1 до 31.

```EME-L
' Извлечь день '
Day = is_day(Dt)
' Day = 15 '
```

---

### is_hour — час из даты/времени

в языке EME-L функция `is_hour` возвращает час из значения даты/времени.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | DateTime | Да | Дата/время |

**Возвращает**: `Integer` — час от 0 до 23.

```EME-L
' Извлечь час '
Hour = is_hour(Dt)
' Hour = 14 '
```

---

### is_minute — минуты из даты/времени

в EME БД функция `is_minute` возвращает минуты из значения даты/времени.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | DateTime | Да | Дата/время |

**Возвращает**: `Integer` — минуты от 0 до 59.

```EME-L
' Извлечь минуты '
Min = is_minute(Dt)
' Min = 30 '
```

---

### is_second — секунды из даты/времени

в языке EME-L функция `is_second` возвращает секунды из значения даты/времени.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | DateTime | Да | Дата/время |

**Возвращает**: `Integer` — секунды от 0 до 59.

```EME-L
' Извлечь секунды '
Sec = is_second(Dt)
' Sec = 45 '
```

---

### is_csecond — сотые доли секунды

в системе EME.WMS функция `is_csecond` возвращает сотые доли секунды из значения даты/времени.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | DateTime | Да | Дата/время |

**Возвращает**: `Integer` — сотые доли секунды от 0 до 99.

```EME-L
' Извлечь сотые доли секунды '
CSec = is_csecond(Dt)
```

---

### is_date — создание даты

в языке EME-L функция `is_date` создает значение типа дата из компонентов или возвращает текущую дату.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer / DateTime | Нет | День месяца (1-31) или дата/время для извлечения чистой даты |
| args[1] | Integer | Нет | Месяц (1-12). По умолчанию 1 |
| args[2] | Integer | Нет | Год. По умолчанию 100 |
| args[3] | Integer | Нет | Час (0-23). По умолчанию 0 |
| args[4] | Integer | Нет | Минута (0-59). По умолчанию 0 |
| args[5] | Integer | Нет | Секунда (0-59). По умолчанию 0 |

**Вызов без аргументов**: возвращает текущую дату.

**Вызов с одним аргументом типа DateTime**: возвращает чистую дату с обнуленным временем.

**Возвращает**: `Date` — значение типа дата.

```EME-L
' Текущая дата '
Today = is_date()

' Создать дату из компонентов '
Dt = is_date(15, 3, 2025)

' Извлечь чистую дату из даты/времени '
PureDate = is_date(DateTimeValue)
```

---

### is_current_date — бортовая дата компьютера

в EME БД функция `is_current_date` возвращает текущую бортовую дату компьютера.

**Параметры**: нет.

**Возвращает**: `Date` — текущая дата.

```EME-L
' Бортовая дата компьютера '
BoardDate = is_current_date()
```

---

### is_current_time — бортовое время компьютера

в языке EME-L функция `is_current_time` возвращает текущее бортовое время компьютера.

**Параметры**: нет.

**Возвращает**: `Time` — текущее время.

```EME-L
' Бортовое время компьютера '
BoardTime = is_current_time()
```

---

### is_days_in_month — дней в месяце

в системе EME.WMS функция `is_days_in_month` возвращает количество дней в указанном месяце.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer / Date | Да | Номер месяца (1-12) или дата |
| args[1] | Integer | Нет | Год. Обязателен, если args[0] = 2 (февраль) и задан как номер месяца |

**Возвращает**: `Integer` — количество дней в месяце (28-31).

```EME-L
' Дней в марте '
Days = is_days_in_month(3)
' Days = 31 '

' Дней в феврале 2024 (високосный) '
Days = is_days_in_month(2, 2024)
' Days = 29 '

' Дней в месяце по дате '
Days = is_days_in_month(Dt)
```

---

### is_days_in_year — дней в году

в языке EME-L функция `is_days_in_year` возвращает количество дней в указанном году.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer / Date | Да | Год или дата |

**Возвращает**: `Integer` — 365 или 366.

```EME-L
' Дней в 2024 году '
Days = is_days_in_year(2024)
' Days = 366 '

' Дней в году по дате '
Days = is_days_in_year(Dt)
```

---

### is_null_date — нулевая дата

в EME БД функция `is_null_date` возвращает нулевую дату (00.00.0000).

**Параметры**: нет.

**Возвращает**: `Date` — нулевая дата.

```EME-L
' Нулевая дата '
NullDt = is_null_date()
```

---

### is_null_time — нулевое время

в языке EME-L функция `is_null_time` возвращает нулевое время (00:00:00.00).

**Параметры**: нет.

**Возвращает**: `Time` — нулевое время.

```EME-L
' Нулевое время '
NullTm = is_null_time()
```

---

### is_time_interval — строковое представление интервала

в системе EME.WMS функция `is_time_interval` формирует строковое представление временного интервала.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Real | Да | Временной интервал в днях. Может быть получен как разность двух значений дата/время |
| args[1] | Integer | Нет | Формат вывода: 0 — до минут, 1 — до секунд, 2 — с округлением, 3 — HH:MM, 4 — HH:MM:SS, 5 — дни или часы. По умолчанию — X суток, HH часов, MM минут |

**Возвращает**: `String` — строковое представление интервала.

```EME-L
' Интервал в днях '
Interval = EndDt - StartDt

' Формат по умолчанию '
Text = is_time_interval(Interval)

' До минут '
Text = is_time_interval(Interval, 0)

' До секунд '
Text = is_time_interval(Interval, 1)

' Формат HH:MM '
Text = is_time_interval(Interval, 3)
```

---

### is_datetime2USDTstring — дата/время в USDT-строку

в языке EME-L функция `is_datetime2USDTstring` формирует строку универсального сортируемого формата даты/времени (YYYYMMDDHHMMSS).

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Date | Да | Дата |
| args[1] | Time | Да | Время |

**Возвращает**: `String` — строка в формате YYYYMMDDHHMMSS.

```EME-L
' Формирование сортируемой строки '
USDT = is_datetime2USDTstring(Dt, Tm)
' USDT = "20250315143000" '
```

---

### is_USDTstring2datetime — USDT-строка в дату/время

в EME БД функция `is_USDTstring2datetime` преобразует строку формата YYYYMMDDHHMMSS в значение даты/времени.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Строка в формате YYYYMMDDHHMMSS |

**Возвращает**: `DateTime` — значение типа дата/время.

```EME-L
' Преобразование сортируемой строки '
Dt = is_USDTstring2datetime("20250315143000")
```

---

### is_time — создание времени

в системе EME.WMS функция `is_time` создает значение типа время из компонентов или возвращает текущее время.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer / DateTime | Нет | Час (0-23) или дата/время для извлечения времени |
| args[1] | Integer | Нет | Минута (0-59). По умолчанию 0 |
| args[2] | Integer | Нет | Секунда (0-59). По умолчанию 0 |
| args[3] | Integer | Нет | Сотые доли секунды (0-99). По умолчанию 0 |

**Вызов без аргументов**: возвращает текущее время.

**Вызов с одним аргументом типа DateTime**: возвращает время из указанного значения.

**Возвращает**: `Time` — значение типа время.

```EME-L
' Текущее время '
Now = is_time()

' Создать время из компонентов '
Tm = is_time(14, 30, 45)

' Извлечь время из даты/времени '
PureTime = is_time(DateTimeValue)
```

---

### is_system_utc_local_bias — смещение UTC

в языке EME-L функция `is_system_utc_local_bias` возвращает системное смещение между локальным временем и UTC в минутах.

**Параметры**: нет.

**Возвращает**: `Integer` — смещение в минутах. Отрицательное для восточных, положительное для западных часовых поясов. При ошибке возвращает NEW_LINE_MARK.

```EME-L
' Получить смещение UTC '
Bias = is_system_utc_local_bias()
' Bias = -180 для Москвы (UTC+3) '
```

---

### is_system_time_to_local_time — UTC в локальное время

в EME БД функция `is_system_time_to_local_time` преобразует универсальное координированное время (UTC) в локальное время с учетом часового пояса и перехода на летнее/зимнее время.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | DateTime | Да | Дата и время в формате UTC |
| args[1] | Integer | Нет | Смещение в минутах между UTC и локальным временем без учета летнего/зимнего времени. По умолчанию используется системное значение |

**Возвращает**: `DateTime` — локальное дата/время.

```EME-L
' Преобразовать UTC в локальное время '
LocalDt = is_system_time_to_local_time(UTC_Dt)
```

---

### is_local_time_to_system_time — локальное время в UTC

в языке EME-L функция `is_local_time_to_system_time` преобразует локальное время в универсальное координированное время (UTC) с учетом часового пояса и перехода на летнее/зимнее время.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | DateTime | Да | Локальная дата и время |
| args[1] | Integer | Нет | Смещение в минутах между UTC и локальным временем без учета летнего/зимнего времени. По умолчанию используется системное значение |

**Возвращает**: `DateTime` — UTC дата/время.

```EME-L
' Преобразовать локальное время в UTC '
UTCDt = is_local_time_to_system_time(LocalDt)
```

---

### is_start_date_period — начало четырехнедельного периода

в системе EME.WMS функция `is_start_date_period` возвращает дату начала четырехнедельного периода.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Date | Нет | Дата отсчета. По умолчанию текущая дата |
| args[1] | Integer | Нет | Смещение периодов: 1 — следующий, -1 — предыдущий. По умолчанию 0 |

**Возвращает**: `Date` — дата начала четырехнедельного периода.

```EME-L
' Начало текущего периода '
PeriodStart = is_start_date_period()

' Начало следующего периода '
NextPeriod = is_start_date_period(, 1)

' Начало периода от заданной даты '
PeriodStart = is_start_date_period(Dt)
```

---

### is_ISO8601_to_datetime — ISO 8601 в дату/время

в языке EME-L функция `is_ISO8601_to_datetime` преобразует строку в формате ISO 8601 в значение типа дата/время.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Строка даты/времени в формате ISO 8601 |

**Возвращает**: `DateTime` — значение типа дата/время.

```EME-L
' Преобразовать ISO 8601 в дату/время '
Dt = is_ISO8601_to_datetime("2025-03-15T14:30:00+03:00")
```

---

### is_datetime_to_ISO8601 — дата/время в ISO 8601

в EME БД функция `is_datetime_to_ISO8601` преобразует значение даты/времени в строку формата ISO 8601.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | DateTime | Да | Дата и время |

**Возвращает**: `String` — строка в формате ISO 8601.

```EME-L
' Преобразовать дату/время в ISO 8601 '
ISO = is_datetime_to_ISO8601(Dt)
' ISO = "2025-03-15T14:30:00+03:00" '
```

---

### is_time_zone — часовой пояс

в системе EME.WMS функция `is_time_zone` возвращает строковое представление текущего часового пояса.

**Параметры**: нет.

**Возвращает**: `String` — временная зона в формате (+|-)hh:mm.

```EME-L
' Получить текущий часовой пояс '
Zone = is_time_zone()
' Zone = "+03:00" '
```

---

### is_date_diff — разность дат (DATEDIFF)

в языке EME-L функция `is_date_diff` возвращает количество пересеченных границ указанного типа между двумя датами (аналог DATEDIFF из MS SQL).

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Тип границы: day, week, month, year |
| args[1] | Date | Да | Начальная дата |
| args[2] | Date | Да | Конечная дата |

**Возвращает**: `Integer` — количество пересеченных границ со знаком. При неизвестном типе возвращает пустое значение.

```EME-L
' Разность в днях '
Diff = is_date_diff("day", StartDt, EndDt)

' Разность в неделях '
Diff = is_date_diff("week", StartDt, EndDt)

' Разность в месяцах '
Diff = is_date_diff("month", StartDt, EndDt)

' Разность в годах '
Diff = is_date_diff("year", StartDt, EndDt)
```

---

### is_diff_datetime_in_sec — разность в секундах

в EME БД функция `is_diff_datetime_in_sec` возвращает разность между двумя значениями даты/времени в секундах.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | DateTime | Да | Первая дата/время |
| args[1] | DateTime | Да | Вторая дата/время |

**Возвращает**: `Integer` — разность в секундах. Если arg0 меньше arg1 или год вне диапазона 1971-3000, возвращает -1.

```EME-L
' Разность в секундах '
DiffSec = is_diff_datetime_in_sec(Dt1, Dt2)
```
