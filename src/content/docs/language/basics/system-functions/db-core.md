---
title: "База данных: ядро"
sidebar:
  order: 12
  label: "База данных: ядро"
---

## База данных: ядро

is-функции группы DB отвечают за работу с данными базы данных в языке EME-L. в системе EME.WMS эти функции обеспечивают чтение и запись полей, навигацию по записям и строкам, поиск данных, получение метаданных структуры БД и проверку состояния строк.

---

### is_data — чтение данных по реляционному пути

в языке EME-L функция `is_data` читает содержимое поля базы данных по реляционному пути через одну или несколько записей. Поддерживает прохождение через ссылочные поля, мягкие ссылки и массивы ссылок.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String / Integer | Да | Имя записи, физический номер (>= 1000), заголовок структуры БД (>= 2000) или положение объекта |
| args[1] | Integer | Да | Номер строки в записи |
| args[2] | String / Integer | Да | Имя или номер первого поля реляционного пути |
| args[3+] | String / Integer / Integer | Нет | Дополнительные поля пути или индекс массива ссылок |

**Возвращает**: значение последнего поля в пути — `String`, `Integer`, `Boolean`, `Date`, `Time`, `Reference` или `Array` в зависимости от атрибута поля. При пустой ссылке в середине пути возвращает `NULL_REF` для ссылочных типов или пустое значение.

```EME-L
' Прочитать название клиента из записи Клиенты, строка 5, поле Название '
Name = is_data("Клиенты", 5, "Название")
' Name = "ООО Ромашка" '

' Прочитать через ссылочное поле: заказ -> клиент -> название '
ClientName = is_data("Заказы", 10, "Клиент", "Название")
' ClientName = "ООО Ромашка" '

' Прочитать элемент массива ссылок: строка 3, поле Товары, индекс 0 '
ProductRef = is_data("Заказы", 3, "Товары", 0)
' ProductRef = 15 - ссылка на строку 15 записи Товары '

' Чтение из текущего объекта (положение 0) '
Value = is_data(0, 0, "Название")
' Value = значение поля Название текущей строки '
```

---

### is_get_data — чтение поля по номерам

в системе EME.WMS функция `is_get_data` читает содержимое поля записи по номеру записи, поля и строки. Использует прямые номера в отличие от `is_data`, которая работает с именами и реляционными путями.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Номер записи |
| args[1] | Integer | Да | Номер поля |
| args[2] | Integer | Да | Номер строки |
| args[3] | Integer | Нет | Код атрибута для интерпретации данных. По умолчанию используется атрибут поля из БД |

**Возвращает**: содержимое поля — `String`, `Integer`, `Boolean`, `Date`, `Time`, `Reference` или `Array` в зависимости от атрибута.

```EME-L
' Прочитать строковое поле 5 записи 10, строка 3 '
Value = is_get_data(10, 5, 3)
' Value = содержимое поля '

' Прочитать с явным указанием атрибута (строка) '
Text = is_get_data(10, 5, 3, 1)
' Text = содержимое поля, интерпретированное как строка '
```

---

### is_put_data — запись поля по номерам

в языке EME-L функция `is_put_data` записывает данные в поле записи по номеру записи, поля и строки.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Номер записи |
| args[1] | Integer | Да | Номер поля |
| args[2] | Integer | Да | Номер строки |
| args[3] | Any | Да | Значение для записи |

**Возвращает**: пустое значение.

```EME-L
' Записать "Привет" в поле 5 записи 10, строка 3 '
is_put_data(10, 5, 3, "Привет")

' Записать число 42 в поле 7 записи 10, строка 3 '
is_put_data(10, 7, 3, 42)
' Запись выполнена '
```

---

### is_get_data_in_base64 — чтение данных в Base64

в системе EME.WMS функция `is_get_data_in_base64` читает данные из поля записи и возвращает их в виде строки, закодированной в Base64.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Номер записи |
| args[1] | Integer | Да | Номер поля |
| args[2] | Integer | Да | Номер строки |

**Возвращает**: `String` — данные поля в формате Base64.

```EME-L
' Прочитать данные поля в Base64 '
Base64Data = is_get_data_in_base64(10, 5, 3)
' Base64Data = "SGVsbG8gV29ybGQ=" '
```

---

### is_put_data_in_base64 — запись данных из Base64

в языке EME-L функция `is_put_data_in_base64` записывает данные в поле записи из строки, закодированной в Base64.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Номер записи |
| args[1] | Integer | Да | Номер поля |
| args[2] | Integer | Да | Номер строки |
| args[3] | String | Да | Строка с данными в формате Base64 |

**Возвращает**: пустое значение.

```EME-L
' Записать данные из Base64 в поле '
is_put_data_in_base64(10, 5, 3, "SGVsbG8gV29ybGQ=")
' Данные записаны '
```

---

### is_rec — номер базовой записи

в системе EME.WMS функция `is_rec` возвращает номер базовой записи объекта в указанном положении иерархии.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Нет | Положение объекта: -1 — родитель, 0 — сам объект, 1 — потомок. По умолчанию 0 |

**Возвращает**: `Integer` — номер записи.

```EME-L
' Получить номер текущей записи '
RecNo = is_rec()
' RecNo = 10 '

' Получить номер записи родительского объекта '
ParentRec = is_rec(-1)
' ParentRec = 5 '
```

---

### is_line — номер базовой строки

в языке EME-L функция `is_line` возвращает номер строки базовой записи объекта или значение специальной константы.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Нет | Положение объекта: -1 — родитель, 0 — сам объект, 1 — потомок. По умолчанию 0 |
| args[1] | Integer | Нет | Специальное значение: 0 — NEW_LINE_MARK, 2 — номер строки диалога, иначе — NULL_REF |

**Возвращает**: `Integer` — номер строки или значение константы.

```EME-L
' Получить номер текущей строки '
LineNo = is_line()
' LineNo = 5 '

' Получить специальную константу NEW_LINE_MARK '
NewLine = is_line(, 0)
' NewLine = -1 '

' Получить номер строки родительского объекта '
ParentLine = is_line(-1)
' ParentLine = 3 '
```

---

### is_wrong_line — проверка недопустимой строки

в EME БД функция `is_wrong_line` проверяет, является ли указанная строка записи недопустимой (удаленной или некорректной).

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String / Integer | Нет | Имя или номер записи. По умолчанию базовая запись |
| args[1] | Integer | Нет | Номер строки. По умолчанию текущая строка |

**Возвращает**: `Boolean` — `TRUE` если строка недопустима, иначе `FALSE`.

```EME-L
' Проверить, удалена ли строка 5 записи Клиенты '
IsDeleted = is_wrong_line("Клиенты", 5)
' IsDeleted = TRUE - строка удалена '

' Проверить текущую строку '
Result = is_wrong_line()
' Result = FALSE - строка валидна '
```

---

### is_deleted_line — проверка удаленной строки

в языке EME-L функция `is_deleted_line` проверяет, помечена ли строка записи как удаленная.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String / Integer | Да | Имя или номер записи |
| args[1] | Integer | Да | Номер строки |

**Возвращает**: `Boolean` — `TRUE` если строка удалена, иначе `FALSE`.

```EME-L
' Проверить строку 3 записи Заказы '
Deleted = is_deleted_line("Заказы", 3)
' Deleted = FALSE - строка не удалена '
```

---

### is_modified_line — проверка модифицированной строки

в системе EME.WMS функция `is_modified_line` проверяет флаг модификации строки записи или возвращает номера всех модифицированных строк.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String / Integer | Да | Имя или номер записи |
| args[1] | Integer / Reference | Да | Номер строки для проверки флага или объект массива для записи номеров |
| args[2] | Integer | Нет | Игнорируется |

**Возвращает**: при числовой строке — флаг модификации (`Integer`); при массиве — количество добавленных номеров (`Integer`).

```EME-L
' Проверить флаг модификации строки 5 '
Flag = is_modified_line("Заказы", 5)
' Flag = 1 - строка модифицирована '

' Получить все модифицированные строки в массив '
ModArray = Object("Array")
Count = is_modified_line("Заказы", ModArray)
' Count = 3 - найдено 3 модифицированные строки '
```

---

### is_new_item — проверка новой строки

в языке EME-L функция `is_new_item` проверяет, является ли текущая строка в базовой записи новой (еще не сохраненной в базе данных).

**Параметры**: нет.

**Возвращает**: `Boolean` — `TRUE` если строка новая, иначе `FALSE`.

```EME-L
' Проверить, является ли текущая строка новой '
IsNew = is_new_item()
' IsNew = TRUE - строка еще не сохранена '
```

---

### is_No_of_lines — количество строк в записи

в EME БД функция `is_No_of_lines` возвращает количество строк в указанной записи.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String / Integer | Нет | Имя или номер записи. По умолчанию базовая запись |
| args[1] | Integer | Нет | Режим: 0 — общее количество (по умолчанию), 1 — неудаленные, 2 — удаленные |

**Возвращает**: `Integer` — количество строк.

```EME-L
' Общее количество строк в записи Клиенты '
Total = is_No_of_lines("Клиенты")
' Total = 100 '

' Количество неудаленных строк '
Active = is_No_of_lines("Клиенты", 1)
' Active = 95 '

' Количество удаленных строк '
Deleted = is_No_of_lines("Клиенты", 2)
' Deleted = 5 '
```

---

### is_No_of_records — количество записей в проекте

в языке EME-L функция `is_No_of_records` возвращает общее количество записей в проекте.

**Параметры**: нет.

**Возвращает**: `Integer` — количество записей.

```EME-L
' Получить общее количество записей '
Count = is_No_of_records()
' Count = 150 '
```

---

### is_No_of_fields — количество полей в записи

в системе EME.WMS функция `is_No_of_fields` возвращает количество полей в указанной записи.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String / Integer | Да | Имя или номер записи |

**Возвращает**: `Integer` — количество полей.

```EME-L
' Количество полей в записи Клиенты '
Fields = is_No_of_fields("Клиенты")
' Fields = 15 '

' Количество полей по номеру записи '
FieldsByNo = is_No_of_fields(10)
' FieldsByNo = 15 '
```

---

### is_No_of_array — количество элементов массива

в языке EME-L функция `is_No_of_array` возвращает количество элементов массива в указанном поле записи.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String / Integer | Да | Имя записи, физический номер или положение объекта |
| args[1] | Integer | Нет | Номер строки. По умолчанию текущая строка |
| args[2] | String / Integer | Да | Имя или номер поля массива |

**Возвращает**: `Integer` — количество элементов массива.

```EME-L
' Количество элементов в массиве ссылок '
Count = is_No_of_array("Заказы", 5, "Товары")
' Count = 3 - в заказе 3 товара '
```

---

### is_fld_length — длина поля в байтах

в EME БД функция `is_fld_length` возвращает длину указанного поля записи в байтах.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String / Integer | Да | Имя или номер записи |
| args[1] | String / Integer | Да | Имя или номер поля |
| args[2] | Integer | Нет | Номер строки. Если не указана, возвращается максимальная длина поля |

**Возвращает**: `Integer` — размер данных в байтах.

```EME-L
' Длина поля в конкретной строке '
Size = is_fld_length("Клиенты", "Название", 5)
' Size = 50 '

' Максимальная длина поля в записи '
MaxSize = is_fld_length("Клиенты", "Название")
' MaxSize = 100 '
```

---

### is_fld_type — тип атрибута поля

в языке EME-L функция `is_fld_type` возвращает код типа атрибута поля записи.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Номер записи |
| args[1] | Integer | Да | Номер поля |

**Возвращает**: `Integer` — код типа атрибута поля.

```EME-L
' Получить тип атрибута поля 5 записи 10 '
AttrType = is_fld_type(10, 5)
' AttrType = 1 - текстовая строка '
```

---

### is_fld_ref_type — проверка ссылочного поля

в системе EME.WMS функция `is_fld_ref_type` проверяет, является ли поле ссылочного типа.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Номер записи |
| args[1] | Integer | Да | Номер поля |

**Возвращает**: `Boolean` — `TRUE` если поле ссылочное, иначе `FALSE`.

```EME-L
' Проверить, является ли поле 5 ссылочным '
IsRef = is_fld_ref_type(10, 5)
' IsRef = TRUE - поле является ссылкой '
```

---

### is_fld_name_by_number — имя поля по номеру

в языке EME-L функция `is_fld_name_by_number` возвращает софтовое имя поля по его номеру.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Номер записи |
| args[1] | Integer | Да | Номер поля |

**Возвращает**: `String` — софтовое имя поля.

```EME-L
' Получить имя поля 5 записи 10 '
Name = is_fld_name_by_number(10, 5)
' Name = "Название" '
```

---

### is_record_narrative — имя записи по номеру

в системе EME.WMS функция `is_record_narrative` возвращает софтовое имя записи по ее номеру.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Номер записи |

**Возвращает**: `String` — софтовое имя записи.

```EME-L
' Получить имя записи номер 10 '
Name = is_record_narrative(10)
' Name = "Клиенты" '
```

---

### is_field_narrative — имя поля по номерам

в языке EME-L функция `is_field_narrative` возвращает софтовое имя поля по номеру записи и номеру поля.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Номер записи |
| args[1] | Integer | Да | Номер поля |

**Возвращает**: `String` — софтовое имя поля.

```EME-L
' Получить имя поля 5 записи 10 '
Name = is_field_narrative(10, 5)
' Name = "Название" '
```

---

### is_attribute_narrative — название атрибута

в EME БД функция `is_attribute_narrative` возвращает название типа атрибута по его коду.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Код типа атрибута |

**Возвращает**: `String` — название атрибута.

```EME-L
' Получить название атрибута с кодом 1 '
AttrName = is_attribute_narrative(1)
' AttrName = "Текстовая строка" '
```

---

### is_line_key — ключ строки записи

в языке EME-L функция `is_line_key` возвращает ключ строки записи.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String / Integer | Нет | Имя или номер записи. По умолчанию базовая запись |
| args[1] | Integer | Нет | Номер строки. По умолчанию текущая строка |

**Возвращает**: `Integer` — ключ строки.

```EME-L
' Получить ключ текущей строки '
Key = is_line_key()
' Key = 12345 '

' Получить ключ строки 5 записи Клиенты '
Key = is_line_key("Клиенты", 5)
' Key = 67890 '
```

---

### is_equ_data — поиск строки по значению

в системе EME.WMS функция `is_equ_data` ищет строку в записи, в которой содержимое указанного поля равно заданному значению.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String / Integer | Нет | Имя записи, физический номер или положение объекта. По умолчанию базовая запись |
| args[1] | String / Integer | Да | Имя или номер поля |
| args[2] | Any | Да | Искомое значение |
| args[3] | String | Нет | Битовая маска для побитового сравнения |
| args[4] | String | Нет | Текстовое выражение для дополнительной проверки |

**Возвращает**: `Integer` — номер найденной строки. Если не найдена, возвращает `NULL_REF`.

```EME-L
' Найти строку в записи Клиенты, где поле Код = "C001" '
Line = is_equ_data("Клиенты", "Код", "C001")
' Line = 5 '

' Найти с битовой маской '
Line = is_equ_data("Флаги", "Значение", 5, "\x0F")
' Line = номер строки, где младшие 4 бита равны 5 '

' Найти с дополнительным условием '
Line = is_equ_data("Заказы", "Статус", 1, , "is_arg(0) <> 5")
' Line = строка со статусом 1, исключая строку 5 '
```

---

### is_max_value — максимальное значение поля

в языке EME-L функция `is_max_value` возвращает максимальное значение из заданного поля по всем строкам записи.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String / Integer | Да | Имя или номер записи |
| args[1] | String / Integer | Да | Имя или номер поля |
| args[2] | String | Нет | Текстовое выражение для отбора строк |

**Возвращает**: максимальное значение — `Integer`, `Double` или `String` в зависимости от типа поля.

```EME-L
' Максимальная цена в прайс-листе '
MaxPrice = is_max_value("Прайс", "Цена")
' MaxPrice = 9999.99 '

' Максимальный код с условием отбора '
MaxCode = is_max_value("Товары", "Код", "is_arg(0) > 10")
' MaxCode = 150 '
```

---

### is_for_rec — обход строк с аккумулятором

в EME БД функция `is_for_rec` выполняет обход всех строк записи с накоплением значения в аккумуляторе.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String / Integer | Нет | Имя или номер записи. По умолчанию базовая запись |
| args[1] | Any | Да | Начальное значение аккумулятора |
| args[2] | String | Да | Текстовое выражение отбора. Если ИСТИНА, строка пропускается |
| args[3] | String | Да | Текстовое выражение для вычисления нового значения аккумулятора |

**Возвращает**: конечное значение аккумулятора — тип соответствует начальному значению.

```EME-L
' Подсчитать сумму поля Цена по всем строкам '
Total = is_for_rec("Заказы", 0, "", "is_arg(1) + is_data(\"Заказы\", is_arg(0), \"Цена\")")
' Total = общая сумма цен '

' Подсчитать только неудаленные строки '
Total = is_for_rec("Заказы", 0, "is_wrong_line(\"Заказы\", is_arg(0))", "is_arg(1) + 1")
' Total = количество неудаленных строк '
```

---

### is_full_path — полный путь классификатора

в языке EME-L функция `is_full_path` возвращает полный путь классификатора в виде строки с разделителями-точками.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Нет | Номер записи. -1 — текущая запись. По умолчанию используется контекст |
| args[1] | Integer | Нет | Номер строки. -1 — текущая строка |
| args[2+] | Integer | Нет | Номера полей, формирующих реляционный путь |

**Возвращает**: `String` — полный путь классификатора.

```EME-L
' Полный путь классификатора '
Path = is_full_path(-1, -1, 0, 1)
' Path = "Узел1.Узел2.Узел3" '
```

---

### is_alias — поиск поля по имени

в системе EME.WMS функция `is_alias` ищет первое поле с указанным именем в заданной записи.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Номер записи |
| args[1+] | String | Да | Одно или несколько имен полей для поиска |

**Возвращает**: `Integer` — номер первого найденного поля. Если не найдено, вызывает ошибку.

```EME-L
' Найти поле с именем "Название" или "Имя" в записи 10 '
FieldNo = is_alias(10, "Название", "Имя")
' FieldNo = 5 '
```

---

### is_try_find_field — поиск поля по имени (без ошибки)

в языке EME-L функция `is_try_find_field` ищет поле в записи по софтовому имени. В отличие от `is_alias`, не вызывает ошибку если поле не найдено.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Номер записи |
| args[1] | String | Да | Имя поля для поиска |

**Возвращает**: `Integer` — номер найденного поля или `NULL_FIELD` (-100) если не найдено.

```EME-L
' Попытаться найти поле '
FieldNo = is_try_find_field(10, "Название")
' FieldNo = 5 '

' Поле не существует '
FieldNo = is_try_find_field(10, "Несуществующее")
' FieldNo = -100 '
```

---

### is_try_find_record — поиск записи по имени (без ошибки)

в EME БД функция `is_try_find_record` ищет запись в базе данных по софтовому имени. В отличие от прямого обращения, не вызывает ошибку если запись не найдена.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Софтовое имя записи |

**Возвращает**: `Integer` — номер записи или `NULL_RECORD` (-100) если не найдена.

```EME-L
' Попытаться найти запись '
RecNo = is_try_find_record("Клиенты")
' RecNo = 10 '

' Запись не существует '
RecNo = is_try_find_record("Несуществующая")
' RecNo = -100 '
```

---

### is_get_record_by_name — номер записи по имени (системные)

в языке EME-L функция `is_get_record_by_name` возвращает номер записи по софтовому имени, включая системные записи.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Софтовое имя записи |

**Возвращает**: `Integer` — номер записи.

```EME-L
' Получить номер системной записи '
RecNo = is_get_record_by_name("SYS_SYSTEM")
' RecNo = номер системной записи '
```

---

### is_get_field_by_name — номер поля по имени (системные)

в системе EME.WMS функция `is_get_field_by_name` возвращает номер поля по имени в заданной записи, включая системные поля.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Номер записи |
| args[1] | String | Да | Софтовое имя поля |

**Возвращает**: `Integer` — номер поля.

```EME-L
' Получить номер системного поля '
FldNo = is_get_field_by_name(10, "SYS_FIELD")
' FldNo = номер системного поля '
```

---

### is_get_record_flags — флаги записи

в языке EME-L функция `is_get_record_flags` возвращает флаги записи базы данных.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String / Integer | Да | Имя или номер записи |

**Возвращает**: `Integer` — флаги записи.

```EME-L
' Получить флаги записи Клиенты '
Flags = is_get_record_flags("Клиенты")
' Flags = битовая маска флагов '
```

---

### is_get_field_flags — флаги поля

в EME БД функция `is_get_field_flags` возвращает флаги поля записи базы данных.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String / Integer | Да | Имя или номер записи |
| args[1] | String / Integer | Да | Имя или номер поля |

**Возвращает**: `Integer` — флаги поля.

```EME-L
' Получить флаги поля Название записи Клиенты '
Flags = is_get_field_flags("Клиенты", "Название")
' Flags = битовая маска флагов поля '
```

---

### is_find_string_like_regexp — поиск по регулярному выражению

в системе EME.WMS функция `is_find_string_like_regexp` осуществляет поиск в базе данных по регулярному выражению.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String / Integer | Да | Имя или номер записи |
| args[1] | String / Integer | Да | Имя или номер поля |
| args[2] | String | Да | Регулярное выражение для поиска |
| args[3] | Reference | Да | Объект массива для сохранения номеров найденных строк |

**Возвращает**: `Integer` — количество найденных строк.

```EME-L
' Поиск по регулярному выражению '
ResultArray = Object("Array")
Count = is_find_string_like_regexp("Клиенты", "Название", "^ООО", ResultArray)
' Count = 5 - найдено 5 строк, начинающихся с "ООО" '
```

---

### is_get_db_file_stamp — временной штамп файла поля

в языке EME-L функция `is_get_db_file_stamp` возвращает временной штамп файла поля базы данных.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String / Integer | Да | Имя или номер записи |
| args[1] | String / Integer | Да | Имя или номер поля |

**Возвращает**: `Integer` — временной штамп файла.

```EME-L
' Получить штамп файла поля '
Stamp = is_get_db_file_stamp("Клиенты", "Название")
' Stamp = временной штамп '
```

---

### is_get_field_CRC — контрольная сумма поля

в EME БД функция `is_get_field_CRC` возвращает контрольную сумму CRC файла поля базы данных.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String / Integer | Да | Имя или номер записи |
| args[1] | String / Integer | Да | Имя или номер поля |

**Возвращает**: `Integer` — контрольная сумма CRC.

```EME-L
' Получить CRC поля '
CRC = is_get_field_CRC("Клиенты", "Название")
' CRC = контрольная сумма '
```

---

### is_own_line — проверка принадлежности филиалу

в языке EME-L функция `is_own_line` проверяет, принадлежит ли строка текущему филиалу.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String / Integer | Нет | Имя или номер записи. По умолчанию базовая запись |
| args[1] | Integer | Нет | Номер строки. По умолчанию текущая строка |

**Возвращает**: `Boolean` — `TRUE` если строка принадлежит текущему филиалу, иначе `FALSE`.

```EME-L
' Проверить принадлежность строки текущему филиалу '
IsOwn = is_own_line("Клиенты", 5)
' IsOwn = TRUE '
```

---

### is_owner — название филиала-владельца

в системе EME.WMS функция `is_owner` возвращает название филиала, на котором была создана строка.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String / Integer | Нет | Имя или номер записи. По умолчанию базовая запись |
| args[1] | Integer | Нет | Номер строки. По умолчанию текущая строка |

**Возвращает**: `String` — название филиала или `Boolean`.

```EME-L
' Получить название филиала, создавшего строку '
Filial = is_owner("Клиенты", 5)
' Filial = "Головной офис" '
```

---

### is_field_file_size — размер файла поля

в языке EME-L функция `is_field_file_size` возвращает размер файла поля базы данных.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Номер записи |
| args[1] | Integer | Да | Номер поля |

**Возвращает**: `Integer` — размер файла поля.

```EME-L
' Получить размер файла поля '
Size = is_field_file_size(10, 5)
' Size = 1048576 '
```

---

### is_field_occupied_size — занятый размер поля

в EME БД функция `is_field_occupied_size` возвращает фактический размер поля базы данных на диске.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Номер записи |
| args[1] | Integer | Да | Номер поля |

**Возвращает**: `Integer` — занятый размер на диске.

```EME-L
' Получить занятый размер поля '
Size = is_field_occupied_size(10, 5)
' Size = 524288 '
```

---

### is_test_references_counter — тест счетчиков ссылок

в системе EME.WMS функция `is_test_references_counter` выполняет тестирование и проверку счетчиков ссылок в базе данных.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Нет | Номер первой записи для проверки |
| args[1] | Integer | Нет | Номер последней записи для проверки |

**Возвращает**: пустое значение.

```EME-L
' Проверить счетчики ссылок '
is_test_references_counter()
' Проверка выполнена '

' Проверить диапазон записей '
is_test_references_counter(10, 50)
' Проверка выполнена для записей 10-50 '
```

---

### is_run_dbtest — тест целостности базы данных

в языке EME-L функция `is_run_dbtest` запускает тест целостности базы данных на текущей станции.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Нет | Тип теста: 0 — физический + логический, 1 — только физический, 2 — только логический |
| args[1] | Integer | Нет | Номер записи для тестирования. По умолчанию все записи |
| args[2] | Integer | Нет | Номер поля для тестирования. По умолчанию все поля |

**Возвращает**: `Integer` — результат выполнения теста.

```EME-L
' Полный тест целостности '
Result = is_run_dbtest()
' Result = 0 - ошибок не обнаружено '

' Только физический тест для записи 10 '
Result = is_run_dbtest(1, 10)
' Result = 0 '
```
