---
title: "IS-функции"
sidebar:
  order: 5
---

## Обзор is-функций в языке EME-L

is-функции в EME-L являются внешними встроенными функциями, которые не могут быть реализованы на EME-L. Вызов is-функции имеет следующий формат:

```EME-L
{is-функция}([{Аргумент1}, [{Аргумент2} ...]])
```

is-функции языка EME-L подразделяются на функциональные группы: Bit, Browser, Context и другие.

---

## is-функции группы Bit — битовые операции

is-функции группы Bit отвечают за работу с битовыми признаками в языке EME-L. в системе EME.WMS битовые операции используются для манипуляции флагами, проверки прав доступа, формирования масок и низкоуровневой обработки данных.

Доступные функции:

| Функция | Описание |
|---------|----------|
| `is_bit(arg0, arg1)` | Проверка установленного бита. Возвращает `TRUE`/`FALSE` |
| `is_clear_bit(arg0, arg1)` | Сброс указанного бита в 0 |
| `is_set_bit(arg0, arg1)` | Установка указанного бита в 1 |
| `is_reverse_bit(arg0, arg1)` | Инверсия бита (0→1, 1→0) |
| `is_and([arg0[, ...]])` | Побитовое И. Произвольное число параметров |
| `is_or([arg0[, ...]])` | Побитовое ИЛИ. Произвольное число параметров |
| `is_xor(arg0, arg1)` | Исключающее ИЛИ |
| `is_not(arg0)` | Побитовое НЕ (инверсия всех битов) |
| `is_mask(arg0[, ...])` | Формирование битовой маски |
| `is_shift_left(arg0, arg1)` | Побитовый сдвиг влево |
| `is_shift_right(arg0, arg1)` | Побитовый сдвиг вправо |

**Подробная документация** с таблицами параметров, типами и примерами кода — в разделе [Системные функции → Битовые операции](../system-functions/bit-operations/).

```EME-L
' Проверка бита 3 '
Result = is_bit(Flags, 3)

' Установить бит 5 '
Flags = is_set_bit(Flags, 5)

' Сбросить бит 5 '
Flags = is_clear_bit(Flags, 5)

' Маска с битами 1, 3 и 5: 0b101010 = 0x2A '
Mask = is_mask(1, 3, 5)
```

---

## is-функции группы Browser — работа с браузерами

is-функции группы Browser отвечают за работу с браузерами в EME БД. в системе EME.WMS браузеры используются для отображения и выбора данных из записей базы данных.

Доступные функции:

| Функция | Описание |
|---------|----------|
| `is_hewed(mark, line[, level])` | Проверка высечения строки |
| `is_hew_item(mark, line[, level])` | Высечение (выбор) строки |
| `is_empty_hew(mark[, level[, clear]])` | Проверка пустоты переменной высечения |
| `is_browse(name1[, name2[, ...]])` | Запуск браузера в каскадном режиме |
| `is_browse_data(column[, line])` | Содержимое колонки браузера |
| `is_browse_data_se(column)` | Содержимое колонки как вещественное число |
| `is_browser_base_record(line)` | Базовая запись браузера |
| `is_run_browse(record)` | Запуск браузера записи |
| `is_keydown_flag()` | Флаги состояния клавиатуры |

**Подробная документация** с таблицами параметров, типами и примерами кода — в разделе [Системные функции → Работа с браузерами](../system-functions/browser/).

```EME-L
' Запуск браузера '
Line = is_browse("BrowserOrders")

' Чтение данных колонки '
Value = is_browse_data("ClientName", Line)

' Проверка высечения '
Result = is_hewed("MyHew", 5)
```

---

## is-функции группы Context — работа с контекстом

is-функции группы Context отвечают за работу с глобальным контекстом в EME БД. в системе EME.WMS контекст используется для передачи данных между различными частями приложения, хранения состояния и проверки текущего контекста выполнения.

Доступные функции:

| Функция | Описание |
|---------|----------|
| `is_put_context(name, value)` | Сохранение значения в контекст |
| `is_get_context_as_string(name)` | Получение контекста как строки |
| `is_get_context_as_ref(name)` | Получение контекста как числа |
| `is_del_context(name)` | Удаление ячейки контекста |
| `is_office_doc_save(rec, field, line[, del])` | Проверка сохранения документа офиса |
| `is_office_doc_already_open(rec, field, line)` | Проверка открытия документа офиса |
| `is_check_context(type[, name])` | Проверка текущего контекста |
| `is_get_context(type)` | Получение имени текущего контекста |

**Подробная документация** с таблицами параметров, типами и примерами кода — в разделе [Системные функции → Работа с контекстом](../system-functions/context/).

```EME-L
' Сохранить и прочитать значение из контекста '
is_put_context("MyContext", "Hello")
Value = is_get_context_as_string("MyContext")

' Проверить текущий контекст '
Result = is_check_context("Dialog")

' Получить имя текущего диалога '
DialogName = is_get_context("Dialog")
```

---

## is-функции группы DateTime — дата и время

is-функции группы DateTime отвечают за работу с датами, временем, календарными периодами и часовыми поясами в языке EME-L. в системе EME.WMS эти функции используются для форматирования дат, расчета периодов, преобразования между временными зонами и извлечения компонентов даты/времени.

Доступные функции:

| Функция | Описание |
|---------|----------|
| `is_dos_date()` | Текущая системная дата |
| `is_dos_time()` | Текущее системное время |
| `is_datetime(arg0)` | Преобразование к типу дата/время |
| `is_fdatetime(arg0[, arg1])` | Форматирование даты/времени или обратное преобразование |
| `is_leap_year(arg0)` | Проверка високосного года |
| `is_strmonth(arg0[, arg1])` | Название месяца на русском языке |
| `is_start_date_week(arg0[, arg1])` | Дата начала недели (понедельник) |
| `is_final_date_week(arg0[, arg1])` | Дата конца недели (воскресенье) |
| `is_day_of_week(arg0)` | Номер дня недели (1 — понедельник) |
| `is_number_week_of_year(arg0)` | Номер недели в году |
| `is_week(arg0)` | Номер недели (неполная считается полной) |
| `is_start_date_week_of_number(arg0, arg1)` | Дата начала недели по номеру и году |
| `is_day_count_from_Christ(arg0)` | Количество дней от базовой даты |
| `is_start_date_month(arg0[, arg1])` | Дата начала месяца |
| `is_final_date_month(arg0[, arg1])` | Дата конца месяца |
| `is_start_date_decade(arg0[, arg1])` | Дата начала декады |
| `is_final_date_decade(arg0[, arg1])` | Дата конца декады |
| `is_start_date_quarter(arg0[, arg1])` | Дата начала квартала |
| `is_final_date_quarter(arg0[, arg1])` | Дата конца квартала |
| `is_parsing_date(arg0[, arg1[, arg2]])` | Разбор строки даты |
| `is_parsing_time(arg0[, arg1])` | Разбор строки времени |
| `is_quarter_number(arg0)` | Номер квартала |
| `is_year(arg0)` | Год из даты/времени |
| `is_shortyear(arg0)` | Последние две цифры года |
| `is_month(arg0)` | Месяц из даты/времени |
| `is_day(arg0)` | День месяца |
| `is_hour(arg0)` | Час из даты/времени |
| `is_minute(arg0)` | Минуты из даты/времени |
| `is_second(arg0)` | Секунды из даты/времени |
| `is_csecond(arg0)` | Сотые доли секунды |
| `is_date([arg0[, ...]])` | Создание даты из компонентов |
| `is_current_date()` | Бортовая дата компьютера |
| `is_current_time()` | Бортовое время компьютера |
| `is_days_in_month(arg0[, arg1])` | Количество дней в месяце |
| `is_days_in_year(arg0)` | Количество дней в году |
| `is_null_date()` | Нулевая дата |
| `is_null_time()` | Нулевое время |
| `is_time_interval(arg0[, arg1])` | Строковое представление временного интервала |
| `is_datetime2USDTstring(arg0, arg1)` | Дата/время в строку YYYYMMDDHHMMSS |
| `is_USDTstring2datetime(arg0)` | Строка YYYYMMDDHHMMSS в дату/время |
| `is_time([arg0[, ...]])` | Создание времени из компонентов |
| `is_system_utc_local_bias()` | Смещение UTC в минутах |
| `is_system_time_to_local_time(arg0[, arg1])` | UTC в локальное время |
| `is_local_time_to_system_time(arg0[, arg1])` | Локальное время в UTC |
| `is_start_date_period([arg0[, arg1]])` | Начало четырехнедельного периода |
| `is_ISO8601_to_datetime(arg0)` | ISO 8601 в дату/время |
| `is_datetime_to_ISO8601(arg0)` | Дата/время в ISO 8601 |
| `is_time_zone()` | Часовой пояс (+|-)hh:mm |
| `is_date_diff(arg0, arg1, arg2)` | Разность дат (аналог DATEDIFF) |
| `is_diff_datetime_in_sec(arg0, arg1)` | Разность в секундах |

**Подробная документация** с таблицами параметров, типами и примерами кода — в разделе [Системные функции → Дата и время](../system-functions/datetime/).

```EME-L
' Текущая дата и время '
Today = is_dos_date()
Now = is_dos_time()

' Форматирование даты '
TextDate = is_fdatetime(Today)

' Извлечение компонентов '
Year = is_year(Today)
Month = is_month(Today)
Day = is_day(Today)

' Разность дат в днях '
Diff = is_date_diff("day", StartDate, EndDate)
```

---

## is-функции группы DB — база данных

is-функции группы DB отвечают за работу с данными базы данных в языке EME-L. в системе EME.WMS эти функции обеспечивают чтение и запись полей, навигацию по записям и строкам, работу со ссылками, управление транзакциями, модификацию данных и работу с файлами.

Доступные функции:

| Функция | Описание |
|---------|----------|
| `is_data(запись, строка, поле1[, ...])` | Чтение данных по реляционному пути |
| `is_get_data(запись, поле, строка[, атрибут])` | Чтение поля по номерам |
| `is_put_data(запись, поле, строка, данные)` | Запись поля по номерам |
| `is_get_data_in_base64(запись, поле, строка)` | Чтение данных в Base64 |
| `is_put_data_in_base64(запись, поле, строка, данные)` | Запись данных из Base64 |
| `is_rec([положение])` | Номер базовой записи объекта |
| `is_line([положение[, константа]])` | Номер базовой строки |
| `is_wrong_line([запись[, строка]])` | Проверка недопустимой строки |
| `is_deleted_line(запись, строка)` | Проверка удаленной строки |
| `is_modified_line(запись, строка)` | Проверка модифицированной строки |
| `is_new_item()` | Проверка новой строки |
| `is_No_of_lines([запись[, режим]])` | Количество строк в записи |
| `is_No_of_records()` | Количество записей в проекте |
| `is_No_of_fields(запись)` | Количество полей в записи |
| `is_No_of_array(запись, строка, поле)` | Количество элементов массива |
| `is_fld_length(запись, поле[, строка])` | Длина поля в байтах |
| `is_fld_type(запись, поле)` | Тип атрибута поля |
| `is_fld_ref_type(запись, поле)` | Проверка ссылочного поля |
| `is_fld_name_by_number(запись, поле)` | Имя поля по номеру |
| `is_record_narrative(запись)` | Имя записи по номеру |
| `is_field_narrative(запись, поле)` | Имя поля по номерам |
| `is_attribute_narrative(атрибут)` | Название атрибута по коду |
| `is_line_key([запись[, строка]])` | Ключ строки записи |
| `is_equ_data(запись, поле, значение[, маска[, условие]])` | Поиск строки по значению |
| `is_max_value(запись, поле[, условие])` | Максимальное значение поля |
| `is_for_rec(запись, аккумулятор, условие, выражение)` | Обход строк с аккумулятором |
| `is_full_path([запись[, строка[, поле1[, ...]]]])` | Полный путь классификатора |
| `is_alias(запись, имя_поля1[, ...])` | Поиск поля по имени |
| `is_try_find_field(запись, имя_поля)` | Поиск поля без ошибки |
| `is_try_find_record(имя)` | Поиск записи без ошибки |
| `is_get_record_by_name(имя)` | Номер записи по имени (системные) |
| `is_get_field_by_name(запись, имя)` | Номер поля по имени (системные) |
| `is_get_record_flags(запись)` | Флаги записи |
| `is_get_field_flags(запись, поле)` | Флаги поля |
| `is_find_string_like_regexp(запись, поле, regexp, массив)` | Поиск по регулярному выражению |
| `is_get_db_file_stamp(запись, поле)` | Временной штамп файла |
| `is_get_field_CRC(запись, поле)` | Контрольная сумма CRC |
| `is_own_line([запись[, строка]])` | Проверка принадлежности филиалу |
| `is_owner([запись[, строка]])` | Название филиала-владельца |
| `is_field_file_size(запись, поле)` | Размер файла поля |
| `is_field_occupied_size(запись, поле)` | Занятый размер на диске |
| `is_ref_to([поля[, режим[, запись[, строка]]]])` | Количество ссылок на строку |
| `is_exist_ref_to([поля[, режим[, запись[, строка]]]])` | Проверка наличия ссылок |
| `is_referenceed_line(запись, поле[, начальная[, целевая]])` | Поиск строки по ссылке |
| `is_ref_to_child(...)` | Ссылки на дочерние строки |
| `is_ref_rec(запись, поле)` | Номер ссылаемой записи |
| `is_vvr_rec(мягкая_ссылка)` | Номер записи из мягкой ссылки |
| `is_vvr_line(мягкая_ссылка)` | Номер строки из мягкой ссылки |
| `is_vvr(запись, строка)` | Формирование мягкой ссылки |
| `is_in_ref_array(запись, поле, строка, значение)` | Проверка в массиве ссылок |
| `is_show_author(...)` | Данные из записи по ссылке массива |
| `is_linked(запись, поле, строка1, строка2)` | Проверка ветви классификатора |
| `is_prev_line(значение, запись, поле_значения, поле_фильтра, значение_фильтра)` | Поиск предыдущей строки |
| `is_ref_to_subitems(...)` | Ссылки на дочерние строки |
| `is_check_chain(...)` | Проверка цепочки |
| `is_find_reference_field(...)` | Поиск поля-ссылки |
| `is_restore_refs(запись, поле)` | Восстановление ссылок |
| `is_relink_ref(запись, поле, старое, новое)` | Замена ссылок |
| `is_redirect_referencies(запись, откуда, куда)` | Перенаправление ссылок |
| `is_clean_refs()` | Обнуление ссылок |
| `is_root(орган)` | Корневой элемент дерева |
| `is_parent(орган)` | Родительский элемент дерева |
| `is_transaction([направление[, параметр1[, ...]]])` | Управление транзакцией |
| `is_open_transaction(название)` | Условное открытие транзакции |
| `is_open_master_transaction()` | Открытие мастер-транзакции |
| `is_abort_transaction()` | Аварийное завершение транзакции |
| `is_local_transaction([направление])` | Управление локальной транзакцией |
| `is_emulate_transaction(направление)` | Эмуляция транзакции |
| `is_enable_multiple_transactions()` | Снятие ограничения на транзакции |
| `is_modify_record(запись1[, ...])` | Пометка всех строк записи |
| `is_unmodify_record(запись1[, ...])` | Снятие пометок модификации |
| `is_modify_line(запись, строка)` | Пометка строки |
| `is_unmodify_lines(запись, начальная, количество)` | Снятие флагов с группы строк |
| `is_delete_record_line(запись, строка)` | Удаление строки |
| `is_delete_records(запись1[, ...])` | Удаление всех строк в записях |
| `is_compress_records(режим, массив)` | Сжатие файлов записей |
| `is_prune_record(режим, запись)` | Сжатие одной записи |
| `is_compress_vlf(запись, поле)` | Сжатие VLF-поля |
| `is_empty_vlf(запись, поле, строка)` | Опустошение VLF-поля |
| `is_ws_align_data()` | Выравнивание данных с сервера |
| `is_disable_align_data([флаг])` | Отключение выравнивания |
| `is_reverse_db_data(тип[, начальная[, конечная]])` | Реверс данных в БД |
| `is_set_header_correction(режим)` | Коррекция заголовка БД |
| `is_set_check_transaction()` | Параметры проверки транзакции |
| `is_load_file_to_db(файл, запись, поле, строка)` | Загрузка файла в БД |
| `is_open_file_from_db(заголовок, расширение, запись, поле, строка)` | Открытие файла из БД |
| `is_print_file_from_db(расширение, запись, поле, строка[, таймаут])` | Печать файла из БД |
| `is_reduce_storage_to_date(запись, поле, дата)` | Очистка хранилища |
| `is_sql_client_execute(запрос, подключение)` | Выполнение SQL-запроса |
| `is_DelayedRefNuller_procedure()` | Отложенное обнуление ссылок |
| `is_run_export_ex()` | Ядерный экспорт раздела |
| `is_db_watch([команда])` | Наблюдение за изменениями |
| `is_check_db_watch(запись[, поле[, строка]])` | Проверка изменений |
| `is_get_modified_lines(массив, запись[, поле])` | Получение измененных строк |
| `is_test_references_counter([первая[, последняя]])` | Тест счетчиков ссылок |
| `is_run_dbtest([тип[, запись[, поле]]])` | Тест целостности БД |

**Подробная документация** с таблицами параметров, типами и примерами кода — в разделах:
- [Системные функции → База данных: ядро](../system-functions/db-core/)
- [Системные функции → База данных: ссылки](../system-functions/db-references/)
- [Системные функции → База данных: транзакции и модификация](../system-functions/db-transactions/)
- [Системные функции → База данных: файлы и хранилище](../system-functions/db-files/)

```EME-L
' Чтение данных из базы '
Name = is_data("Клиенты", 5, "Название")

' Работа с транзакциями '
is_transaction(1, "Создание заказа")
' ... операции с данными ... '
is_transaction(-1)

' Поиск строки по значению '
Line = is_equ_data("Клиенты", "Код", "C001")
```

---

## is-функции группы Debug — отладка и диагностика

is-функции группы Debug отвечают за отладку, профилирование, диагностику, экспорт/импорт данных, управление консолью вывода и вспомогательные операции разработки. в системе EME.WMS эти функции используются при разработке метапроектов, тестировании и диагностике проблем.

Доступные функции:

| Функция | Описание |
|---------|----------|
| `is_breakpoint()` | Точка останова с выводом диагностического сообщения |
| `is_error(тип, сообщение)` | Вывод ошибки указанного типа (0-4) |
| `is_catastrophic_log(сообщение)` | Запись сообщения в системный лог |
| `is_log(сообщение)` | Синоним is_catastrophic_log |
| `is_post_console_out(сообщение)` | Вывод в журнал мозгового компьютера |
| `is_profile_point(имя)` | Регистрация профилируемой точки |
| `is_profile_name(имя)` | Задание прикладного идентификатора профилирования |
| `is_eximport(каталог, файл, массив, направление)` | Экспорт/импорт массива в бинарный файл |
| `is_eximport2(каталог, файл, направление, имена, данные, шаг)` | Экспорт/импорт именованных данных v2 |
| `is_eximport3(каталог, файл, направление, имена, данные, шаг)` | Экспорт/импорт именованных данных v3 |
| `is_merge_EMEL(класс, ctor_бд, methods_бд, ctor_импорт, methods_импорт, команда)` | Слияние EME-L кода через TortoiseMerge |
| `is_debug()` | Проверка debug-сборки модуля |
| `is_debug_mode()` | Проверка включения отладочного режима EME-L |
| `is_project_list(массив)` | Заполнение массива списком проектов |
| `is_get_type(значение)` | Строковое представление типа VARIANT |
| `is_help_file(класс, html_файл)` | Сохранение справки класса в HTML |
| `is_add_string_to_output(тип, формат, ...)` | Форматированный вывод в консоль (printf-style) |
| `is_clear_output_console([тип])` | Очистка консоли вывода |
| `is_show_output_console(показать)` | Показать или скрыть консоль |
| `is_memory_leak(размер)` | Создание утечки памяти для тестирования |
| `is_mem_usagestat(флаг[, файл[, строка[, count]]])` | Статистика использования памяти (debug) |
| `is_lost_fields(отсутствующие[, типы[, дубликаты]])` | Проверка совместимости метапроекта с данными |
| `is_switch_workstation_mode()` | Переключение моно/рабстанция |
| `is_run_robot(имя[, задержка[, таймаут[, скрыть]]])` | Запуск робота для тестирования диалогов |
| `is_server_log([файл])` | Получение лога сервера БД |
| `is_vfs_test([тест[, параметр[, предыдущее]]])` | Юнит-тесты виртуальной файловой системы |
| `is_OLE_error_trace(включить)` | Включение/отключение трассировки OLE |
| `is_trace_exist(имя)` | Получение объекта трассировки по имени |
| `is_resolve_double_keys(массив)` | Устранение задвоенных ключей в БД |
| `is_object_ptr(объект)` | Числовое представление указателя на объект |

**Подробная документация** с таблицами параметров, типами и примерами кода — в разделе [Системные функции → Профилирование, отладка и прочее](../system-functions/debug/).

```EME-L
' Записать сообщение в лог '
is_catastrophic_log("Начало обработки")

' Вывести отладочное сообщение '
is_add_string_to_output("LOG", "Обработано %d записей", Count)

' Проверить совместимость метапроекта '
Missing = Object("Array")
is_lost_fields(Missing)
If (Missing.GetSize() > 0)
    is_error(1, "Обнаружены несоответствия")
End If
```
