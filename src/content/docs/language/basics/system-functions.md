---
title: "Системные функции"
sidebar:
  order: 6
---



в языке EME-L системные функции — это встроенные is-функции, реализованные на уровне ядра (C++) и доступные для вызова из EME-L кода. в системе EME.WMS системные функции обеспечивают доступ к свойствам окружения, управления интерфейсом, работе с цветами, транзакциям, филиалам и другим возможностям платформы.



## Сообщения и диалоги

### is_msg — сообщение проекта

возвращает сообщение проекта по имени.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Имя сообщения проекта |

**Возвращает**: `String` — текст сообщения проекта.

```EME-L
text = is_msg("MessageName")
```



### is_message — вывод MessageBox

выводит диалоговое окно MessageBox с кнопками и иконкой. в языке EME-L является не GUI функцией — в Linux выводит в консоль.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Заголовок окна |
| args[1] | String | Да | Текст сообщения |
| args[2] | Integer/String | Нет | Количество кнопок (по умолчанию 2). Строковые значения: "ABORTRETRYIGNORE", "OK", "OKCANCEL", "RETRYCANCEL", "YESNOCANCEL", "YESNO". 4 = "Да"/"Нет", 5 = диалог из outera |
| args[3] | Integer/String | Нет | Тип иконки: 0/"INFORMATION" — 'i', 1/"EXCLAMATION" — восклицание, 2/"QUESTION" — вопрос, 3/"STOP" — стоп |
| args[4] | Integer | Нет | Номер кнопки по умолчанию (0, 1, 2) |
| args[5] | Boolean | Нет | Системный режим — выводит MessageBox в любом потоке, в т.ч. в транзакции. По умолчанию FALSE |
| args[6] | Integer | Нет | Таймер в мс. По умолчанию 0 (бесконечность) |

**Возвращает**: при 2 кнопках — `Boolean` (OK/Yes = TRUE, Cancel/No = FALSE). При 3 кнопках — `Integer` (Yes = -1, No = 0, Cancel = 1). При текстовом args[2] — `String` ("OK", "CANCEL", "YES", "NO" и т.д.).

Если args = 0 или 1: MessageBox без иконки с одной кнопкой OK.

```EME-L
If (is_message("Заголовок", "Текст сообщения"))
    'Нажали OK'
End If

result = is_message("Вопрос", "Продолжить?", 3, 2)
' result: -1=Yes, 0=No, 1=Cancel

result = is_message("Заголовок", "Текст", "YESNO", "QUESTION")
' result: "YES" или "NO"
```



### is_message_beep — звуковой сигнал

выводит звук в динамики компьютера.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer/String | Да | Тип звука. Строка: "ICONASTERISK", "ICONEXCLAMATION", "ICONERROR", "ICONHAND", "ICONINFORMATION", "ICONQUESTION", "ICONSTOP", "OK". Число: 0-7 (по порядку) |

**Возвращает**: `Boolean` — результат выполнения.

```EME-L
is_message_beep("ICONEXCLAMATION")
is_message_beep(0)
```



### is_timer_message — MessageBox с таймером

выводит строку в TimerMessageBox. Параметры аналогичны `is_message`, но args[6] — TimeOut в миллисекундах (по умолчанию 10000).

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0..5] | — | — | См. is_message |
| args[6] | Integer | Нет | TimeOut в мс (по умолчанию 10000) |

**Возвращает**: см. is_message.

```EME-L
result = is_timer_message("Заголовок", "Автозакрытие через 5с", 2, 0, 0, FALSE, 5000)
```



### is_input — ввод текстовой строки

выводит диалог для ввода однострочного текста с кнопками "OK" и "Отмена".

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Подсказка (prompt) |
| args[1] | String | Да | Стартовое значение |
| args[2] | String | Нет | Заголовок окна (по умолчанию "EME") |

**Возвращает**: `String` — введённую строку при OK, пустую строку при Cancel.

```EME-L
result = is_input("Введите значение:", "по умолчанию", "Мой диалог")
```



### is_multiinput — многострочный ввод

аналог `is_input`, но для многострочного текста.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Подсказка (prompt) |
| args[1] | String | Да | Стартовое значение |
| args[2] | String | Нет | Заголовок окна (по умолчанию "EME") |

**Возвращает**: `String` — введённый текст при OK, пустую строку при Cancel.

```EME-L
text = is_multiinput("Введите комментарий:", "", "Комментарий")
```



### is_get_password — ввод пароля

выводит диалог для ввода пароля и возвращает введённый текст.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Заголовок диалога |
| args[1] | String | Да | Текст подсказки на диалоге |

**Возвращает**: `String` — введённый пароль.

```EME-L
pwd = is_get_password("Авторизация", "Введите пароль:")
```



### is_show_popup_window — всплывающее уведомление

показывает всплывающее окно в правой нижней части экрана.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Текст сообщения |
| args[1] | String | Да | Запускаемый диалог после нажатия |
| args[2] | String | Нет | Запускаемый диалог (дополнительный) |
| args[3] | Integer | Нет | Код запуска |

**Возвращает**: пустое значение.

```EME-L
is_show_popup_window("Новое задание", "DialogOrders", "", 0)
```



### is_show_email_icon — иконка email

отображает или скрывает иконку email.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Boolean | Да | TRUE — отобразить, FALSE — скрыть |

**Возвращает**: ничего.

```EME-L
is_show_email_icon(TRUE)
```



## Окружение и информация о системе

### is_computer_name — имя компьютера

возвращает имя компьютера.

**Параметры**: нет.

**Возвращает**: `String` — имя компьютера.

```EME-L
name = is_computer_name()
```



### is_domain_name — имя домена

возвращает наименование домена текущего пользователя ОС.

**Параметры**: нет.

**Возвращает**: `String` — имя домена. Пустая строка при ошибке.

```EME-L
domain = is_domain_name()
```



### is_domain_dc — Domain Component

возвращает наименование домена в виде DC (Domain Component).

**Параметры**: нет.

**Возвращает**: `String` — DC домена. Пустая строка при ошибке.

```EME-L
dc = is_domain_dc()
```



### is_domain_username — доменное имя пользователя

возвращает доменное имя текущего пользователя ОС.

**Параметры**: нет.

**Возвращает**: `String` — доменное имя пользователя. Пустая строка при ошибке.

```EME-L
user = is_domain_username()
```



### is_server — проверка сервера

проверяет, что код выполняется на сервере.

**Параметры**: нет.

**Возвращает**: `Boolean` — TRUE, если выполнение на сервере.

```EME-L
If (is_server())
    'Код выполняется на сервере'
End If
```



### is_http_server — проверка HTTP-сервера

проверяет, что код выполняется на HTTP-сервере.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Boolean | Нет | Сервер транзакций (по умолчанию FALSE). При TRUE проверяет транзакционный порт HTTP-сервера |

**Возвращает**: `Boolean` — TRUE, если выполнение на HTTP-сервере.

```EME-L
If (is_http_server())
    'Код на HTTP-сервере'
End If

If (is_http_server(TRUE))
    'Код на транзакционном порту HTTP-сервера'
End If
```



### is_brain_computer — мозговой компьютер

проверяет, что код выполняется на мозговом компьютере.

**Параметры**: нет.

**Возвращает**: `Boolean` — TRUE, если выполнение на мозговом компьютере.

```EME-L
If (is_brain_computer())
    'Мозговой компьютер'
End If
```



### is_workstation_mode — режим рабочей станции

возвращает режим рабочей станции как строку текста.

**Параметры**: нет.

**Возвращает**: `String` — "SERVER", "MONO", "WORKSTATION", "CANAL_BRAIN", "TERMINAL", "TERM_BRAIN".

```EME-L
mode = is_workstation_mode()
```



### is_workstation_type — тип рабочей станции

возвращает тип рабочей станции как строку текста.

**Параметры**: нет.

**Возвращает**: `String` — "UNDEFINED_WS", "FULLVM_WS", "NETVM_WS".

```EME-L
wtype = is_workstation_type()
```



### is_system_family — семейство ОС

возвращает семейство операционной системы.

**Параметры**: нет.

**Возвращает**: `String` — "LINUX" или "WINDOWS".

```EME-L
If (is_system_family() = "LINUX")
    'Выполнение на Linux'
End If
```



### is_db_driver_name — имя DLL драйвера

возвращает имя библиотеки .DLL, в которой реализованы специфичные для проекта классы.

**Параметры**: нет.

**Возвращает**: `String` — имя DLL-библиотеки драйвера.

```EME-L
dll = is_db_driver_name()
```



### is_get_tick_count — счётчик миллисекунд

возвращает количество миллисекунд с момента старта системы.

**Параметры**: нет.

**Возвращает**: `Integer` — миллисекунды с момента старта.

```EME-L
tick = is_get_tick_count()
```



### is_keycode — нажатые клавиши

возвращает нажатые клавиши в виде строки.

**Параметры**: нет.

**Возвращает**: `String` — комбинация клавиш (например "Ctrl+Alt+Shift+Z").

```EME-L
keys = is_keycode()
```



### is_key_press — проверка нажатия клавиши

проверяет, нажата ли клавиша с указанным кодом.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Код клавиши (HEX). Коды см. в WinUser.h "Virtual Keys" |

**Возвращает**: `Boolean` — TRUE, если клавиша нажата.

```EME-L
If (is_key_press(0x41))
    'Нажата клавиша A'
End If
```



## Управление выполнением

### is_beep — звуковой сигнал

подает звуковой сигнал заданной частоты и длительности.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Частота, Гц |
| args[1] | Integer | Да | Продолжительность, мс |

**Возвращает**: ничего.

```EME-L
is_beep(1000, 500)
```



### is_sleep — приостановка потока

приостанавливает текущий поток на указанное время.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Период приостановки, мс |

**Возвращает**: ничего.

```EME-L
is_sleep(1000)  'пауза 1 секунда
```



### is_exit — завершение программы

завершает работу программы.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Нет | Номер сообщения из db.rus |
| args[1] | Integer | Нет | Номер сообщения из db.rus |
| args[2] | String | Нет | Произвольный текст |

**Возвращает**: ничего.

```EME-L
is_exit()
is_exit(7, 366, "Ошибка!")
```



### is_do_wait_cursor — песочные часы

управляет курсором "песочные часы".

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Нет | 1 — включить, -1 — выключить, 0 — сбросить |

**Возвращает**: `Integer` — 0.

```EME-L
is_do_wait_cursor(1)   'включить
is_do_wait_cursor(-1)  'выключить
```



### is_main_thread — проверка главного потока

проверяет, является ли текущий поток главным.

**Параметры**: нет.

**Возвращает**: `Boolean` — TRUE, если текущий поток — главный.

```EME-L
If (is_main_thread())
    'Главный поток'
End If
```



### is_managed_thread — управляемый поток

проверяет или настраивает управление потоком. Управляемый поток должен регулярно сообщать о выполнении.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Нет | Интервал проверки в мс. 0 — отключает. Без параметра — возвращает статус |

**Возвращает**: без параметра — `Boolean` (управляемый ли поток). С параметром — пустое значение.

```EME-L
If (is_managed_thread())
    'Поток управляемый'
End If
is_managed_thread(60000)  'интервал 60 секунд
```



### is_DelayedRefNuller — отложенное обнуление ссылок

управляет отложенным обнулением ссылок.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Boolean | Да | TRUE — включить, FALSE — выключить |

**Возвращает**: пустое значение.

```EME-L
is_DelayedRefNuller(FALSE)
```



## Системные настройки

### is_system — системная настройка

возвращает значение системной настройки по индексу.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Индекс системной настройки |

Индексы:
- 0 — Ссылка на текущего оператора
- 1 — Флаги доступа текущего оператора
- 2 — Идентификатор (имя) текущего оператора
- 3 — Код запуска модуля
- 4 — Код текущего филиала
- 5 — Ссылка на текущий филиал
- 6 — Код проекта
- 7 — is_workstation (TRUE/FALSE)
- 8 — Мозговой компьютер (TRUE/FALSE)

**Возвращает**: значение настройки (тип зависит от индекса).

```EME-L
ref = is_system(0)      'ссылка на оператора
flags = is_system(1)    'флаги доступа
name = is_system(2)     'имя оператора
code = is_system(3)     'код запуска
fcode = is_system(4)    'код филиала
```



### is_set_system — установка кода запуска

устанавливает код запуска модуля. Доступно только в версиях ядра до 52.0.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Код запуска |

**Возвращает**: пустое значение.

```EME-L
is_set_system(10)
```



### is_sys_param — параметр настройки

возвращает значение настройки по имени. Настройки рабочей станции начинаются с "W://", настройки модулей — с "M://".

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Имя настройки (W://Имя или M://Имя) |
| args[1] | Any | Нет | Тип (определяется автоматически: строка, целое, вещественное). По умолчанию — строка |
| args[2] | Integer | Нет | Режим: 0 (по умолчанию) — EXCEPT_MODE, 1 — SOFT_MODE |

**Возвращает**: значение настройки в соответствии с типом args[1].

```EME-L
val = is_sys_param("M://MySetting", "")
val = is_sys_param("W://WsSetting", 0)
val = is_sys_param("M://SoftSetting", "", 1)
```



### is_add_sys_param — добавление настройки

добавляет или обновляет параметр настройки.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Имя настройки (W:// или M://) |
| args[1] | String | Да | Значение |
| args[2] | String | Нет | Текст справки |
| args[3] | String | Нет | Список значений (разделитель \r\n) |
| args[4] | Boolean | Нет | Режим обновления настроек (по умолчанию TRUE) |

**Возвращает**: пустое значение.

```EME-L
is_add_sys_param("M://NewSetting", "value", "Описание", "val1\r\nval2")
```



### is_check_text_key — проверка ключа командной строки

проверяет прикладной ключ командной строки. Ключи начинаются с префикса "/d_".

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Наименование ключа (без "/d_"). Например "EMEL" для "/d_emel" |

**Возвращает**: `Boolean` — TRUE, если ключ установлен.

```EME-L
If (is_check_text_key("EMEL"))
    'Ключ /d_emel установлен'
End If
```



## Права доступа и пользователи

### is_administrator — проверка администратора

проверяет, является ли текущий оператор администратором.

**Параметры**: нет.

**Возвращает**: `Boolean` — TRUE, если оператор — администратор.

```EME-L
If (is_administrator())
    'Администратор'
End If
```



### is_access — проверка прав доступа

проверяет наличие прав доступа у текущего оператора.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Номер бита в 32-битном поле доступа |

**Возвращает**: `Boolean` — TRUE, если право есть.

```EME-L
If (is_access(5))
    'Право доступа 5 есть'
End If
```



### is_password — диалог ввода пароля

запускает диалог идентификации оператора по паролю.

**Параметры**: нет.

**Возвращает**: `Boolean` — TRUE при успешной идентификации.

```EME-L
If (is_password())
    'Пароль верный'
End If
```



### is_change_password — смена пароля

запускает диалог смены пароля.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Нет | Номер строки пользователя (по умолчанию NULL_REF — текущий) |
| args[1] | Boolean | Нет | Требовать ли старый пароль (по умолчанию TRUE) |

**Возвращает**: ничего.

```EME-L
is_change_password()
is_change_password(user_ref, TRUE)
```



### is_set_user_read_only — режим только чтение

устанавливает флаг "только чтение" в памяти у текущего пользователя (бит 1 в поле "32 бита допусков"). В БД не меняет. Запись в БД запрещена.

**Параметры**: нет.

**Возвращает**: ничего.

```EME-L
is_set_user_read_only()
```



### is_clr_user_read_only — снять режим только чтение

снимает флаг "только чтение" в памяти у текущего пользователя. В БД не меняет. Запись в БД разрешена.

**Параметры**: нет.

**Возвращает**: ничего.

```EME-L
is_clr_user_read_only()
```



### is_disconnect_user — отключение пользователя

отправляет на все рабочие станции команду отключить пользователя.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Ссылка на пользователя в записи "Список пользователей" |

**Возвращает**: пустое значение.

```EME-L
is_disconnect_user(user_ref)
```



### is_impersonate_user — установка пользователя

позволяет в неинтерфейсном потоке задать текущего пользователя. Используется для ЦУП.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Ссылка на пользователя |

**Возвращает**: пустое значение.

```EME-L
is_impersonate_user(admin_ref)
```



### is_security_event — событие безопасности

генерирует произвольное событие информационной безопасности. Обрабатывается в методе OnSecurityEvent класса Resident.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Ссылка на пользователя |
| args[1] | Integer | Да | Код события |
| args[2] | String | Да | Описание события |

**Возвращает**: ничего.

```EME-L
is_security_event(user_ref, 1001, "Попытка несанкционированного доступа")
```



## Филиалы и разделы

### is_center — центральный филиал

проверяет, является ли текущий филиал ЦЕНТРОМ.

**Параметры**: нет.

**Возвращает**: `Boolean` — TRUE, если текущий филиал — центр.

```EME-L
If (is_center())
    'Центральный филиал'
End If
```



### is_root_center — главный центр

проверяет, является ли текущий филиал ГЛАВНЫМ ЦЕНТРОМ в многоуровневой иерархии.

**Параметры**: нет.

**Возвращает**: `Boolean` — TRUE, если текущий филиал — главный центр.

```EME-L
If (is_root_center())
    'Главный центр'
End If
```



### is_node_center — локальный центр

проверяет, является ли текущий филиал ЛОКАЛЬНЫМ ЦЕНТРОМ в многоуровневой иерархии.

**Параметры**: нет.

**Возвращает**: `Boolean` — TRUE, если текущий филиал — локальный центр.

```EME-L
If (is_node_center())
    'Локальный центр'
End If
```



### is_filial — филиал (не центр)

проверяет, что текущий филиал — НЕ ЦЕНТР в многоуровневой иерархии.

**Параметры**: нет.

**Возвращает**: `Boolean` — TRUE, если текущий филиал — не центр.

```EME-L
If (is_filial())
    'Обычный филиал'
End If
```



### is_system_filial — системный филиал

проверяет, является ли текущий филиал СИСТЕМНЫМ.

**Параметры**: нет.

**Возвращает**: `Boolean` — TRUE, если текущий филиал — системный.

```EME-L
If (is_system_filial())
    'Системный филиал'
End If
```



### is_parent_filial_ref — ссылка на родительский филиал

возвращает ссылку на филиал, родительский для текущего.

**Параметры**: нет.

**Возвращает**: `Integer` — ссылка на родительский филиал.

```EME-L
pref = is_parent_filial_ref()
```



### is_parent_filial_code — код родительского филиала

возвращает код филиала, родительского для текущего.

**Параметры**: нет.

**Возвращает**: `Integer` — код родительского филиала.

```EME-L
pcode = is_parent_filial_code()
```



### is_filial_no — номер текущего филиала

возвращает номер текущего филиала БД.

**Параметры**: нет.

**Возвращает**: `Integer` — номер филиала.

```EME-L
fno = is_filial_no()
```



### is_filial_ref — ссылка на текущий филиал

возвращает ссылку на текущий филиал в записи "Филиалы".

**Параметры**: нет.

**Возвращает**: `Integer` — ссылка на филиал.

```EME-L
fref = is_filial_ref()
```



### is_division_no — номер текущего раздела

возвращает номер текущего раздела БД.

**Параметры**: нет.

**Возвращает**: `Integer` — номер раздела.

```EME-L
dno = is_division_no()
```



### is_division_ref — ссылка на текущий раздел

возвращает ссылку на текущий раздел в записи "Разделы БД".

**Параметры**: нет.

**Возвращает**: `Integer` — ссылка на раздел.

```EME-L
dref = is_division_ref()
```



### is_change_division_no — изменение номера раздела

изменяет номер текущего раздела БД. Можно вызывать только в монопольном режиме или режиме сервера.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Новый номер раздела БД |

**Возвращает**: ничего. При неудаче бросает исключение.

```EME-L
is_change_division_no(5)
```



## Цвета

### is_RGB — цвет из RGB

генерирует цвет из трёх значений RGB.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Красный (0-255) |
| args[1] | Integer | Да | Зелёный (0-255) |
| args[2] | Integer | Да | Синий (0-255) |

**Возвращает**: `Integer` — цвет (COLORREF).

```EME-L
color = is_RGB(255, 128, 0)
```



### is_RGBA — цвет из RGBA

возвращает RGBA-цвет. Поддерживает два режима вызова.

**Режим 1** — отдельные каналы:

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Красный (0-255) |
| args[1] | Integer | Да | Зелёный (0-255) |
| args[2] | Integer | Да | Синий (0-255) |
| args[3] | Integer | Нет | Прозрачность (по умолчанию 0xFF — непрозрачный) |

**Режим 2** — из RGB-цвета:

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | RGB-цвет |
| args[1] | Integer | Нет | Прозрачность (по умолчанию 0x00 — прозрачный) |

**Возвращает**: `Integer` — RGBA-цвет.

```EME-L
color = is_RGBA(255, 0, 0, 128)   'полупрозрачный красный
color = is_RGBA(rgb_color, 200)    'из RGB с прозрачностью
```



### is_blendRGB — смешивание цветов

смешивает два цвета с заданным коэффициентом.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Первый цвет |
| args[1] | Integer | Да | Второй цвет |
| args[2] | Real | Нет | Коэффициент смешивания (0.0-1.0, по умолчанию 0.5) |

**Возвращает**: `Integer` — результирующий цвет (RGB).

```EME-L
mixed = is_blendRGB(color1, color2, 0.7)
mixed = is_blendRGB(color1, color2)  '50/50
```



### is_sys_color — системный цвет Windows

возвращает текущий цвет отображаемого элемента Windows.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Название элемента: "3DDKSHADOW", "3DFACE", "BTNFACE", "ACTIVEBORDER", "ACTIVECAPTION", "HIGHLIGHT", "WINDOW", "WINDOWTEXT" и др. |

**Возвращает**: `Integer` — цвет (COLORREF).

```EME-L
color = is_sys_color("WINDOW")
```



### is_legend_color — цвет из палитры легенды

возвращает цвет из встроенной палитры по индексу. Палитра содержит 22 цвета (от красного до тёмного хаки). При выходе за диапазон возвращает серый (192,192,192).

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Номер элемента палитры (0-21) |

**Возвращает**: `Integer` — цвет (COLORREF).

```EME-L
color = is_legend_color(0)  'красный
color = is_legend_color(7)  'салатовый
```



## Язык и локализация

### is_set_language — установка языка

устанавливает код языка для текущего потока.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Нет | Код языка. Если не задан — устанавливает язык по умолчанию для интерфейса |

**Возвращает**: `Integer` — 1 при успехе, 0 если недоступно.

```EME-L
is_set_language("EN")
is_set_language()  'язык по умолчанию
```



### is_get_language — текущий язык

возвращает код языка, установленный для текущего потока.

**Параметры**: нет.

**Возвращает**: `String` — код языка.

```EME-L
lang = is_get_language()
```



### is_set_translate_regim — режим перевода

устанавливает режим перевода для текущего контекста.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Режим перевода (TRANSLATE_REGIM) |

**Возвращает**: `Integer` — 0.

```EME-L
is_set_translate_regim(1)
```



### is_get_translate_regim — текущий режим перевода

возвращает текущий режим перевода.

**Параметры**: нет.

**Возвращает**: `Integer` — режим перевода или -1 если не поддерживается.

```EME-L
regim = is_get_translate_regim()
```



### is_ML — многоязыковая строка

возвращает локализованную строку.

**Параметры**: нет (определяется контекстом).

**Возвращает**: пустое значение если локализация недоступна.



### tr — перевод строки

переводит строку на текущий язык. Если перевод не найден — возвращает исходную строку.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Исходная строка для перевода |
| args[1] | String | Нет | Контекст перевода |

**Возвращает**: `String` — переведённая строка.

```EME-L
translated = tr("Hello")
translated = tr("Hello", "context")
```



### is_transliterate — транслитерация

заменяет все русские буквы в строке на подходящие по произношению английские. Длина строки может увеличиться.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Исходная строка |

**Возвращает**: `String` — транслитерированная строка.

```EME-L
result = is_transliterate("Привет")  'Privet
```



### is_decimal_separator — десятичный разделитель

возвращает текущий десятичный разделитель.

**Параметры**: нет.

**Возвращает**: `String` — десятичный разделитель.

```EME-L
sep = is_decimal_separator()
```



### is_locale_info — информация о локали

возвращает информацию о локальных установках (аналог WinAPI GetLocaleInfo).

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Идентификатор: "LOCALE_SYSTEM_DEFAULT" или "LOCALE_USER_DEFAULT" |
| args[1] | String | Да | Тип: "LOCALE_SDECIMAL", "LOCALE_SLIST" и др. |

**Возвращает**: `String` — значение локальной установки.

```EME-L
sep = is_locale_info("LOCALE_USER_DEFAULT", "LOCALE_SDECIMAL")
```



## Хеш и криптография

### is_qhash — хеш строки

вычисляет хеш-сумму строки с использованием функции qHash (Qt). Перекодирует строку в юникод для воспроизводимости результата на разных платформах.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Строка для хеширования |
| args[1] | String | Нет | Кодировка (по умолчанию "Windows-1251") |

**Возвращает**: `Integer` — хеш-сумма.

```EME-L
h = is_qhash("test string")
h = is_qhash("test string", "UTF-8")
```



## Реестр Windows

### is_reg_query_value — чтение из реестра

читает значение из реестра Windows по указанному пути. Поддерживает типы REG_SZ и REG_DWORD.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Имя предопределённого ключа: "HKCR", "HKLM", "HKCU", "HKU", "HKCC", "HKDD", "HKPD" или полное имя |
| args[1] | String | Да | Имя подключа |
| args[2] | String | Да | Имя настройки |
| args[3] | Any | Нет | Значение по умолчанию. Если отсутствует — бросает исключение при ошибке |

**Возвращает**: значение из реестра (строка или целое число).

```EME-L
val = is_reg_query_value("HKLM", "SOFTWARE\\MyApp", "Setting", "default")
```



## Сообщения между компонентами

### is_send_message — отправка сообщения

посылает сообщение в указанный орган (элемент управления диалога).

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String/Integer | Да | Имя органа (строка), 0 — все органы, 1 — в ГФД |
| args[1] | Integer | Да | Номер сообщения |
| args[2] | Integer | Нет | Положение диалога в иерархии |
| args[3] | Integer | Нет | Запись (параметр метафункций) — только для ГФД |
| args[4] | String | Нет | Строка (параметр метафункций) — только для ГФД |

**Возвращает**: пустое значение.

```EME-L
is_send_message("Button1", 100)
is_send_message(0, 200)  'все органы
```



### is_message_to_workstations — сообщение на рабочие станции

отправляет всплывающее сообщение на рабочие станции.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Ссылка на оператора |
| args[1] | String | Да | Текст сообщения |
| args[2] | String | Нет | Имя диалога |
| args[3] | Integer | Нет | Номер строки диалога |
| args[4] | Integer | Нет | Код запуска |

**Возвращает**: пустое значение.

```EME-L
is_message_to_workstations(user_ref, "Внимание! Система перезагружается")
```



## История операций

### is_add_history_line — запись в историю

записывает строку в историю операций (запись "История операций").

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Код тома |
| args[1] | String | Да | Текст для истории операции |
| args[2] | Integer | Нет | Номер записи |
| args[3] | Integer | Нет | Номер строки записи |

**Возвращает**: пустое значение.

```EME-L
is_add_history_line(1, "Операция выполнена")
is_add_history_line(1, "Операция с записью", rec_no, line_no)
```



## Транзакции сервера

### is_server_transaction_dump — дамп транзакций

на сервере делает дамп транзакций в объект запроса. На рабочей станции подготавливает запрос.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Object(Query) | Да | Пустой запрос для приёма дампа |
| args[1] | Any | Нет | Имя/номер записи (NULL_RECORD — все) |
| args[2] | Any | Нет | Имя/номер поля (NULL_FIELD — все) |
| args[3] | Any | Нет | Номер строки (NULL_REF — все) |
| args[4] | Integer | Нет | Номер транзакции с (0 — все) |
| args[5] | Integer | Нет | Номер транзакции по |
| args[6] | String | Нет | Данные для поиска |
| args[7] | DateTime | Нет | Дата начала |
| args[8] | DateTime | Нет | Дата окончания |
| args[9] | DateTime | Нет | Время начала |
| args[10] | DateTime | Нет | Время окончания |
| args[11] | Boolean | Нет | Не показывать служебную информацию |
| args[12] | Boolean | Нет | История поиска транзакций |

**Возвращает**: `Boolean` — TRUE при успехе.

```EME-L
q = Object("Query")
result = is_server_transaction_dump(q, NULL_RECORD, NULL_FIELD, NULL_REF)
```



### is_server_transaction_data — данные транзакции

на сервере возвращает данные одной команды транзакции. На рабочей станции не делает ничего.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Номер транзакции |
| args[1] | Integer | Да | Номер команды |
| args[2] | Integer | Нет | Номер строки записи |

**Возвращает**: `String` — данные команды или пустое значение.

```EME-L
data = is_server_transaction_data(trans_no, cmd_no)
```



### is_server_first_transaction — границы транзакций

на сервере возвращает номера первой и последней транзакции в хранилище.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer (ByRef) | Да | Ссылка для номера первой транзакции |
| args[1] | Integer (ByRef) | Да | Ссылка для номера последней транзакции |
| args[2] | DateTime (ByRef) | Нет | Ссылка для даты первой транзакции |
| args[3] | DateTime (ByRef) | Нет | Ссылка для времени первой транзакции |

**Возвращает**: `Boolean` — TRUE если транзакции есть.

```EME-L
first = 0
last = 0
If (is_server_first_transaction(first, last))
    'Транзакции есть'
End If
```



### is_server_emel — вызов серверного EME-L

вызывает на сервере метод Resident::OnServerEmel и передаёт параметры.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Номер команды |
| args[1] | Object(Parameters) | Да | Объект класса "Parameters" |

**Возвращает**: `String` — результат метода OnServerEmel.

```EME-L
Params = Object("Parameters")
Params.Put("key", "value")
result = is_server_emel(1, Params)
```



### is_transaction_no — номер транзакции

возвращает текущее значение номера транзакции.

**Параметры**: нет.

**Возвращает**: `Integer` — номер транзакции.

```EME-L
tno = is_transaction_no()
```



## RPC

### is_process_rpc_over_file_system — RPC через ФС

обрабатывает RPC поверх файловой системы.

**Параметры**: нет.

**Возвращает**: результат выполнения RPC.

```EME-L
is_process_rpc_over_file_system()
```



### is_last_rpc_date — дата последнего RPC

возвращает дату последнего RPC.

**Параметры**: нет.

**Возвращает**: `DateTime` — дата.

```EME-L
d = is_last_rpc_date()
```



### is_last_rpc_time — время последнего RPC

возвращает время последнего RPC.

**Параметры**: нет.

**Возвращает**: `DateTime` — время.

```EME-L
t = is_last_rpc_time()
```



### is_last_rpc_number — номер последнего RPC

возвращает номер последнего RPC.

**Параметры**: нет.

**Возвращает**: `Integer` — номер RPC.

```EME-L
n = is_last_rpc_number()
```



### is_reset_last_rpc_info — сброс информации RPC

сбрасывает информацию о последнем RPC.

**Параметры**: нет.

**Возвращает**: пустое значение.

```EME-L
is_reset_last_rpc_info()
```



## Внешние программы

### is_shell_execute — запуск внешней программы

аналог ShellExecute в Windows. Запускает внешнюю программу или открывает документ.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Действие: "open", "explore", "print" и др. |
| args[1] | String | Да | Рабочий каталог |
| args[2] | String | Да | Имя приложения, документа или файла |
| args[3] | String | Нет | Параметры запуска |
| args[4] | Integer | Нет | Таймаут ожидания в мс (0 или без — не ждать) |

**Возвращает**: `Integer` — код результата. При ожидании: код выхода процесса. При ошибках: -1 (ShellExecuteEx), -2 (WaitFailed), -3 (Timeout), -4 (другое).

```EME-L
is_shell_execute("open", "C:\\", "notepad.exe")
is_shell_execute("open", "", "document.pdf", "", 10000)
```



### is_shell_execute_link — запуск по ярлыку

запускает программу при помощи ярлыка (.lnk). Не дожидается старта.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Имя файла ярлыка (например, "d:\\EME\\mono.lnk") |

**Возвращает**: `Boolean` — TRUE при успехе.

```EME-L
is_shell_execute_link("d:\\EME\\app.lnk")
```



## Версии

### is_check_version — проверка версии ядра

проверяет, что текущая версия ядра и метапроекта больше или равна указанной.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Нет | Ядро — major |
| args[1] | Integer | Нет | Ядро — minor |
| args[2] | Integer | Нет | Метапроект — major |
| args[3] | Integer | Нет | Метапроект — minor |

**Возвращает**: `Boolean` — TRUE если текущая версия >= указанной.

```EME-L
If (is_check_version(30, 5))
    'Версия ядра >= 30.5'
End If

If (is_check_version(30, 5, 20, 0))
    'Ядро >= 30.5 и метапроект >= 20.0'
End If
```



### is_type_version — тип версии (лицензия)

возвращает тип лицензии программы.

**Параметры**: нет.

**Возвращает**: `Integer` — 0 (без ключа), 1 (полная версия), 2 (стандартная).

```EME-L
vtype = is_type_version()
```



### is_license_param — параметр лицензии

возвращает значение параметра лицензии по имени.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Имя параметра лицензии |

**Возвращает**: `String` — значение параметра или `Empty` если не найден.

```EME-L
val = is_license_param("MaxUsers")
```



## Буфер обмена

### is_get_clipboard_data_type — тип данных буфера

возвращает тип данных в буфере обмена.

**Параметры**: нет.

**Возвращает**: `Integer` — -1 (пусто/неизвестно), 2 (CF_BITMAP), 15 (CF_HDROP).

```EME-L
ctype = is_get_clipboard_data_type()
```



### is_get_clipboard_as_bitmap — снимок экрана из буфера

получает содержимое буфера обмена как bitmap. Поддерживается только снимок экрана.

**Параметры**: нет.

**Возвращает**: `Empty` если буфер пуст или данные неподдерживаемы. Образ BMP-файла если в буфере снимок экрана.

```EME-L
bmp = is_get_clipboard_as_bitmap()
```



### is_put_clipboard_data_as_text — текст в буфер обмена

записывает текстовую строку в буфер обмена.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Строка для записи |

**Возвращает**: `Boolean` — TRUE при успехе.

```EME-L
is_put_clipboard_data_as_text("Скопированный текст")
```



### is_put_clipboard_data_as_filelist — файл в буфер обмена

записывает ссылку на файл в буфер обмена.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Путь к файлу |
| args[1] | Boolean | Нет | TRUE — копировать (по умолчанию), FALSE — вырезать |

**Возвращает**: `Boolean` — TRUE при успехе.

```EME-L
is_put_clipboard_data_as_filelist("C:\\file.txt", TRUE)
```



## Структура БД

### is_export_db_structure — экспорт структуры БД

экспортирует структуру БД в файл.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Имя файла |

**Возвращает**: `Integer` — 0 при ошибке/пустом имени, 1 при успехе.

```EME-L
result = is_export_db_structure("structure.dbh")
```



### is_import_db_structure — импорт структуры БД

импортирует структуру БД из файла.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Имя файла |
| args[1] | Integer (ByRef) | Нет | Ссылка для возврата версии файла структуры |

**Возвращает**: `Boolean` — TRUE при успехе.

```EME-L
If (is_import_db_structure("structure.dbh"))
    'Импорт успешен'
End If
```



## Профилирование и отладка

### is_prf — профилирование

функция для профилирования кода. Аналог ядерных функций профилирования.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Тип: 0 — reset_prf, 1 — start_prf, 2 — stop_prf, 3 — get_prf |
| args[1] | Integer | Нет | Номер таймера (0-31). Для args[0]=0 не нужен |

**Возвращает**: при args[0]=3 — время в мс. В остальных случаях — ничего.

```EME-L
is_prf(1, 0)   'запуск таймера 0
is_prf(2, 0)   'остановка
time = is_prf(3, 0)  'получить время
```



### is_mem_dump — дамп памяти

записывает в катастроф-лог информацию об использовании памяти.

**Параметры**: нет.

**Возвращает**: ничего.

```EME-L
is_mem_dump()
```



### is_SYS_mem_dump — дамп пула SYS

выполняет дамп пула SYS.

**Параметры**: нет.

**Возвращает**: пустое значение.

```EME-L
is_SYS_mem_dump()
```



### is_ui_test_mode — режим тестирования UI

возвращает TRUE, если программа в режиме тестирования интерфейса. В этом режиме база последовательно запускает все пункты меню. Прикладной код может использовать для отключения браузеров и MessageBox.

**Параметры**: нет.

**Возвращает**: `Boolean` — TRUE если включён режим тестирования.

```EME-L
If (is_ui_test_mode())
    'Не показываем MessageBox'
End If
```



## Планировщик и события

### is_eventer_tick — запуск планировщика

запускает процедуры планировщика.

**Параметры**: нет.

**Возвращает**: `Integer` — результат отправки WM_TIMER, 0 при ошибке.

```EME-L
is_eventer_tick()
```



### is_force_standard_export — стандартный экспорт

делает запрос на экспорт стандартным способом.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Boolean | Да | Выдавать предупреждение |

**Возвращает**: `Integer` — результат.

```EME-L
is_force_standard_export(TRUE)
```



## Прочее

### is_generate_NSI_script — генерация инсталляционного скрипта

генерирует инсталляционный скрипт.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Нет | Postfix |
| args[1] | String | Нет | CurrentDir |
| args[2] | String | Нет | DatFolder |
| args[3] | String | Нет | MetaFolder |
| args[4] | String | Нет | KernelFolder |
| args[5] | String | Нет | OutFile |
| args[6] | String | Нет | LicenseFile |

**Возвращает**: ничего.

```EME-L
is_generate_NSI_script()
```



### is_OLE_serverbusydelay — задержка Server Busy

устанавливает задержку появления диалога OLE Server Busy.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Нет | Задержка в мс (по умолчанию 5000) |

**Возвращает**: `Boolean` — TRUE при успехе, FALSE если фильтр недоступен.

```EME-L
is_OLE_serverbusydelay(10000)
```



### is_set_method_find_mode — режим поиска методов

устанавливает режим поиска методов объектов для текущего контекста. Решает проблему привязки номеров методов к переменной при смене объекта.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | 0 — стандартный режим, 1 — FindMethod перед каждым CallMethod |

**Возвращает**: ничего.

```EME-L
is_set_method_find_mode(1)
```



### is_get_data_path — путь к папке данных

возвращает путь к папке данных проекта.

**Параметры**: нет.

**Возвращает**: `String` — путь к папке данных.

```EME-L
path = is_get_data_path()
```



### robot — ссылка на робота

возвращает ссылку на текущего робота.

**Параметры**: нет.

**Возвращает**: ссылку на объект робота (g_robot).

```EME-L
r = robot()
```



### is_enable_dataslice — включение срезов

включает режим рабочей станции для работы со срезами данных.

**Параметры**: нет.

**Возвращает**: `Boolean` — TRUE при успехе, FALSE если условия не выполнены.

```EME-L
If (is_enable_dataslice())
    'Срезы включены'
End If
```



### is_restart_as_server_request — запрос на переключение сервера

отправляет на резервный сервер запрос на переключение в режим сервера БД. IP адрес берёт из настройки рабочей станции.

**Параметры**: нет.

**Возвращает**: `String` — текст ошибки или пусто при успехе.

```EME-L
err = is_restart_as_server_request()
```



### is_sys_import — системный импорт

выполняет "ручной" системный импорт по файлам из указанной папки.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Путь к папке с файлами импорта |

**Возвращает**: `Integer` — результат выполнения (код EEXPORTRESULT).

```EME-L
result = is_sys_import("C:\\import\\")
```



См. также: [EME_L_Syntax.md](EME_L_Syntax.md) — синтаксис EME-L, [EME_L_IS_Functions.md](EME_L_IS_Functions.md) — is-функции групп Bit/Browser/Context, [EME_L_Classes.md](EME_L_Classes.md) — объектная модель.
