---
title: "Системные функции"
sidebar:
  order: 6
---



в языке EME-L системные функции — это встроенные is-функции, реализованные на уровне ядра (C++) и доступные для вызова из EME-L кода. в системе EME.WMS системные функции обеспечивают доступ к свойствам окружения, управления интерфейсом, работе с цветами, транзакциям, филиалам и другим возможностям платформы.



## Сообщения и диалоги

### is_msg — текст сообщения проекта

Возвращает текст сообщения проекта по его имени из словаря сообщений. Выполняет поиск через GetProjectMessage.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Имя сообщения проекта для поиска в словаре |

**Возвращает**: `String` — текст найденного сообщения проекта. Если сообщение не найдено, возвращает пустую строку.

```EME-L
text = is_msg("MessageName")
```



### is_message — вывод MessageBox

Выводит модальное диалоговое окно MessageBox с заданным заголовком, текстом, набором кнопок, иконкой и опциональным таймаутом. Поддерживает как числовые, так и строковые значения для кнопок и иконок. В системном режиме (systemMode = TRUE) диалог выводится в любом потоке, включая транзакции (в этом случае таймаут должен быть > 0).

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Нет | Заголовок окна. При отсутствии — пустой заголовок |
| args[1] | String | Нет | Текст сообщения. При отсутствии — пустое сообщение |
| args[2] | Integer/String | Нет | Набор кнопок (по умолчанию 2 / OKCANCEL). Числа: 1 = OK, 2 = OKCANCEL, 3 = YESNOCANCEL, 4 = YESNO, 5 = YES_NO_YESTOALL_NOTOALL_CANCEL. Строки: "OK", "OKCANCEL", "YESNOCANCEL", "YESNO", "ABORTRETRYIGNORE", "RETRYCANCEL" |
| args[3] | Integer/String | Нет | Тип иконки (по умолчанию INFORMATION). Числа: 0 = INFORMATION, 1 = EXCLAMATION, 2 = QUESTION, 3 = STOP. Строки: "INFORMATION", "EXCLAMATION", "QUESTION", "STOP" |
| args[4] | Integer | Нет | Номер кнопки по умолчанию (по умолчанию 0): 0 = первая кнопка, 1 = вторая кнопка, 2 = третья кнопка |
| args[5] | Boolean | Нет | Системный режим (по умолчанию FALSE). Если TRUE, диалог выводится в любом потоке и в транзакции |
| args[6] | Integer | Нет | Таймаут в миллисекундах (по умолчанию 0 — бесконечность). При истечении таймаута возвращается значение кнопки по умолчанию |

**Возвращает**: при текстовом args[2] — `String` с именем нажатой кнопки ("OK", "CANCEL", "ABORT", "RETRY", "IGNORE", "YES", "NO", "CLOSE", "HELP"). При YESNOCANCEL (3) и числовом args[2] — `Integer` (Yes = -1, No = 0, Cancel = 1). При YES_NO_YESTOALL_NOTOALL_CANCEL (5) — `String` ("YES", "NO", "YESTOALL", "NOTOALL", "CANCEL"). В остальных случаях — `Boolean` (OK/Yes = TRUE, Cancel/No = FALSE).

```EME-L
If (is_message("Заголовок", "Текст сообщения"))
    'Нажали OK'
End If

result = is_message("Вопрос", "Продолжить?", 3, 2)
' result: -1=Yes, 0=No, 1=Cancel

result = is_message("Заголовок", "Текст", "YESNO", "QUESTION")
' result: "YES" или "NO"
```



### is_message_beep — звуковой сигнал

Воспроизводит системный звуковой сигнал через API MessageBeep. Поддерживает как числовые, так и строковые идентификаторы типа звука.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer/String | Да | Тип звукового сигнала. Числа: 0 = ICONASTERISK, 1 = ICONEXCLAMATION, 2 = ICONERROR, 3 = ICONHAND, 4 = ICONINFORMATION, 5 = ICONQUESTION, 6 = ICONSTOP, 7 = OK. Строки: "ICONASTERISK", "ICONEXCLAMATION", "ICONERROR", "ICONHAND", "ICONINFORMATION", "ICONQUESTION", "ICONSTOP", "OK" |

**Возвращает**: `Integer` — результат вызова MessageBeep (ненулевое при успехе).

```EME-L
is_message_beep("ICONEXCLAMATION")
is_message_beep(0)
```



### is_timer_message — MessageBox с автозакрытием

Выводит модальное диалоговое окно MessageBox с заданным заголовком, текстом, набором кнопок, иконкой и автоматическим закрытием по таймауту. Поддерживает как числовые, так и строковые значения для кнопок и иконок. В системном режиме (systemMode = TRUE) диалог выводится в любом потоке, включая транзакции (в этом случае таймаут должен быть > 0). При истечении таймаута окно закрывается автоматически и возвращается значение кнопки по умолчанию.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Нет | Заголовок окна. При отсутствии — пустой заголовок |
| args[1] | String | Нет | Текст сообщения. При отсутствии — пустое сообщение |
| args[2] | Integer/String | Нет | Набор кнопок (по умолчанию 2 / OKCANCEL). Числа: 1 = OK, 2 = OKCANCEL, 3 = YESNOCANCEL, 4 = YESNO, 5 = YES_NO_YESTOALL_NOTOALL_CANCEL. Строки: "OK", "OKCANCEL", "YESNOCANCEL", "YESNO", "ABORTRETRYIGNORE", "RETRYCANCEL" |
| args[3] | Integer/String | Нет | Тип иконки (по умолчанию INFORMATION). Числа: 0 = INFORMATION, 1 = EXCLAMATION, 2 = QUESTION, 3 = STOP. Строки: "INFORMATION", "EXCLAMATION", "QUESTION", "STOP" |
| args[4] | Integer | Нет | Номер кнопки по умолчанию (по умолчанию 0): 0 = первая кнопка, 1 = вторая кнопка, 2 = третья кнопка |
| args[5] | Boolean | Нет | Системный режим (по умолчанию FALSE). Если TRUE, диалог выводится в любом потоке и в транзакции |
| args[6] | Integer | Нет | Таймаут в миллисекундах (по умолчанию 10000). При значении 0 — бесконечность (без автозакрытия) |

**Возвращает**: при текстовом args[2] — `String` с именем нажатой кнопки ("OK", "CANCEL", "ABORT", "RETRY", "IGNORE", "YES", "NO", "CLOSE", "HELP"). При YESNOCANCEL (3) и числовом args[2] — `Integer` (Yes = -1, No = 0, Cancel = 1). При YES_NO_YESTOALL_NOTOALL_CANCEL (5) — `String` ("YES", "NO", "YESTOALL", "NOTOALL", "CANCEL"). В остальных случаях — `Boolean` (OK/Yes = TRUE, Cancel/No = FALSE). При срабатывании таймаута возвращается значение кнопки по умолчанию (args[4]).

```EME-L
result = is_timer_message("Заголовок", "Автозакрытие через 5с", 2, 0, 0, FALSE, 5000)
```



### is_input — ввод однострочного текста

Выводит диалоговое окно для ввода однострочного текста с кнопками OK и Отмена. Вызов InputStringBox.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Подсказка (prompt), отображаемая над полем ввода |
| args[1] | String | Да | Стартовое значение, предзаполняющее поле ввода |
| args[2] | String | Нет | Заголовок окна диалога (по умолчанию "EME") |

**Возвращает**: `String` — введённая пользователем строка на нажатие OK. Пустую строку на нажатие Отмена или при ошибке.

```EME-L
result = is_input("Введите значение:", "по умолчанию", "Мой диалог")
```



### is_multiinput — ввод многострочного текста

Выводит диалоговое окно для ввода многострочного текста с кнопками OK и Отмена. Аналогична is_input, но использует многострочное поле ввода. Вызов InputMessageText.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Подсказка (prompt), отображаемая над полем ввода |
| args[1] | String | Да | Стартовое значение, предзаполняющее поле ввода |
| args[2] | String | Нет | Заголовок окна диалога (по умолчанию "EME") |

**Возвращает**: `String` — введённая пользователем многострочная строка на нажатие OK. Пустую строку на нажатие Отмена или при ошибке.

```EME-L
text = is_multiinput("Введите комментарий:", "", "Комментарий")
```



### is_get_password — ввод пароля

Выводит диалоговое окно для ввода пароля с маскированием вводимых символов.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Заголовок диалогового окна |
| args[1] | String | Да | Текст-подсказка, отображаемый в диалоге |

**Возвращает**: `String` — введённый пользователем пароль. Если поддержка диалога ввода пароля отсутствует, возвращает пустое значение.

```EME-L
pwd = is_get_password("Авторизация", "Введите пароль:")
```



### is_show_popup_window — всплывающее уведомление

Показывает всплывающее окно в правой нижней части экрана. Вызов EMEShowPopupWindow.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Текст сообщения |
| args[1] | String | Да | Имя запускаемого диалога при нажатии |
| args[2] | Integer | Да | Номер строки диалога |
| args[3] | Integer | Да | Код запуска (OVL-код) |

**Возвращает**: пустое значение.

```EME-L
is_show_popup_window("Новое задание", "DialogOrders", 1, 0)
```



### is_show_email_icon — иконка email

Управляет отображением иконки email в статусной строке главного окна.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Boolean | Да | TRUE — показать иконку, FALSE — скрыть |

**Возвращает**: пустое значение.

```EME-L
is_show_email_icon(TRUE)
```



## Окружение и информация о системе

### is_computer_name — сетевое имя компьютера

Возвращает сетевое имя локального компьютера. Вызов GetComputerName.

**Параметры**: нет.

**Возвращает**: `String` — имя компьютера.

```EME-L
name = is_computer_name()
```



### is_domain_name — имя домена

Возвращает имя домена текущего пользователя операционной системы. Вызов GetCurrentUserDomainInfo.

**Параметры**: нет.

**Возвращает**: `String` — имя домена. В случае ошибки возвращает пустое значение.

```EME-L
domain = is_domain_name()
```



### is_domain_dc — Domain Component

Возвращает имя домена текущего пользователя ОС в формате DC (Domain Component). Вызов GetCurrentUserDomainInfo.

**Параметры**: нет.

**Возвращает**: `String` — имя домена в формате DC. В случае ошибки возвращает пустое значение.

```EME-L
dc = is_domain_dc()
```



### is_domain_username — доменное имя пользователя

Возвращает доменное имя текущего пользователя операционной системы. Вызов GetCurrentUserDomainInfo.

**Параметры**: нет.

**Возвращает**: `String` — доменное имя пользователя. В случае ошибки возвращает пустое значение.

```EME-L
user = is_domain_username()
```



### is_server — проверка сервера БД

Проверяет, выполняется ли текущий код на сервере БД. Сравнение get_workstation_mode() == SERVER.

**Параметры**: нет.

**Возвращает**: `Boolean` — TRUE, если код выполняется на сервере, иначе FALSE.

```EME-L
If (is_server())
    'Код выполняется на сервере'
End If
```



### is_http_server — проверка HTTP-сервера

Проверяет, выполняется ли текущий код на HTTP-сервере. Если указан параметр transactionPort = TRUE, дополнительно проверяет, что запрос пришёл на транзакционный порт HTTP-сервера.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| transactionPort | Boolean | Нет | TRUE — проверить транзакционный порт (по умолчанию FALSE) |

**Возвращает**: `Boolean` — TRUE, если условие выполнено, иначе FALSE.

```EME-L
If (is_http_server())
    'Код на HTTP-сервере'
End If

If (is_http_server(TRUE))
    'Код на транзакционном порту HTTP-сервера'
End If
```



### is_brain_computer — мозговой компьютер

Проверяет, выполняется ли текущий код на мозговом компьютере (brain computer). Проверка флага командной строки CLF_BC.

**Параметры**: нет.

**Возвращает**: `Boolean` — TRUE, если код выполняется на мозговом компьютере, иначе FALSE.

```EME-L
If (is_brain_computer())
    'Мозговой компьютер'
End If
```



### is_workstation_mode — режим рабочей станции

Возвращает режим рабочей станции в виде строки.

**Параметры**: нет.

**Возвращает**: `String` — режим рабочей станции: `"MONO"` — монопольный режим, `"WORKSTATION"` — рабочая станция, `"SERVER"` — сервер, `"CANAL_BRAIN"` — мозговой компьютер канала, `"TERMINAL"` — терминал, `"TERM_BRAIN"` — мозговой компьютер терминала.

```EME-L
mode = is_workstation_mode()
```



### is_workstation_type — тип рабочей станции

Возвращает тип рабочей станции по наличию данных виртуальной машины.

**Параметры**: нет.

**Возвращает**: `String` — тип рабочей станции: `"UNDEFINED_WS"` — не определено, `"FULLVM_WS"` — полная виртуальная машина, `"NETVM_WS"` — сетевая виртуальная машина.

```EME-L
wtype = is_workstation_type()
```



### is_system_family — семейство ОС

Возвращает семейство текущей операционной системы.

**Параметры**: нет.

**Возвращает**: `String` — `"LINUX"` для Linux, `"WINDOWS"` для Windows.

```EME-L
If (is_system_family() = "LINUX")
    'Выполнение на Linux'
End If
```



### is_db_driver_name — имя DLL драйвера БД

Возвращает имя DLL-библиотеки драйвера базы данных проекта. Вызов CEMEPrj::GetDriverName.

**Параметры**: нет.

**Возвращает**: `String` — имя DLL-библиотеки драйвера.

```EME-L
dll = is_db_driver_name()
```



### is_get_tick_count — счётчик миллисекунд

Возвращает количество миллисекунд, прошедших с момента запуска системы. Вызов GetTickCount.

**Параметры**: нет.

**Возвращает**: `Integer` — время с момента запуска системы в миллисекундах.

```EME-L
tick = is_get_tick_count()
```



### is_keycode — нажатые клавиши

Возвращает комбинацию нажатых в данный момент клавиш в виде текстовой строки. Вызов GetKeyState.

**Параметры**: нет.

**Возвращает**: `String` — строка с комбинацией клавиш (например `"Ctrl+Alt+Shift+Z"`).

```EME-L
keys = is_keycode()
```



### is_key_press — проверка нажатия клавиши

Проверяет, нажата ли в данный момент указанная виртуальная клавиша. Вызов GetAsyncKeyState.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| vkey | Integer | Да | Код виртуальной клавиши (например `0x10` — Shift, `0x11` — Ctrl) |

**Возвращает**: `Boolean` — TRUE, если клавиша нажата, иначе FALSE.

```EME-L
If (is_key_press(0x41))
    'Нажата клавиша A'
End If
```



## Управление выполнением

### is_beep — звуковой сигнал

Воспроизводит звуковой сигнал заданной частоты и длительности через API Beep.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Частота звука в герцах |
| args[1] | Integer | Да | Длительность звука в миллисекундах |

**Возвращает**: `пустое значение`.

```EME-L
is_beep(1000, 500)  'звук 1000 Гц, 500 мс
```



### is_sleep — приостановка потока

Приостанавливает выполнение текущего потока на указанное время. Вызов API Sleep.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Время приостановки в миллисекундах |

**Возвращает**: пустое значение.

```EME-L
is_sleep(1000)  'пауза 1 секунда
```



### is_exit — завершение приложения

Завершает работу приложения с опциональным сообщением об ошибке. Вызов exit_with_message.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Нет | Номер первого сообщения из словаря |
| args[1] | Integer | Нет | Номер второго сообщения из словаря |
| args[2] | String | Нет | Произвольный текст сообщения |

**Возвращает**: `пустое значение` (приложение завершается).

```EME-L
is_exit()
is_exit(7, 366, "Ошибка!")
```



### is_do_wait_cursor — песочные часы

Управляет отображением песочных часов (курсора ожидания). Вызов do_wait_cursor.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Нет | Направление действия: 1 — включить (увеличить счётчик), -1 — выключить (уменьшить счётчик), 0 — сбросить счётчик. При отсутствии параметра ничего не делает |

**Возвращает**: `Integer` — 0 (целое число).

```EME-L
is_do_wait_cursor(1)   'включить песочные часы
is_do_wait_cursor(-1)  'выключить песочные часы
is_do_wait_cursor(0)   'сбросить счётчик
```



### is_main_thread — проверка главного потока

Проверяет, является ли текущий поток главным (основным) потоком приложения.

**Параметры**: нет.

**Возвращает**: `Boolean` — TRUE если текущий поток — главный, иначе FALSE.

```EME-L
If (is_main_thread())
    'Главный поток'
End If
```



### is_managed_thread — управляемый поток

Управляет режимом управляемого потока или проверяет, является ли текущий поток управляемым. Управляемый поток должен периодически сообщать о своей активности.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Нет | Интервал проверки потока в миллисекундах. 0 — отключить управление потоком. При отсутствии параметра только возвращает статус |

**Возвращает**: без параметров — `Boolean` — TRUE если текущий поток управляемый, иначе FALSE. С параметром — `пустое значение`.

```EME-L
If (is_managed_thread())
    'Поток управляемый'
End If

' Включить управление с интервалом 1 секунда
is_managed_thread(1000)
' ... выполнение работы ...
is_managed_thread(0)     'отключить управление
```



### is_DelayedRefNuller — отложенное обнуление ссылок

Включает или отключает отложенное обнуление ссылок в системе.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Boolean | Да | TRUE — включить отложенное обнуление, FALSE — выключить |

**Возвращает**: пустое значение.

```EME-L
is_DelayedRefNuller(FALSE)
```



## Системные настройки

### is_system — системная настройка

Возвращает системную настройку по указанному индексу.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Индекс системной настройки (целое число) |

Индексы:
- 0 — Ссылка на текущего оператора (целое число)
- 1 — Флаги доступа текущего оператора (целое число, битовая маска 32 бита)
- 2 — Идентификатор (имя) текущего оператора (строка)
- 3 — Код запуска модуля (целое число)
- 4 — Код текущего филиала (целое число)
- 5 — Ссылка на текущий филиал (целое число)
- 6 — Код (имя файла) проекта (строка)
- 7 — Признак рабочей станции (boolean)
- 8 — Признак мозгового компьютера (boolean)

**Возвращает**: значение системной настройки в соответствии с индексом. Тип зависит от индекса (см. выше).

```EME-L
ref = is_system(0)      'ссылка на оператора
flags = is_system(1)    'флаги доступа (32 бита)
name = is_system(2)     'имя оператора
code = is_system(3)     'код запуска модуля
fcode = is_system(4)    'код филиала
fref = is_system(5)     'ссылка на филиал
pname = is_system(6)    'код (имя файла) проекта
ws = is_system(7)       'признак рабочей станции
bc = is_system(8)       'признак мозгового компьютера
```



### is_set_system — установка кода запуска (устаревшая)

> **Устаревшая**: функция доступна только в версиях ядра до 52.0 (`#if LAST_VERSION < MAKE_VERSION(52,0)`).

Устанавливает код запуска модуля (OVL-код) для текущего диалога. Вызов `SetCurrentOvlCode`.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Новый код запуска модуля (целое число) |

**Возвращает**: пустое значение.

```EME-L
is_set_system(10)    'установить OVL-код = 10
```



### is_sys_param — параметр настройки

Возвращает значение системной настройки по имени. Имя настройки должно начинаться с префикса `W://` (настройки рабочей станции) или `M://` (настройки модулей).

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Имя настройки с префиксом W:// или M:// |
| args[1] | Any | Нет | Тип возвращаемого значения: `VT_BSTR` — строка, `VT_I2`/`VT_I4` — целое число, `VT_R8` — вещественное число. По умолчанию определяется автоматически по типу variant |
| args[2] | Boolean | Нет | Режим обработки ошибок (по умолчанию `FALSE`): `FALSE` — EXCEPT_MODE (при отсутствии настройки вызывает ошибку), `TRUE` — SOFT_MODE (при отсутствии настройки возвращает пустое значение) |

**Возвращает**: значение настройки (тип зависит от args[1]: строка, целое число или вещественное число).

```EME-L
val = is_sys_param("M://MySetting", "")
val = is_sys_param("W://WsSetting", 0)
val = is_sys_param("M://SoftSetting", "", TRUE)    'SOFT_MODE
```



### is_add_sys_param — добавление настройки

Создаёт или обновляет системную настройку. Имя настройки должно начинаться с префикса `W://` (настройки рабочей станции) или `M://` (настройки модулей). Для `W://` настройка сохраняется в базе данных через `StoreWsSetting`, для `M://` — через `create_module_param`.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Имя настройки с префиксом W:// или M:// |
| args[1] | String | Да | Значение настройки |
| args[2] | String | Нет | Текст подсказки (HelpText) |
| args[3] | String | Нет | Список допустимых значений в виде строки с разделителями `\r\n` |
| args[4] | Boolean | Нет | `TRUE` — перезагрузить все модули после изменения (по умолчанию `TRUE`) |

**Возвращает**: пустое значение.

```EME-L
is_add_sys_param("M://NewSetting", "value", "Описание", "val1\r\nval2")
```



### is_check_text_key — проверка ключа командной строки

Проверяет наличие прикладного ключа командной строки при запуске программы. Прикладные ключи имеют префикс `/d_`, например `/d_emel` передаётся как `"EMEL"`. Вызов `CheckTextKey`.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Наименование ключа без префикса `/d_` |

**Возвращает**: `Boolean` — `TRUE`, если ключ установлен, иначе `FALSE`.

```EME-L
If (is_check_text_key("EMEL"))
    'Ключ /d_emel установлен'
End If
```



## Права доступа и пользователи

### is_administrator — проверка администратора

Проверяет, обладает ли текущий оператор правами администратора. Вызов `user_as_administrator`.

**Параметры**: нет.

**Возвращает**: `Boolean` — `TRUE`, если текущий оператор — администратор, иначе `FALSE`.

```EME-L
If (is_administrator())
    'Администратор'
End If
```



### is_access — проверка прав доступа

Проверяет наличие у текущего оператора права доступа по указанному биту в 32-битном поле флагов доступа. Вызов `get_flag_access` с маской `1 << bitNo`.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Номер бита в поле доступа (0..31) |

**Возвращает**: `Boolean` — `TRUE`, если бит установлен (оператор имеет право), иначе `FALSE`.

```EME-L
If (is_access(5))
    'Право доступа 5 есть'
End If
```



### is_password — диалог ввода пароля

Выводит диалог ввода пароля для идентификации текущего оператора. Вызов `password(TRUE)`.

**Параметры**: нет.

**Возвращает**: `Boolean` — `TRUE` при успешной идентификации, `FALSE` при ошибке.

```EME-L
If (is_password())
    'Пароль верный'
End If
```



### is_change_password — смена пароля

Выводит диалог смены пароля для указанного пользователя.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Нет | Ссылка на пользователя (целое число, по умолчанию `NULL_REF` — текущий пользователь) |
| args[1] | Boolean | Нет | `TRUE` — требовать ввод старого пароля (по умолчанию `TRUE`) |

**Возвращает**: пустое значение.

```EME-L
is_change_password()                       'смена пароля текущего пользователя
is_change_password(user_ref, TRUE)         'с указанием пользователя
```



### is_set_user_read_only — режим только чтение

Устанавливает в памяти у текущего пользователя флаг «только чтение» (бит номер 1 в поле 32 бита допусков). Изменение не сохраняется в БД. При установленном флаге запись в базу данных запрещена.

**Параметры**: нет.

**Возвращает**: пустое значение.

```EME-L
is_set_user_read_only()
```



### is_clr_user_read_only — снять режим только чтение

Снимает в памяти у текущего пользователя флаг «только чтение» (бит номер 1 в поле 32 бита допусков). Изменение не сохраняется в БД. При снятом флаге запись в базу данных разрешена.

**Параметры**: нет.

**Возвращает**: пустое значение.

```EME-L
is_clr_user_read_only()
```



### is_disconnect_user — отключение пользователя

Отправляет на все рабочие станции команду отключить указанного пользователя. Вызов `DisconnectUser`.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Ссылка на пользователя в записи «Список пользователей» |

**Возвращает**: пустое значение.

```EME-L
is_disconnect_user(user_ref)
```



### is_impersonate_user — установка пользователя

Устанавливает текущего пользователя в неинтерфейсном потоке (HTTP, CLR и т.д.). Вызов `impersonate_user`.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Ссылка на пользователя в записи «Список пользователей» |

**Возвращает**: пустое значение.

```EME-L
is_impersonate_user(admin_ref)
```



### is_security_event — событие безопасности

Генерирует произвольное событие информационной безопасности. Обработка события выполняется в методе `OnSecurityEvent` класса `Resident`.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Ссылка на пользователя |
| args[1] | Integer | Да | Код события |
| args[2] | String | Да | Описание события |

**Возвращает**: пустое значение.

```EME-L
is_security_event(user_ref, 1001, "Попытка несанкционированного доступа")
```



## Филиалы и разделы

### is_center — центральный филиал

проверяет, является ли текущий филиал центром. Вызов DBS_IsCenterFilial.

**Параметры**: нет.

**Возвращает**: `Boolean` — TRUE, если текущий филиал — центр, иначе FALSE.

```EME-L
If (is_center())
    'Центральный филиал'
End If
```



### is_root_center — главный (корневой) центр

проверяет, является ли текущий филиал главным (корневым) центром в многоуровневой иерархии филиалов. Вызов IsRootCenter.

**Параметры**: нет.

**Возвращает**: `Boolean` — TRUE, если текущий филиал — главный центр, иначе FALSE.

```EME-L
If (is_root_center())
    'Главный центр'
End If
```



### is_node_center — локальный (промежуточный) центр

проверяет, является ли текущий филиал локальным (промежуточным) центром в многоуровневой иерархии филиалов. Вызов IsNodeCenter.

**Параметры**: нет.

**Возвращает**: `Boolean` — TRUE, если текущий филиал — локальный центр, иначе FALSE.

```EME-L
If (is_node_center())
    'Локальный центр'
End If
```



### is_filial — обычный филиал (не центр)

проверяет, является ли текущий филиал обычным филиалом (не центром) в многоуровневой иерархии. Вызов IsFilial.

**Параметры**: нет.

**Возвращает**: `Boolean` — TRUE, если текущий филиал — обычный филиал, иначе FALSE.

```EME-L
If (is_filial())
    'Обычный филиал'
End If
```



### is_system_filial — системный филиал

проверяет, является ли текущий филиал системным филиалом. Вызов MS_IsSystemFilial.

**Параметры**: нет.

**Возвращает**: `Boolean` — TRUE, если текущий филиал — системный, иначе FALSE.

```EME-L
If (is_system_filial())
    'Системный филиал'
End If
```



### is_parent_filial_ref — ссылка на родительский филиал

возвращает ссылку на филиал-родитель текущего филиала. Вызов get_parent_filial_ref.

**Параметры**: нет.

**Возвращает**: `Integer` — ссылка на родительский филиал.

```EME-L
pref = is_parent_filial_ref()
```



### is_parent_filial_code — код родительского филиала

возвращает код филиала-родителя текущего филиала. Вызов get_parent_filial_code.

**Параметры**: нет.

**Возвращает**: `Integer` — код родительского филиала.

```EME-L
pcode = is_parent_filial_code()
```



### is_filial_no — код (номер) текущего филиала

возвращает код (номер) текущего филиала базы данных.

**Параметры**: нет.

**Возвращает**: `Integer` — код текущего филиала.

```EME-L
fno = is_filial_no()
```



### is_filial_ref — ссылка на текущий филиал

возвращает ссылку на текущий филиал базы данных в записи Филиалы.

**Параметры**: нет.

**Возвращает**: `Integer` — ссылка на текущий филиал.

```EME-L
fref = is_filial_ref()
```



### is_division_no — номер текущего раздела

возвращает номер текущего раздела базы данных.

**Параметры**: нет.

**Возвращает**: `Integer` — номер текущего раздела.

```EME-L
dno = is_division_no()
```



### is_division_ref — ссылка на текущий раздел

возвращает ссылку на текущий раздел базы данных в записи Разделы БД.

**Параметры**: нет.

**Возвращает**: `Integer` — ссылка на текущий раздел.

```EME-L
dref = is_division_ref()
```



### is_change_division_no — изменение номера раздела

изменяет номер текущего раздела базы данных. Можно вызывать только в монопольном режиме или на сервере. При ошибке вызывает исключение.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Новый номер раздела БД |

**Возвращает**: пустое значение.

```EME-L
is_change_division_no(5)
```



## Цвета

### is_RGB — цвет из RGB

формирует цветовое значение типа COLORREF из трёх компонент RGB. Вызов макроса RGB.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Интенсивность красного канала (0-255) |
| args[1] | Integer | Да | Интенсивность зелёного канала (0-255) |
| args[2] | Integer | Да | Интенсивность синего канала (0-255) |

**Возвращает**: `Integer` — цветовое значение COLORREF.

```EME-L
color = is_RGB(255, 128, 0)
```



### is_RGBA — цвет из RGBA

формирует цветовое значение RGBA с альфа-каналом (прозрачностью). Поддерживает два режима вызова. Вызов qRgba.

**Режим 1** — отдельные каналы (4 аргумента):

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Интенсивность красного канала (0-255) |
| args[1] | Integer | Да | Интенсивность зелёного канала (0-255) |
| args[2] | Integer | Да | Интенсивность синего канала (0-255) |
| args[3] | Integer | Нет | Альфа-канал (0-255, по умолчанию 0x00 — полностью прозрачный) |

**Режим 2** — из готового RGB-цвета (2 аргумента):

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Готовый RGB-цвет (COLORREF) |
| args[1] | Integer | Нет | Альфа-канал (0-255, по умолчанию 0x00 — полностью прозрачный) |

**Возвращает**: `Integer` — цветовое значение RGBA.

```EME-L
color = is_RGBA(255, 0, 0, 128)   'полупрозрачный красный из каналов
color = is_RGBA(rgb_color, 200)    'из готового RGB с прозрачностью
```



### is_blendRGB — смешивание цветов

вычисляет промежуточный цвет между двумя заданными цветами по линейной интерполяции с указанным коэффициентом.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Первый цвет (COLORREF) |
| args[1] | Integer | Да | Второй цвет (COLORREF) |
| args[2] | Real | Нет | Коэффициент смешивания (0.0-1.0, по умолчанию 0.5). 0.0 — полностью второй цвет, 1.0 — полностью первый цвет. |

**Возвращает**: `Integer` — смешанный цвет COLORREF.

```EME-L
mixed = is_blendRGB(color1, color2, 0.7)  '70% первый, 30% второй
mixed = is_blendRGB(color1, color2)        '50/50
mixed = is_blendRGB(color1, color2, 0.0)   'полностью второй цвет
mixed = is_blendRGB(color1, color2, 1.0)   'полностью первый цвет
```



### is_sys_color — системный цвет Windows

возвращает текущий системный цвет указанного элемента Windows. Вызов GetSysColor.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Название отображаемого элемента Windows. Поддерживаемые значения: "3DDKSHADOW", "3DFACE", "BTNFACE", "3DHILIGHT", "3DLIGHT", "3DSHADOW", "ACTIVEBORDER", "ACTIVECAPTION", "APPWORKSPACE", "BACKGROUND", "BTNTEXT", "CAPTIONTEXT", "GRAYTEXT", "HIGHLIGHT", "HIGHLIGHTTEXT", "INACTIVEBORDER", "INACTIVECAPTION", "INACTIVECAPTIONTEXT", "INFOBK", "INFOTEXT", "MENU", "MENUTEXT", "SCROLLBAR", "WINDOW", "WINDOWFRAME", "WINDOWTEXT", "GRADIENTACTIVECAPTION", "GRADIENTINACTIVECAPTION", "HOTLIGHT". |

**Возвращает**: `Integer` — текущий системный цвет элемента (COLORREF).

```EME-L
color = is_sys_color("WINDOW")
color = is_sys_color("HIGHLIGHTTEXT")
color = is_sys_color("GRADIENTACTIVECAPTION")
```



### is_legend_color — цвет из палитры легенды

возвращает цвет из встроенной палитры легенды по индексу (0-21).

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Номер цвета в палитре (0-21). См. таблицу палитры ниже. |

**Возвращает**: `Integer` — цветовое значение из палитры (COLORREF). При выходе индекса за диапазон возвращает серый цвет RGB(192,192,192).

**Палитра легенды (22 цвета):**

| Индекс | Название | Описание |
|--------|----------|----------|
| 0 | красный | Красный |
| 1 | томатный | Томатный |
| 2 | розовый | Розовый |
| 3 | оранжевый | Оранжевый |
| 4 | жёлтый | Жёлтый |
| 5 | светло-жёлтый | Светло-жёлтый |
| 6 | зелёный | Зелёный |
| 7 | салатовый | Салатовый |
| 8 | тёмный циан | Тёмный циан |
| 9 | тёмное морское зелёное | Тёмное морское зелёное |
| 10 | синий | Синий |
| 11 | васильковый синий | Васильковый синий |
| 12 | циановый | Циановый |
| 13 | небесно-голубой | Небесно-голубой |
| 14 | фиолетовый | Фиолетовый |
| 15 | маджента | Маджента |
| 16 | светло-фиолетовый | Светло-фиолетовый |
| 17 | коричневый | Коричневый |
| 18 | огнеупорный кирпичный | Огнеупорный кирпичный |
| 19 | молочный шоколад | Молочный шоколад |
| 20 | оливковый | Оливковый |
| 21 | тёмный хаки | Тёмный хаки |

```EME-L
color = is_legend_color(0)   'красный
color = is_legend_color(7)   'салатовый
color = is_legend_color(21)  'тёмный хаки
color = is_legend_color(99)  'серый (192,192,192) — вне диапазона
```



## Язык и локализация

### is_set_language — установка языка

устанавливает код языка локализации для текущего потока. Если код не указан, устанавливается язык по умолчанию для интерфейса.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Нет | Код языка (например, "RUS", "ENG"). При отсутствии — язык по умолчанию для интерфейса |

**Возвращает**: `Integer` — 1 при успешной установке, 0 если функциональность недоступна.

```EME-L
is_set_language("EN")
is_set_language()  'язык по умолчанию
```



### is_get_language — текущий язык

возвращает код языка локализации, установленный для текущего потока.

**Параметры**: нет.

**Возвращает**: `String` — код языка. Если функциональность недоступна, возвращает "0".

```EME-L
lang = is_get_language()
```



### is_set_translate_regim — режим перевода

устанавливает режим перевода для открытия отчётов с диалогами.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Режим перевода: 0 — отключить перевод, 1 — только считывать тексты, 2 — переводить тексты |

**Возвращает**: `Integer` — 0.

```EME-L
is_set_translate_regim(1)
```



### is_get_translate_regim — текущий режим перевода

возвращает текущий режим перевода для открытия отчётов с диалогами.

**Параметры**: нет.

**Возвращает**: `Integer` — режим перевода: 0 — отключен, 1 — считывать тексты, 2 — переводить тексты.

```EME-L
regim = is_get_translate_regim()
```



### ML — заглушка локализации

заглушка для функции локализации. Всегда возвращает пустое значение.

**Параметры**: нет.

**Возвращает**: пустое значение.



### tr — перевод строки

возвращает перевод строки на текущий язык из записи Локализация. Если перевод отсутствует, возвращает исходную строку (или её транслитерацию). Перевод зависит от контекста.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Исходная строка для перевода |
| args[1] | String | Нет | Контекст перевода. Для EME-L-кода формируется автоматически. Пример: "EMEL:ProcessTemplates" |

**Возвращает**: `String` — переведённая строка. Если перевод не найден — исходная строка.

```EME-L
translated = tr("Hello")
translated = tr("Hello", "context")
```



### is_transliterate — транслитерация

транслитерирует строку: заменяет русские буквы и символы кодировки Windows-1251 на подходящие по произношению английские буквы и сочетания. Длина результирующей строки может увеличиться.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Исходная строка |

**Возвращает**: `String` — транслитерированная строка.

```EME-L
result = is_transliterate("Привет")  'Privet
```



### is_decimal_separator — десятичный разделитель

возвращает текущий десятичный разделитель из локальных настроек системы. Вызов GetLocaleInfo с LOCALE_SDECIMAL.

**Параметры**: нет.

**Возвращает**: `String` — десятичный разделитель (обычно "." или ",").

```EME-L
sep = is_decimal_separator()
```



### is_locale_info — информация о локали

возвращает локальную установку системы. Аналог WinAPI GetLocaleInfo.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Идентификатор локали: "LOCALE_SYSTEM_DEFAULT" (системная) или "LOCALE_USER_DEFAULT" (пользователя) |
| args[1] | String | Да | Тип запрашиваемой установки: "LOCALE_SDECIMAL" (десятичный разделитель), "LOCALE_SLIST" (разделитель списка) и др. |

**Возвращает**: `String` — значение локальной установки.

```EME-L
sep = is_locale_info("LOCALE_USER_DEFAULT", "LOCALE_SDECIMAL")
```



## Хеш и криптография

### is_qhash — хеш-сумма строки

Вычисляет хеш-сумму строки с использованием функции Qt qHash. Для воспроизводимости результата на разных платформах строка предварительно перекодируется в Unicode через QTextCodec. Доступна только в версии ядра 30.0 и выше.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Строка, хеш которой необходимо вычислить |
| args[1] | String | Нет | Имя кодировки для декодирования строки (по умолчанию "Windows-1251") |

**Возвращает**: `Integer` — хеш-сумму переданной строки, вычисленную функцией qHash. В версиях ядра ниже 30.0 возвращает пустое значение.

```EME-L
h = is_qhash("test string")
h = is_qhash("test string", "UTF-8")
```



## Реестр Windows

### is_reg_query_value — чтение из реестра

читает значение из реестра Windows по указанному пути. Поддерживает типы REG_SZ и REG_DWORD.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Корневой раздел реестра: "HKCR"/"HKEY_CLASSES_ROOT", "HKLM"/"HKEY_LOCAL_MACHINE", "HKCU"/"HKEY_CURRENT_USER", "HKU"/"HKEY_USERS", "HKCC"/"HKEY_CURRENT_CONFIG", "HKDD"/"HKEY_DYN_DATA", "HKPD"/"HKEY_PERFORMANCE_DATA" |
| args[1] | String | Да | Путь к подразделу, например "Software\MyCompany\MyApp" |
| args[2] | String | Да | Имя читаемого параметра |
| args[3] | Any | Нет | Значение по умолчанию, если параметр не найден. При отсутствии — вызывает исключение |

**Возвращает**: значение параметра (строка или целое число в зависимости от типа в реестре).

```EME-L
val = is_reg_query_value("HKLM", "SOFTWARE\\MyApp", "Setting", "default")
```



## Сообщения между компонентами

### is_send_message — отправка сообщения

отправляет сообщение указанному органу управления диалога или всем органам. При отправке в главный диалог (ГФД) требуются дополнительные параметры record и line.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String/Integer | Да | Адресат сообщения: строка — имя органа управления, 0 — все органы текущего диалога, 1 — главный диалог (ГФД) |
| args[1] | Integer | Да | Номер сообщения |
| args[2] | Integer | Нет | Уровень диалога в иерархии (по умолчанию 0 — текущий). 1 — родительский, 2 — родитель родителя и т.д. |
| args[3] | Integer | Нет | Номер записи для вызова метафункции ГФД |
| args[4] | Integer | Нет | Номер строки записи для вызова метафункции ГФД |

**Возвращает**: пустое значение.

```EME-L
is_send_message("Button1", 100)
is_send_message(0, 200)  'все органы
```



### is_message_to_workstations — сообщение на рабочие станции

отправляет всплывающее сообщение на рабочие станции указанному пользователю. Вызов EMEShowPopupWindowToWorkstations.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Ссылка на оператора |
| args[1] | String | Да | Текст сообщения |
| args[2] | String | Да | Имя запускаемого диалога при нажатии на окно |
| args[3] | Integer | Да | Номер строки диалога |
| args[4] | Integer | Да | Код запуска (OVL-код) |

**Возвращает**: пустое значение.

```EME-L
is_message_to_workstations(user_ref, "Внимание! Система перезагружается")
```



## История операций

### is_add_history_line — запись в историю

добавляет строку в историю операций. При двух параметрах используется запись OperationHistory с автоматическим определением текущего оператора. При четырёх параметрах вызывает устаревшую функцию add_history_line.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Код тома |
| args[1] | String | Да | Текст записи для истории операций |
| args[2] | Integer | Нет | Номер записи |
| args[3] | Integer | Нет | Номер строки записи |

**Возвращает**: пустое значение.

```EME-L
is_add_history_line(1, "Операция выполнена")
is_add_history_line(1, "Операция с записью", rec_no, line_no)
```



## Транзакции сервера

### is_server_transaction_dump — дамп транзакций

на сервере делает дамп транзакций в объект запроса. На рабочей станции подготавливает запрос.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Object(Query) | Да | Пустой запрос для приёма дампа |
| args[1] | Any | Нет | Имя/номер записи (NULL_RECORD — все) |
| args[2] | Any | Нет | Имя/номер поля (NULL_FIELD — все) |
| args[3] | Any | Нет | Номер строки (NULL_REF — все) |
| args[4] | Integer | Нет | Номер транзакции с (0 — все) |
| args[5] | Integer | Нет | Номер транзакции по |
| args[6] | String | Нет | Данные для поиска |
| args[7] | DateTime | Нет | Дата начала |
| args[8] | DateTime | Нет | Дата окончания |
| args[9] | DateTime | Нет | Время начала |
| args[10] | DateTime | Нет | Время окончания |
| args[11] | Boolean | Нет | Не показывать служебную информацию |
| args[12] | String (ByRef) | Нет | Строка для возврата истории поиска (по RefOf) |

**Возвращает**: `Boolean` — TRUE при успехе.

```EME-L
q = Object("Query")
result = is_server_transaction_dump(q, NULL_RECORD, NULL_FIELD, NULL_REF)
```



### is_server_transaction_data — данные транзакции

на сервере возвращает данные одной команды транзакции. На рабочей станции не делает ничего.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Номер транзакции |
| args[1] | Integer | Да | Номер команды в транзакции |
| args[2] | Integer | Да | Номер строки записи |

**Возвращает**: `String` — данные команды или пустое значение.

```EME-L
data = is_server_transaction_data(trans_no, cmd_no)
```



### is_server_first_transaction — границы транзакций

на сервере возвращает номера первой и последней транзакции в хранилище, а также дату и время первой транзакции.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer (ByRef) | Да | Ссылка на переменную для номера первой транзакции |
| args[1] | Integer (ByRef) | Да | Ссылка на переменную для номера последней транзакции |
| args[2] | DateTime (ByRef) | Да | Ссылка на переменную для даты первой транзакции |
| args[3] | DateTime (ByRef) | Да | Ссылка на переменную для времени первой транзакции |

**Возвращает**: `Boolean` — TRUE если транзакции есть.

```EME-L
first = 0
last = 0
If (is_server_first_transaction(first, last))
    'Транзакции есть'
End If
```



### is_server_emel — вызов серверного EME-L

вызывает на сервере метод Resident::OnServerEmel и передаёт параметры.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Номер команды |
| args[1] | Object(Parameters) | Да | Объект класса "Parameters" |

**Возвращает**: `String` — результат метода OnServerEmel.

```EME-L
Params = Object("Parameters")
Params.Put("key", "value")
result = is_server_emel(1, Params)
```



### is_transaction_no — номер транзакции

возвращает текущее значение номера транзакции.

**Параметры**: нет.

**Возвращает**: `Integer` — номер транзакции.

```EME-L
tno = is_transaction_no()
```



## RPC

### is_process_rpc_over_file_system — RPC через ФС

Обрабатывает удалённый вызов процедур (RPC) поверх файловой системы. Вызов FileSystem::RunRPC.

**Параметры**: нет.

**Возвращает**: `Boolean` — результат выполнения RPC.

```EME-L
is_process_rpc_over_file_system()
```



### is_last_rpc_date — дата последнего RPC

возвращает дату последнего RPC.

**Параметры**: нет.

**Возвращает**: `DateTime` — дата.

```EME-L
d = is_last_rpc_date()
```



### is_last_rpc_time — время последнего RPC

возвращает время последнего RPC.

**Параметры**: нет.

**Возвращает**: `DateTime` — время.

```EME-L
t = is_last_rpc_time()
```



### is_last_rpc_number — номер последнего RPC

возвращает номер последнего RPC.

**Параметры**: нет.

**Возвращает**: `Integer` — номер RPC.

```EME-L
n = is_last_rpc_number()
```



### is_reset_last_rpc_info — сброс информации RPC

сбрасывает информацию о последнем RPC.

**Параметры**: нет.

**Возвращает**: пустое значение.

```EME-L
is_reset_last_rpc_info()
```



## Внешние программы

### is_shell_execute — запуск внешней программы

Запускает внешнее приложение, открывает документ или протокол через ShellExecute/ShellExecuteEx. Если указан таймаут, дожидается завершения процесса и возвращает его exit code. В режиме службы (NT Service) запуск заблокирован.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Действие (строка): "open", "explore", "print" и т.д. |
| args[1] | String | Да | Рабочий каталог (строка) |
| args[2] | String | Да | Имя приложения, документа, файла или протокола (строка) |
| args[3] | String | Нет | Параметры командной строки (строка) |
| args[4] | Integer | Нет | Таймаут ожидания завершения в миллисекундах (целое число). 0 — ждать бесконечно. При отсутствии параметра возвращает результат ShellExecute |

**Возвращает**: без args[4] — `Integer` — результат ShellExecute (> 32 при успехе). С args[4] — `Integer` — exit code завершённого процесса. Отрицательные коды при ошибках: -1 — ошибка ShellExecuteEx, -2 — ошибка ожидания, -3 — таймаут, -4 — некорректное завершение.

```EME-L
is_shell_execute("open", "C:\\", "notepad.exe")
is_shell_execute("open", "", "document.pdf", "", 10000)
```



### is_shell_execute_link — запуск по ярлыку

Запускает программу по указанному ярлыку (.lnk) через ShellExecute. Не дожидается завершения запущенного процесса. В режиме службы (NT Service) запуск заблокирован.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Полный путь к файлу ярлыка (например, "d:\\EME\\mono.lnk") |

**Возвращает**: `Boolean` — TRUE при успехе.

```EME-L
is_shell_execute_link("d:\\EME\\app.lnk")
```



## Версии

### is_check_version — проверка версии ядра

проверяет, что текущая версия ядра и метапроекта больше или равна указанной.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Нет | Ядро — major |
| args[1] | Integer | Нет | Ядро — minor |
| args[2] | Integer | Нет | Метапроект — major |
| args[3] | Integer | Нет | Метапроект — minor |

**Возвращает**: `Boolean` — TRUE если текущая версия >= указанной.

```EME-L
If (is_check_version(30, 5))
    'Версия ядра >= 30.5'
End If

If (is_check_version(30, 5, 20, 0))
    'Ядро >= 30.5 и метапроект >= 20.0'
End If
```



### is_type_version — тип версии (лицензия)

возвращает тип лицензии программы.

**Параметры**: нет.

**Возвращает**: `Integer` — 0 (без ключа), 1 (полная версия), 2 (стандартная).

```EME-L
vtype = is_type_version()
```



### is_license_param — параметр лицензии

возвращает значение параметра лицензии по имени.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Имя параметра лицензии |

**Возвращает**: `String` — значение параметра или `Empty` если не найден.

```EME-L
val = is_license_param("MaxUsers")
```



## Буфер обмена

### is_get_clipboard_data_type — тип данных буфера

возвращает тип данных в буфере обмена.

**Параметры**: нет.

**Возвращает**: `Integer` — -1 (пусто/неизвестно), 2 (CF_BITMAP), 15 (CF_HDROP).

```EME-L
ctype = is_get_clipboard_data_type()
```



### is_get_clipboard_as_bitmap — снимок экрана из буфера

получает содержимое буфера обмена как bitmap. Поддерживается только снимок экрана.

**Параметры**: нет.

**Возвращает**: `Empty` если буфер пуст или данные неподдерживаемы. Образ BMP-файла если в буфере снимок экрана.

```EME-L
bmp = is_get_clipboard_as_bitmap()
```



### is_put_clipboard_data_as_text — текст в буфер обмена

записывает текстовую строку в буфер обмена в формате Unicode.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Строка для записи |

**Возвращает**: `Boolean` — TRUE при успехе.

```EME-L
is_put_clipboard_data_as_text("Скопированный текст")
```



### is_put_clipboard_data_as_filelist — файл в буфер обмена

записывает ссылку на файл в буфер обмена.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Путь к файлу |
| args[1] | Boolean | Нет | TRUE — копировать (по умолчанию), FALSE — вырезать |

**Возвращает**: `Boolean` — TRUE при успехе.

```EME-L
is_put_clipboard_data_as_filelist("C:\\file.txt", TRUE)
```



## Структура БД

### is_export_db_structure — экспорт структуры БД

экспортирует структуру БД в файл.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Имя файла |

**Возвращает**: `Integer` — 0 при успехе, -1 при ошибке.

```EME-L
result = is_export_db_structure("structure.dbh")
```



### is_import_db_structure — импорт структуры БД

импортирует структуру БД из файла.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Имя файла |
| args[1] | String (ByRef) | Нет | Переменная по ссылке для возврата номера версии файла структуры |

**Возвращает**: `Boolean` — TRUE при успехе.

```EME-L
If (is_import_db_structure("structure.dbh"))
    'Импорт успешен'
End If
```



## Архивация БД

### is_archive_DB — архивация базы данных

выполняет архивацию базы данных: помечает устаревшие строки записей к удалению и создаёт архивный экспорт обрезанной копии базы. в языке EME-L функция должна вызываться в монопольном (MONO) режиме на мозговом компьютере вне транзакции. Если первый параметр пустой, пометка строк к архивации не выполняется — только архивный экспорт ранее помеченных строк. Вызов без аргументов или с одним аргументом недопустим, вызывает ошибку «Слишком мало параметров».

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Date | Да | Дата, до которой удалять документы и операции |
| args[1] | Date | Да | Срок годности (BBD), до которого удалять документы |
| args[2] | Date | Нет | Значение некорректного BBD, которое нужно игнорировать при фильтрации. По умолчанию максимально возможная дата (0x7FFF год) |
| args[3] | String | Нет | Директория для сохранения файлов архивного экспорта. По умолчанию используется стандартная директория архивного раздела |

**Возвращает**: пустое значение.

```EME-L
' Архивация: удалить документы до 01.01.2024, BBD до 01.01.2023
is_archive_DB(Date(2024, 1, 1), Date(2023, 1, 1))

' С указанием некорректного BBD и пути экспорта
is_archive_DB(Date(2024, 1, 1), Date(2023, 1, 1), Date(2099, 12, 31), "C:\Archive\export")
```

---

### is_eme_is_arc — проверка флага архивации строки

проверяет, установлен ли у строки записи флаг архивации (бит `TO_ARCHIVE_EXPORT_LINE_BIT` в ядерном поле флагов). в системе EME.WMS используется для выборочной пометки строк перед процедурой архивации.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Reference / Integer | Да | Объект записи CEMERec (ссылка) или номер записи в базе данных (целое число) |
| args[1] | Integer | Условно | Номер строки записи. Обязателен, если args[0] задан как номер записи. Не используется, если args[0] — объект CEMERec |

**Возвращает**: `Boolean` — `TRUE` если строка помечена к архивации, `FALSE` если не помечена.

```EME-L
' Проверка по номеру записи и строки
If (is_eme_is_arc(100, 5))
    ' Строка 5 записи 100 помечена к архивации'
End If

' Проверка по объекту CEMERec
If (is_eme_is_arc(rec))
    ' Текущая строка записи помечена'
End If
```

---

### is_eme_arc — установка флага архивации

устанавливает флаг архивации (бит `TO_ARCHIVE_EXPORT_LINE_BIT`) для строки записи или набора строк, заданных битовым буфером. Должна вызываться внутри транзакции.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Reference / Integer | Да | Объект записи CEMERec (ссылка) или номер записи в базе данных (целое число) |
| args[1] | Integer / Reference | Условно | Номер строки записи (целое число) или битовый буфер CIBitBuffer (ссылка). Обязателен, если args[0] задан как номер записи. Если передан битовый буфер, флаг устанавливается для всех строк, присутствующих в буфере |

**Возвращает**: пустое значение.

```EME-L
' Пометить строку 5 записи 100 к архивации
is_eme_arc(100, 5)

' Пометить все строки из битового буфера
is_eme_arc(rec, bitBuffer)
```

---

### is_eme_unarc — снятие флага архивации

снимает флаг архивации (бит `TO_ARCHIVE_EXPORT_LINE_BIT`) со строки записи. Должна вызываться внутри транзакции.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Reference / Integer | Да | Объект записи CEMERec (ссылка) или номер записи в базе данных (целое число) |
| args[1] | Integer | Условно | Номер строки записи. Обязателен, если args[0] задан как номер записи. Не используется, если args[0] — объект CEMERec |

**Возвращает**: пустое значение.

```EME-L
' Снять пометку архивации со строки 5 записи 100
is_eme_unarc(100, 5)

' Снять пометку по объекту CEMERec
is_eme_unarc(rec)
```

---

### is_eme_arc_deleted — архивация удалённых строк

помечает к архивации все удалённые (невалидные) строки указанной записи, устанавливая бит `TO_ARCHIVE_EXPORT_LINE_BIT` в ядерном поле флагов. Должна вызываться внутри транзакции.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Reference / Integer | Да | Объект записи CEMERec (ссылка) или номер записи в базе данных (целое число) |

**Возвращает**: пустое значение.

```EME-L
' Пометить все удалённые строки записи 100 к архивации
is_eme_arc_deleted(100)
```

---

### is_eme_arc_all — архивация всех строк записи

помечает к архивации все строки указанной записи, устанавливая бит `TO_ARCHIVE_EXPORT_LINE_BIT` в ядерном поле флагов для каждой строки. Должна вызываться внутри транзакции.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Reference / Integer | Да | Объект записи CEMERec (ссылка) или номер записи в базе данных (целое число) |

**Возвращает**: пустое значение.

```EME-L
' Пометить все строки записи 100 к архивации
is_eme_arc_all(100)
```

---

### is_eme_unarc_all_records — сброс флагов архивации

снимает флаг архивации (бит `TO_ARCHIVE_EXPORT_LINE_BIT`) со всех строк всех записей базы данных. в системе EME.WMS используется для сброса пометок перед началом процедуры пометки строк к архивации. Должна вызываться внутри транзакции.

**Параметры**: нет.

**Возвращает**: пустое значение.

```EME-L
' Сбросить все флаги архивации по всей базе данных
is_eme_unarc_all_records()
```

---

## Удаление документов

### is_delete_documents — удаление закрытых документов

удаляет все закрытые документы WMS, строки документов, пачки заказов, заказы, партии, ГТД и связанные подчинённые записи, у которых дата закрытия меньше или равна указанной дате. в языке EME-L функция работает только в MONO-режиме на мозговом компьютере. Перед удалением отключает отложенное обнуление ссылок, выполняет удаление внутри серверной транзакции, а затем восстанавливает отложенное обнуление ссылок. После удаления выполняет архивную выгрузку базы данных.

**Ограничения:**
- Функция доступна только в MONO-режиме работы станции. В противном случае генерируется ошибка «Архивация БД возможна только в МОНО режиме»
- Функция доступна только на мозговом компьютере (canal brain computer). В противном случае генерируется ошибка «Архивация БД возможна только на мозговом компьютере»

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Date | Да | Дата, до которой (включительно) удаляются закрытые документы и связанные объекты |
| args[1] | String | Нет | Путь для сохранения архивной выгрузки базы данных после удаления. Если не указан или пустая строка, используется стандартный путь архивной выгрузки |

**Возвращает**: пустое значение.

```EME-L
' Удалить все документы, закрытые до 01.01.2024
is_delete_documents(Date(2024, 1, 1))

' С указанием пути архивной выгрузки
is_delete_documents(Date(2024, 1, 1), "C:\Archive\export")
```

---

## EANCOM (EDI-интеграция)

### is_import_EANCOM_messages — импорт/экспорт EANCOM-сообщений

выполняет импорт входящих и экспорт исходящих сообщений EANCOM. Читает сообщения из внешних каталогов и внутренней файловой системы EME, передаёт их парсеру EANCOM и архивирует обработанные файлы.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Reference | Нет | Ссылка на объект EME-L класса-обработчика. Если передан, используется для определения целевой папки во внутренней файловой системе через метод GetInternalFolderName |

**Возвращает**: пустое значение.

```EME-L
' Импорт-экспорт без обработчика
is_import_EANCOM_messages()

' С объектом-обработчиком для определения папки
is_import_EANCOM_messages(myHandler)
```

---

### is_EANCOM_name — код EANCOM-сообщения

определяет код EANCOM-сообщения из переданного текста или файла. Функция последовательно ищет служебные сегменты UNA (опционально), UNB и UNH, извлекая из UNH код сообщения (значение до символа ':').

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Путь к файлу с сообщением EANCOM или непосредственно текст сообщения. Если строка короче 512 символов и соответствует существующему файлу, содержимое считывается из файла |

**Возвращает**: `String` — код EANCOM-сообщения. Если сегменты UNB или UNH отсутствуют, возвращается пустая строка.

```EME-L
code = is_EANCOM_name("UNA:+.? 'UNB+UNOC:2...")
' code = "DESADV"

code = is_EANCOM_name("C:\Messages\incoming.edi")
```

---

### is_EANCOM_segment — извлечение сегмента EANCOM

извлекает указанный сегмент из EANCOM-сообщения (файла или строки) и сохраняет его элементы данных (DE) в переданную коллекцию CICollection. Если элемент данных состоит из компонентов (CE, разделённые ':'), он сохраняется как массив CIArray с ключом «Array». в системе EME.WMS используется для парсинга входящих EDI-сообщений.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Reference | Да | Объект CICollection для сохранения распарсенных элементов сегмента |
| args[1] | String | Да | Путь к файлу с сообщением EANCOM или текст сообщения. Если строка короче 512 символов и соответствует существующему файлу, данные читаются из файла |
| args[2] | String | Да | Идентификатор искомого сегмента, например «UNH» или «LIN» |
| args[3] | Integer | Нет | Порядковый номер вхождения сегмента с указанным именем, начиная с 0. По умолчанию 0 |

**Возвращает**: `Boolean` — `TRUE` если сегмент найден и успешно разобран, `FALSE` если сегмент не найден.

```EME-L
' Извлечь первый сегмент LIN из файла
Dim coll = New CICollection()
If (is_EANCOM_segment(coll, "C:\Messages\order.edi", "LIN"))
    ' coll содержит элементы сегмента LIN'
End If

' Извлечь второй сегмент LIN (индекс 1)
If (is_EANCOM_segment(coll, messageText, "LIN", 1))
    ' Второй LIN-сегмент обработан'
End If
```

---

### is_goods_item_code_to_EME — форматирование кода товара для EME

форматирует код товара из входящего EANCOM-сообщения для использования в EME: удаляет ведущие нули слева до достижения минимальной длины кода (задаётся настройкой EANCOM.Минимальная длина кода, по умолчанию 8 символов). Если код состоял только из нулей, возвращает пустую строку.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Исходный код товара из входящего сообщения |

**Возвращает**: `String` — отформатированный код товара. Если исходный код содержал только нули, возвращается пустая строка.

```EME-L
code = is_goods_item_code_to_EME("0000123456")
' code = "123456" (при минимальной длине 8)

code = is_goods_item_code_to_EME("00000000")
' code = "" (все нули)
```

---

### is_goods_item_code_to_SAP — форматирование кода товара для SAP

форматирует код товара для исходящего EANCOM-сообщения (SAP): дополняет код ведущими нулями слева до минимальной длины, заданной в настройке EANCOM.Минимальная длина кода (по умолчанию 8 символов).

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Код товара для отправки в исходящем сообщении |

**Возвращает**: `String` — отформатированный код товара, выровненный нулями слева до минимальной длины.

```EME-L
code = is_goods_item_code_to_SAP("123456")
' code = "00123456" (при минимальной длине 8)

code = is_goods_item_code_to_SAP("ABC")
' code = "00000ABC"
```

---

## Управление органами диалогов (GUI)

### is_read_only — проверка флага «только чтение»

GUI-функция. Проверяет, установлен ли для указанного органа управления флаг READ_ONLY (только для чтения). Если имя органа не указано, проверяется текущий орган управления в контексте редактирования.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Нет | Имя органа управления для проверки. Если не задано, используется текущий орган управления |

**Возвращает**: `Boolean` — `TRUE` если флаг READ_ONLY установлен, `FALSE` если не установлен.

```EME-L
If (is_read_only("EditField1"))
    ' Орган только для чтения'
End If

' Проверка текущего органа
If (is_read_only())
    ' Текущий орган в режиме «только чтение»'
End If
```

---

### is_selected — проверка выбора радиокнопки/флажка

GUI-функция. Проверяет, выбрана ли указанная радиокнопка или установлен ли указанный флажок в группе органов управления.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Имя группы радиокнопок или флажков |
| args[1] | Integer | Да | Номер радиокнопки или флажка для проверки |
| args[2] | Integer | Нет | Уровень иерархии диалога. 0 — текущий диалог (по умолчанию). Для веб-контекста допустимы только значения -1 и 0 |

**Возвращает**: `Boolean` — `TRUE` если радиокнопка выбрана или флажок установлен, `FALSE` если нет.

```EME-L
If (is_selected("RadioGroup", 2))
    ' Выбрана вторая радиокнопка'
End If
```

---

### is_edit_data — содержимое органа управления

GUI-функция. Возвращает содержимое указанного органа управления или колонки таблицы с учётом его атрибутов и привязки к данным. Поддерживает чтение через реляционные пути и привязанные органы управления.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Нет | Имя органа управления или колонки таблицы. Если не задано, используется текущий орган управления |
| args[1] | Integer | Нет | Уровень иерархии объекта/диалога. 0 — текущий объект (по умолчанию) |
| args[2] | String | Нет | Реляционный путь для разыменования ссылочного поля. Если задан непустой путь, выполняется переход по ссылкам БД |

**Возвращает**: тип зависит от атрибута поля — строка, целое число, вещественное число, дата, время, `Boolean` или ссылка.

```EME-L
' Чтение значения текущего органа
value = is_edit_data()

' Чтение значения колонки таблицы
name = is_edit_data("NameColumn")

' Чтение с реляционным путём
refName = is_edit_data("Field1", 0, "Name")
```

---

### is_sum — сумма по колонке/ячейке

GUI-функция. Вычисляет сумму значений по указанной колонке таблицы или по указанной ячейке отчёта с накоплением по всем строкам/элементам отчёта. в контексте таблицы суммирует значения колонки. в контексте отчёта суммирует данные по всем экземплярам ячейки вертикального банда.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Имя ячейки отчёта или колонки таблицы |
| args[1] | Boolean | Нет | Признак округления перед суммированием. `TRUE` — округлять в соответствии с точностью поля (по умолчанию). `FALSE` — суммировать без округления |

**Возвращает**: `Float` или `Integer` (в зависимости от атрибута поля) — сумму значений. Если данных нет, возвращает 0.

```EME-L
total = is_sum("AmountColumn")
' total = сумма по всем строкам колонки AmountColumn

' Без округления
total = is_sum("AmountColumn", FALSE)
```

---

### is_focus — проверка фокуса ввода

GUI-функция. Проверяет, находится ли указанный орган управления в фокусе ввода.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Нет | Имя органа управления. Если не задано, проверяется текущий орган управления |

**Возвращает**: `Boolean` — `TRUE` если орган управления имеет фокус ввода, `FALSE` если нет.

```EME-L
If (is_focus("EditField1"))
    ' Фокус на EditField1'
End If
```

---

### is_modified — признак изменения органа

GUI-функция. Возвращает признак того, было ли изменено содержимое указанного органа управления или колонки таблицы пользователем.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Нет | Имя органа управления или колонки таблицы. Если не задано, проверяется текущий орган управления |
| args[1] | Integer | Нет | Уровень иерархии объекта/диалога. 0 — текущий объект (по умолчанию) |

**Возвращает**: `Boolean` — `TRUE` если содержимое было изменено, `FALSE` если нет.

```EME-L
If (is_modified("QuantityColumn"))
    ' Значение колонки было изменено пользователем'
End If
```

---

### is_tree_line — номер строки узла дерева

GUI-функция. Возвращает номер строки (DB line) текущего или указанного узла дерева. Поддерживает навигацию по родительским узлам на заданное количество уровней вверх.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Нет | Софтовое имя органа управления «Дерево». Если не задано, используется дерево, встроенное в текущий диалог |
| args[1] | Integer | Нет | Уровень иерархии диалога с деревом. 0 — текущий диалог (по умолчанию) |
| args[2] | Integer | Нет | Смещение по иерархии узлов относительно текущего выбранного. Поддерживаются только отрицательные значения (родительские уровни). 0 — текущий узел (по умолчанию) |
| args[3] | Boolean | Нет | Игнорировать отсутствие родительского узла. `TRUE` — вернуть `NULL_REF` при отсутствии родителя. `FALSE` — вызвать ошибку (по умолчанию) |

**Возвращает**: `Integer` — номер строки элемента дерева. `NULL_REF` — если ничего не выбрано или при args[3]=`TRUE` отсутствует родительский узел.

```EME-L
' Строка текущего узла дерева
line = is_tree_line()

' Строка родительского узла (на 1 уровень вверх)
parentLine = is_tree_line(, , -1)

' Строка родителя с обработкой отсутствия
parentLine = is_tree_line("MyTree", 0, -1, TRUE)
' parentLine = NULL_REF если родителя нет
```

---

### is_tree_rec — номер записи узла дерева

GUI-функция. Возвращает номер записи (rec) текущего выбранного элемента дерева.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Нет | Софтовое имя органа управления «Дерево». Если не задано, используется дерево, встроенное в текущий диалог |

**Возвращает**: `Integer` — номер записи элемента дерева.

```EME-L
rec = is_tree_rec()
' rec = номер записи текущего выбранного узла

rec = is_tree_rec("MyTree")
' rec = номер записи выбранного узла дерева MyTree
```

---

### is_recursion_ref — ссылка на родительский узел дерева

GUI-функция. Возвращает ссылку (DB line) на родительский узел текущего выбранного элемента в диалоге с управляющим органом «Дерево». в языке EME-L предназначена для использования при добавлении новой строки (NEW_LINE_MARK), когда требуется определить родителя для рекурсивной вставки.

**Параметры**: нет.

**Возвращает**: `Integer` — ссылка на родительский узел дерева. `NULL_REF` — если дерево отсутствует, ничего не выбрано, или у текущего узла нет родителя.

```EME-L
parentRef = is_recursion_ref()
If (parentRef <> NULL_REF)
    ' Есть родительский узел для рекурсивной вставки'
End If
```

---

### is_get_key_full_text — текстовое представление горячей клавиши

GUI-функция. Преобразует числовой код горячей клавиши, полученный методом `CONTROL_KEYDOWN_NOTIFY_EX::GetKeyFullCode`, в текстовое представление (аналог метода GetKeyFullText).

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Числовой код горячей клавиши |

**Возвращает**: `String` — текстовое представление комбинации клавиш. Пустая строка — если код не распознан или функциональность недоступна.

```EME-L
text = is_get_key_full_text(keyCode)
' text = "Ctrl+Shift+F" (пример)
```

---

### is_show_ctrl — видимость органа управления

GUI-функция. Изменяет видимость указанного органа управления в диалоге.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Имя органа управления в диалоге |
| args[1] | Boolean | Да | Признак видимости. `TRUE` — показать, `FALSE` — скрыть |

**Возвращает**: пустое значение.

```EME-L
' Скрыть орган управления
is_show_ctrl("ExtraField", FALSE)

' Показать орган управления
is_show_ctrl("ExtraField", TRUE)
```

---

## Печать и PDF

### is_find_printer — поиск принтера по имени

выполняет поиск принтера в системе по части его имени. Возвращает полное имя найденного принтера или пустую строку, если принтер не найден.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Фрагмент имени принтера для поиска |

**Возвращает**: `String` — полное имя найденного принтера. Пустая строка если принтер не найден.

```EME-L
printer = is_find_printer("Zebra")
' printer = "ZDesigner ZT411-300dpi ZPL" (пример)

If (is_find_printer("HP") = "")
    ' Принтер не найден'
End If
```

---

### is_join_pdf — объединение PDF-файлов

объединяет несколько PDF-файлов в один выходной документ. в языке EME-L для объединения используется внутренняя реализация на базе Ghostscript. Входные файлы передаются в виде массива строк.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Имя выходного PDF-файла |
| args[1] | Array | Да | Массив строк с путями к входным PDF-файлам |

**Возвращает**: `Integer` — 1 при успехе, 0 при ошибке объединения, 9 если в массиве менее двух элементов.

```EME-L
Dim files = New CIArray()
files.Add("C:\Docs\page1.pdf")
files.Add("C:\Docs\page2.pdf")
files.Add("C:\Docs\page3.pdf")

result = is_join_pdf("C:\Docs\combined.pdf", files)
' result = 1 при успехе
```

---

### is_create_blank_pdf — создание пустого PDF

создаёт пустой PDF-документ с указанным именем. При необходимости использует существующий файл в качестве образца для формирования структуры документа.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Имя создаваемого PDF-файла |
| args[1] | String | Нет | Имя файла-образца для копирования структуры документа. По умолчанию пустая строка |

**Возвращает**: `Integer` — 1 при успешном создании, 0 при ошибке.

```EME-L
result = is_create_blank_pdf("C:\Docs\blank.pdf")
' result = 1 при успехе

' С файлом-образцом
result = is_create_blank_pdf("C:\Docs\new.pdf", "C:\Docs\template.pdf")
```

---

### is_getnoofpages_pdf — количество страниц PDF

возвращает общее количество страниц в указанном PDF-документе.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Имя PDF-файла для анализа |

**Возвращает**: `Integer` — количество страниц в документе.

```EME-L
pages = is_getnoofpages_pdf("C:\Docs\report.pdf")
' pages = 12 (пример)
```

---

### is_get_pdfimages — извлечение страниц PDF в изображения

извлекает страницы из PDF-файла и сохраняет их в виде изображений в формате JPEG в указанный контейнер данных. в системе EME.WMS поддерживаемые типы контейнеров: CIArchive, CICollection, CIArray и CIDataStorage. При использовании CIDataStorage сохраняется только одна страница.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Имя исходного PDF-файла |
| args[1] | Reference | Да | Контейнер для сохранения изображений: CIArchive (Store), CICollection (элементы по номеру страницы), CIArray (объекты CIDataStorage), CIDataStorage (одна страница) |
| args[2] | Integer | Нет | Номер начальной страницы. 0 — с первой (по умолчанию) |
| args[3] | Integer | Нет | Номер последней страницы. 0 — до последней (по умолчанию) |
| args[4] | Integer | Нет | Разрешение в DPI. По умолчанию 300 |
| args[5] | Integer | Нет | Коэффициент сглаживания: 1 — минимальное, 2 — среднее, 4 — максимальное (по умолчанию) |

**Возвращает**: `Integer` — количество извлечённых страниц при успехе. Отрицательное значение при ошибке Ghostscript.

```EME-L
' Извлечь все страницы в CIArchive
Dim archive = New CIArchive()
count = is_get_pdfimages("C:\Docs\report.pdf", archive)
' count = количество извлечённых страниц

' Извлечь страницы 1-3 с разрешением 150 DPI
count = is_get_pdfimages("C:\Docs\report.pdf", archive, 0, 2, 150)
```

---

## Мониторинг заказов (табло)

### is_order_monitor — информация о заказе для табло

возвращает JSON-строку с детальной информацией об одном заказе для табло мониторинга. Формирует объект с полями: идентификатор заказа, дата расчёта, клиент, адрес доставки, общее количество, собранное количество, грузчик, статус, упакованное количество и количество заказов. Учитывает только просчитанные заказы (статус >= 200), исключая отклонённые.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Ссылка на заказ в базе данных. Должен быть валидным идентификатором существующего заказа |
| args[1] | Boolean | Нет | Признак пересчёта количества в литры/вес. `TRUE` — пересчитывать с учётом веса нетто товара. `FALSE` — базовое количество в единицах хранения (по умолчанию) |

**Возвращает**: `String` — JSON-объект с полями `{"id", "calcDate", "customer", "address", "total", "collected", "loaderMan", "status", "qtyPacked", "TotalOrders"}`. Пустая строка при невалидном заказе, отклонённом заказе или статусе < 200.

```EME-L
json = is_order_monitor(orderRef)
' json = {"id":123,"calcDate":"2024-01-15","customer":"ООО Вектор",...}

' С пересчётом в литры
json = is_order_monitor(orderRef, TRUE)
```

---

### is_all_orders_monitor — статус всех заказов для табло

возвращает JSON-массив со статусами всех активных заказов для табло мониторинга. в начале массива добавляется объект с версией метапроекта (major.minor). Включает только просчитанные заказы (статус >= 200), исключая отклонённые, которые ещё не покинули склад. При указании склада фильтрует заказы по конкретному складу.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Boolean | Да | Признак пересчёта количества в литры/вес. `TRUE` — пересчитывать с учётом веса нетто товара. `FALSE` — базовое количество в единицах хранения |
| args[1] | Integer | Нет | Ссылка на склад для фильтрации. 0 или `NULL_REF` — все склады (по умолчанию). Ненулевое значение — только заказы указанного склада |

**Возвращает**: `String` — JSON-массив объектов `[{"version":"X.Y"}, {...заказ1...}, {...заказ2...}]`. `"[]"` при отсутствии заказов.

```EME-L
' Все заказы в единицах хранения
json = is_all_orders_monitor(FALSE)

' Заказы конкретного склада в литрах
json = is_all_orders_monitor(TRUE, warehouseRef)
```

---

## Производительность и питание

### is_calc_speed — измерение производительности компьютера

измеряет производительность компьютера путём выполнения тестового набора арифметических операций на языке EME-L и сравнения фактического времени выполнения с эталонным значением. При необходимости использует все доступные ядра процессора для параллельного измерения.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Boolean | Да | Признак использования всех ядер процессора. `TRUE` — параллельное измерение на всех ядрах, результат усредняется. `FALSE` — измерение на одном ядре |

**Возвращает**: `Integer` — производительность в процентах от рекомендованной. 100% соответствует эталону (время выполнения теста 11.5 секунд). Больше 100% — выше эталона, меньше — ниже.

```EME-L
' Замер на одном ядре
speed = is_calc_speed(FALSE)
' speed = 150 — компьютер в 1.5 раза быстрее эталона

' Замер на всех ядрах (усреднённый результат)
speed = is_calc_speed(TRUE)
```

---

### is_switch_to_high_performance_power_scheme — режим высокой производительности

переключает текущую схему управления питанием ОС Windows на режим «Высокая производительность» (High Performance power scheme). в системе EME.WMS используется на рабочих станциях и серверах для предотвращения снижения тактовой частоты процессора.

**Параметры**: нет.

**Возвращает**: пустое значение.

```EME-L
' Переключить питание на высокую производительность
is_switch_to_high_performance_power_scheme()
```

---

## Профилирование и отладка

### is_prf — профилирование

функция для профилирования кода. Аналог ядерных функций профилирования.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | Тип: 0 — reset_prf, 1 — start_prf, 2 — stop_prf, 3 — get_prf |
| args[1] | Integer | Нет | Номер таймера (0-31). Для args[0]=0 не нужен |

**Возвращает**: при args[0]=3 — время в мс. В остальных случаях — ничего.

```EME-L
is_prf(1, 0)   'запуск таймера 0
is_prf(2, 0)   'остановка
time = is_prf(3, 0)  'получить время
```



### is_mem_dump — дамп памяти

записывает в катастроф-лог информацию об использовании памяти.

**Параметры**: нет.

**Возвращает**: ничего.

```EME-L
is_mem_dump()
```



### is_SYS_mem_dump — дамп пула SYS

выполняет дамп пула SYS.

**Параметры**: нет.

**Возвращает**: пустое значение.

```EME-L
is_SYS_mem_dump()
```



### is_ui_test_mode — режим тестирования UI

возвращает TRUE, если программа в режиме тестирования интерфейса. В этом режиме база последовательно запускает все пункты меню. Прикладной код может использовать для отключения браузеров и MessageBox.

**Параметры**: нет.

**Возвращает**: `Boolean` — TRUE если включён режим тестирования.

```EME-L
If (is_ui_test_mode())
    'Не показываем MessageBox'
End If
```



## Планировщик и события

### is_eventer_tick — запуск планировщика

запускает процедуры планировщика.

**Параметры**: нет.

**Возвращает**: `Integer` — результат отправки WM_TIMER, 0 при ошибке.

```EME-L
is_eventer_tick()
```



### is_force_standard_export — стандартный экспорт

Инициирует принудительный стандартный экспорт данных. Вызов QueryExport.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Boolean | Нет | TRUE — выдавать предупреждение перед экспортом (по умолчанию TRUE) |

**Возвращает**: `Integer` — результат запроса на экспорт. При отсутствии поддержки семафоров возвращает 0.

```EME-L
is_force_standard_export(TRUE)
```



## Прочее

### is_generate_NSI_script — генерация инсталляционного скрипта

генерирует инсталляционный скрипт.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Нет | Постфикс имени скрипта |
| args[1] | Integer | Нет | 0 — использовать текущий каталог, 1 — стандартные папки |
| args[2] | String | Нет | Путь к папке данных |
| args[3] | String | Нет | Путь к папке метапроекта |
| args[4] | String | Нет | Путь к папке ядра |
| args[5] | String | Нет | Имя выходного файла |
| args[6] | String | Нет | Путь к файлу лицензии |

**Возвращает**: ничего.

```EME-L
is_generate_NSI_script()
```



### is_OLE_serverbusydelay — задержка Server Busy

устанавливает задержку появления диалога OLE Server Busy.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Нет | Задержка в мс (по умолчанию 5000) |

**Возвращает**: `Boolean` — TRUE при успехе, FALSE если фильтр недоступен.

```EME-L
is_OLE_serverbusydelay(10000)
```



### is_set_method_find_mode — режим поиска методов

устанавливает режим поиска методов объектов для текущего контекста. Решает проблему привязки номеров методов к переменной при смене объекта.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | Integer | Да | 0 — стандартный режим, 1 — FindMethod перед каждым CallMethod |

**Возвращает**: ничего.

```EME-L
is_set_method_find_mode(1)
```



### is_get_data_path — путь к папке данных

возвращает путь к папке данных текущего проекта. Вызов GetProjectDataDir.

**Параметры**: нет.

**Возвращает**: `String` — путь к папке данных.

```EME-L
path = is_get_data_path()
```



### robot — ссылка на робота

возвращает ссылку на текущего выполняющегося робота (CIRobot). Глобальный объект, доступный из EME-L без явной передачи параметра.

**Параметры**: нет.

**Возвращает**: ссылку на объект робота (ссылка).

```EME-L
r = robot()
```



### is_enable_dataslice — включение срезов

включает режим рабочей станции для работы со срезами данных.

**Параметры**: нет.

**Возвращает**: `Boolean` — TRUE при успехе, FALSE если условия не выполнены.

```EME-L
If (is_enable_dataslice())
    'Срезы включены'
End If
```



### is_restart_as_server_request — запрос на переключение сервера

отправляет на резервный сервер запрос на переключение в режим сервера БД. IP адрес берёт из настройки рабочей станции.

**Параметры**: нет.

**Возвращает**: `String` — текст ошибки или пусто при успехе.

```EME-L
err = is_restart_as_server_request()
```



### is_sys_import — системный импорт

выполняет "ручной" системный импорт по файлам из указанной папки.

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| args[0] | String | Да | Путь к папке с файлами импорта |

**Возвращает**: `Integer` — результат выполнения (код EEXPORTRESULT).

```EME-L
result = is_sys_import("C:\\import\\")
```



См. также: [EME_L_Syntax.md](EME_L_Syntax.md) — синтаксис EME-L, [EME_L_IS_Functions.md](EME_L_IS_Functions.md) — is-функции групп Bit/Browser/Context, [EME_L_Classes.md](EME_L_Classes.md) — объектная модель.
