---
title: "Класс ItemsOfDispatcher"
sidebar:
  order: 71
---

# Класс ItemsOfDispatcher

Класс `ItemsOfDispatcher` в языке EME-L — класс-оболочка для формирования иерархических наборов элементов диспетчера терминалов сбора данных в интеграторе DBIntegrator. Производный от `Items`, предназначен для использования в EME-L-скриптах диспетчера терминалов.

Предоставляет методы для создания элементов дерева, настройки их свойств (привязка к записям `CEMERec`, ссылки на терминалы, приоритеты очереди приказов, произвольные пользовательские поля), а также для управления фильтрацией и сортировкой элементов. Поддерживает операции привязки, отвязки и удаления приказов из очереди терминалов, заполнения массивов ссылок на строки очереди приказов для отображения в браузерах.

Используется для построения пользовательского интерфейса диспетчера терминалов, включая формирование древовидных структур заказов, документов, пачек и приказов на перемещение.

Объект класса `ItemsOfDispatcher` создаётся через `Object()` с обязательным указанием имени EME-L-класса-обработчика и подключается к интегратору DBIntegrator методом `AttachToIntegrator`, унаследованным от базового класса `Items`. Все методы настройки свойств воздействуют на **последний добавленный** элемент (через `AddAndFillItem`), если явно не указано иное.

## Создание объекта

Класс `ItemsOfDispatcher` имеет два конструктора — на одно и на два аргумента.

```EME-L
'Конструктор с указанием имени EME-L-класса-обработчика'
objRootItems = Object("ItemsOfDispatcher", "LeftRootDocsItems");
objRootItems.AttachToIntegrator(IntegratorSRC);
```

```EME-L
'Конструктор с указанием класса-обработчика и произвольных данных для OnInit()'
objRootItems = Object("ItemsOfDispatcher", "RightRootTerminalsItems", m_FilterOption);
objRootItems.AttachToIntegrator(IntegratorDST);
```

Второй аргумент конструктора (`данные`) передаётся в функцию `OnInit()` EME-L-класса-обработчика при инициализации и может иметь любой тип — целое число, строка, объект. В реальных скриптах диспетчера через него обычно передают код режима фильтрации левой или правой панели.

## Добавление элементов

Метод `AddAndFillItem` добавляет новый элемент в дерево DBIntegrator и автоматически заполняет его свойства на основе контекста. Автоматика проставляет ссылку на терминал (`__VTR__`), если переданный объект является записью `DataPreparationTerminals` или если такая ссылка уже есть в родительском элементе.

| Метод | Аргументы | Возвращает | Описание |
|-------|-----------|------------|----------|
| `AddAndFillItem(arg0, arg1)` | arg0: String (текст), arg1: String (классЭлемента) | Empty | Добавляет элемент с отображаемым текстом и именем класса дочернего элемента для следующего уровня дерева. Ссылка на терминал наследуется от родителя. |
| `AddAndFillItem(arg0, arg1, arg2)` | + arg2: CEMERec (запись, по ссылке) | Empty | Дополнительно извлекает из переданной записи имя класса CEMERec (свойство `__REC__`) и номер строки (`__REF__`) для последующего открытия связанных документов. |
| `AddAndFillItem(arg0, arg1, arg2, arg3)` | + arg3: Integer (строкаОчереди) | Empty | Дополнительно устанавливает прямую ссылку на строку очереди приказов (`__QOL__`). Значение меньше 0 игнорируется. Если элемент конечный, интегратор выполнит пропуск браузера и автоматическую привязку/отвязку приказов. |

## Управление свойствами элемента

Методы этой группы читают и пишут свойства **последнего добавленного** элемента (затрагиваемого `AddAndFillItem`). Каждое свойство хранится во внутренней категории элемента DBIntegrator. Сеттеры возвращают **предыдущее** значение свойства, что позволяет восстановить его при необходимости.

### Привязка к записи и терминалу

| Метод | Аргументы | Возвращает | Описание |
|-------|-----------|------------|----------|
| `SetImplRecordName(arg0)` | arg0: String | String | Задаёт имя класса CEMERec для свойства `__REC__` (привязка документов). Возвращает прежнее значение. |
| `GetImplRecordName()` | — | String | Имя класса CEMERec из свойства `__REC__`. Пустая строка, если не задано. |
| `GetCurrentRecord()` | — | Integer | Константа типа записи по свойству `__REC__` (см. «Константы типа записи»). |
| `SetValidTerminalRef(arg0)` | arg0: Integer | Integer | Задаёт ссылку на запись терминала сбора данных (`__VTR__`). Возвращает прежнее значение или `NULL_REF`. |
| `GetValidTerminalRef()` | — | Integer | Ссылка на терминал (`__VTR__`) или `NULL_REF`, если не задано. |

### Очередь приказов

| Метод | Аргументы | Возвращает | Описание |
|-------|-----------|------------|----------|
| `SetFirstQueuePriority(arg0)` | arg0: Real | Real | Задаёт приоритет в очереди приказов (`__FQP__`). Принимает **только** значения типа Real (`VT_R4`/`VT_R8`); при передаче другого типа генерирует ошибку и возвращает `0.0`. Возвращает заданное значение. |
| `GetFirstQueuePriority()` | — | Real | Текущий приоритет (`__FQP__`) или `0.0`, если не задано. |
| `SetQueueOrdersLine(arg0)` | arg0: Integer | Integer | Прямая привязка строки очереди приказов (`__QOL__`). Возвращает прежнее значение или `NULL_REF`. **Внимание:** если свойство установлено, переопределить в EME-L функции привязки, отвязки и удаления приказов невозможно. |
| `GetQueueOrdersLine()` | — | Integer | Ссылка на строку очереди приказов (`__QOL__`) или `NULL_REF`. |
| `SetDispOrderType(arg0)` | arg0: Integer | Integer | Тип отображаемых приказов для группировки (`__DOT__`). Возвращает прежнее значение или `NULL_REF`. |
| `GetDispOrderType()` | — | Integer | Тип приказов (`__DOT__`) или `NULL_REF`. |

### Пользовательские поля и сортировка

| Метод | Аргументы | Возвращает | Описание |
|-------|-----------|------------|----------|
| `SetAnyUserField(arg0)` | arg0: String | String | Произвольное пользовательское поле (`__FLD__`). Возвращает прежнее значение. |
| `GetAnyUserField()` | — | String | Значение пользовательского поля или пустая строка. |
| `SetFieldForSort(arg0)` | arg0: String | String | Имя поля сортировки элемента (`__FFS__`). Возвращает прежнее значение. |
| `GetFieldForSort()` | — | String | Имя поля сортировки или пустая строка. |

### Фильтрация и произвольные свойства

| Метод | Аргументы | Возвращает | Описание |
|-------|-----------|------------|----------|
| `SetFilterMode(arg0)` | arg0: Integer | Integer | Режим фильтрации последнего добавленного элемента (`__MOD__`). Возвращает прежнее значение или `NULL_REF`. |
| `GetFilterMode()` | — | Integer | Режим фильтрации текущего элемента или `NULL_REF`. |
| `ReWriteFilterMode(arg0)` | arg0: Integer | Empty | Переопределяет режим фильтрации (`__MOD__`) **для всех элементов текущего уровня** дерева DBIntegrator, а не только последнего. |
| `SetField(arg0, arg1)` | arg0: String (имяКатегории), arg1: String (значение) | String | Запись произвольной категории свойства. Возвращает прежнее значение. |
| `GetField(arg0)` | arg0: String (имяКатегории) | String | Чтение произвольной категории свойства. Пустая строка, если не задано. |

## Внутренние механизмы привязки/удаления приказов

Класс реализует четыре операции, которые **вызывает сам интегратор DBIntegrator** при работе с деревом диспетчера (а не EME-L-скрипт напрямую): `AttachTerminal`, `DetachTerminal`, `DeleteFromRFDispatcher`, `IsExistQueueOrderWithTerminalForDelete`. Каждая операция сначала пытается делегировать выполнение EME-L-классу-обработчику через соответствующий callback (см. ниже «EME-L-обработчики (callbacks)»); если callback не задан или возвращает `-1`, выполняется стандартная реализация ядра. Эти методы не имеют записей `IMPLEMENT_METHOD` и потому **не вызываются напрямую из EME-L** — для скрипта доступны только методы настройки свойств (см. выше) и `FillQueueLinesArray`.

## EME-L-обработчики (callbacks)

При наступлении событий в дереве диспетчера ядро ищет в EME-L-классе-обработчике (указанном в конструкторе) методы с фиксированными именами. Все callbacks **необязательны**: при их отсутствии ядро использует стандартную реализацию. Соглашение о возврате: строка, трактуемая как целое; значение `-1` означает «использовать стандартную реализацию ядра», любое другое значение заменяет результат.

| Callback | Аргументы | Когда вызывается |
|----------|-----------|------------------|
| `OnFillQOLinesArray(this, массивСсылок)` | `this` — объект `ItemsOfDispatcher`; `массивСсылок` — `Array` по ссылке | Из `FillQueueLinesArray`, когда свойство `__QOL__` не задано напрямую. Должен заполнить массив ссылками на строки очереди приказов, относящихся к элементу. |
| `OnAttachTerminal(this, терминал, предСтрока, ошибки)` (4 арг.) или `OnAttachTerminal(this, терминал, предСтрока)` (3 арг.) | `this` — объект; `терминал` — строка записи терминала (Integer); `предСтрока` — позиция вставки (Integer); `ошибки` — `Map` по ссылке для накопления ошибок по строкам | При привязке элемента к терминалу. Ядро сначала пробует 4-аргументную сигнатуру, при отсутствии — 3-аргументную. |
| `OnDetachTerminal(this)` | `this` — объект | При отвязке элемента от терминала. |
| `OnDeleteFromRFDispatcher(this, массив)` (2 арг.) или `OnDeleteFromRFDispatcher(this)` (1 арг.) | `this` — объект; `массив` — `Array` по ссылке | При удалении элемента из диспетчера. Ядро сначала пробует 2-аргументную сигнатуру, при отсутствии — 1-аргументную. |

> Примечание: `this` передаётся в callback как сам объект `ItemsOfDispatcher`, что позволяет в EME-L-обработчике читать его свойства (`GetQueueOrdersLine()`, `GetValidTerminalRef()` и т. п.) и принимать решение о составе возвращаемых ссылок.

## Категории свойств элемента

Методы настройки записывают значения во внутреннюю строку категорий элемента в формате `__KEY__=value`, разделённых `\r\n`. Помимо категорий, управляемых явными методами, `AddAndFillItem` автоматически проставляет служебную категорию `__NGI__=1` (NoGroupItem — «схлопнуть группировочный элемент»), если унаследована от родителя или если переданный объект является записью `DataPreparationTerminals` и включена настройка `Предпочтения.Диспетчер терминалов.Схлопывать группировочные элементы в правой части`. Эта категория считывается интегратором при отрисовке дерева и напрямую из EME-L не переопределяется.

## Заполнение браузеров

| Метод | Аргументы | Возвращает | Описание |
|-------|-----------|------------|----------|
| `FillQueueLinesArray(arg0)` | arg0: Array (по ссылке) | Empty или String | Заполняет массив ссылками на строки очереди приказов текущего элемента. Если напрямую задано свойство `__QOL__` и терминал совпадает, добавляет только эту ссылку; иначе вызывает EME-L-метод `OnFillQOLinesArray`. Возвращает `Empty` при успехе или строку с ошибкой: `"Не задан метод EME-L"`, `"Не задан метод EME-L: OnFillQOLinesArray"`. |

## Константы типа записи

Метод `GetCurrentRecord()` возвращает одну из целочисленных констант C++ в зависимости от значения свойства `__REC__`. Имена констант соответствуют внутренним идентификаторам записей WMS; в EME-L они используются как целые значения для сравнения.

| Значение свойства `__REC__` | Константа C++ | Назначение |
|------------------------------|----------------|------------|
| `"Orders"` | `ORDER_HEADER_RECORD` | Заказ |
| `"OrdersBatch"` | `ORDERS_BATCH_RECORD` | Пачка заказов |
| `"Document"` | `DOCUMENTS_RECORD` | Документ |
| `"DataPreparationTerminals"` | `DATA_PREPARATION_TERMINALS_RECORD` | Терминал сбора данных |
| `"QueueOrders"` | `QUEUE_ORDERS_RECORD` | Строка очереди приказов |
| `"GoodsItem"` | `GOODS_ITEM_RECORD` | Товарная позиция |
| не задано / пусто | `NULL_RECORD` | Привязка отсутствует |

Сеттеры целочисленных свойств (`SetValidTerminalRef`, `SetQueueOrdersLine`, `SetDispOrderType`, `SetFilterMode`) возвращают константу `NULL_REF`, когда свойство не было задано ранее. `NULL_REF` в EME-L представляется целочисленным значением, используемым как «нет ссылки».

## Примеры

Создание корневого набора элементов и подключение к интегратору:

```EME-L
'objRootItems — корневой набор для левой панели диспетчера'
objRootItems = Object("ItemsOfDispatcher", "LeftRootDocsItems", nFilterOption);
objRootItems.AttachToIntegrator(IntegratorSRC);
```

Добавление дочернего элемента с привязкой к записи `OrdersBatch` и приоритетом очереди:

```EME-L
'Заполнить элемент пачки с автоматической привязкой к терминалу родителя'
objItems.AddAndFillItem(GetOrdersBatchItemText(r_OrdersBatch, -1, Count), "OrdersBatchItemsCons", r_OrdersBatch);
objItems.SetFirstQueuePriority(r_OrdersBatch.GetMoveOrder().GetQueueOrders().GetPriority());
```

Привязка конечного элемента к строке очереди приказов (4-аргументный `AddAndFillItem`):

```EME-L
'arg3 — номер строки очереди приказов; при значении < 0 свойство __QOL__ не устанавливается'
objItems.AddAndFillItem(r_QueueOrders.GetItemText(), "", r_QueueOrders, r_QueueOrders.GetLine());
objItems.SetFirstQueuePriority(dPriority);
```

Переопределение режима фильтрации для всего уровня дерева — типовой паттерн внутри `OnInit` класса-обработчика (`objItems` передаётся как аргумент):

```EME-L
'OnInit обработчика вызывается ядром после конструктора ItemsOfDispatcher'
OnInit(objItems As "ItemsOfDispatcher", FilterOption)
    'FilterOption — второй аргумент конструктора ItemsOfDispatcher'
    objItems.ReWriteFilterMode(FilterOption);
    'ReWriteFilterMode меняет __MOD__ у всех элементов уровня, а не только последнего'
    m_FilterOption = FilterOption;
End OnInit
```

> Помимо описанных методов, в обработчиках часто используются унаследованные от `Items` методы: `FindCategoryProperties`, `GetCategory`, `GetCategoryRef`, `SetCurrentCategory`, `SetCurrentIcon`, `GetItemText` — см. [класс Items](./items.md).

## См. также

- [Класс Items](./items.md) — базовый класс для наборов элементов DBIntegrator.
