---
title: "Класс Random"
sidebar:
  order: 90
---

# Класс Random

Класс `Random` в языке EME-L — генератор псевдослучайных значений различных типов. Предназначен для создания случайных целых и вещественных чисел, дат, а также строковых данных заданного семантического типа: слов, буквенно-цифровых комбинаций, числовых последовательностей, адресов, e-mail, интернет-адресов, имен, фамилий, отчеств, полных ФИО, названий компаний и номеров телефонов.

Поддерживает настройку диапазонов генерируемых числовых значений, границ случайных дат, минимальной и максимальной длины строк, типа формируемой строки и языка строковых данных (русский или английский). Объект создаётся без параметров. Генерация конкретных случайных значений выполняется методами `GetInt`, `GetDouble`, `GetDate` и `GetString`. Для удобства предусмотрены специализированные методы генерации строк фиксированного семантического типа: `GetWWWString`, `GetAlfaNumericString`, `GetNumericString`, `GetAddressString`, `GetEmailString`, `GetNameString`, `GetSurnameString`, `GetPatronymicNameString`, `GetFullnameString`, `GetCompanyString` и `GetPhoneString`.

Доступен только в версиях ядра EME.WMS 23.17 и выше (условие компиляции `LAST_VERSION >= MAKE_VERSION(23, 17)`).

Объект класса `Random` создаётся через `Object()` и не привязан к контексту диалога или браузера — это самостоятельный класс для генерации тестовых и прикладных данных.

## Создание объекта

Класс `Random` имеет один конструктор — без аргументов.

```EME-L
'Создаёт генератор случайных значений с начальной инициализацией'
random = Object("Random");
```

Конструктор без аргументов создаёт и инициализирует внутренний генератор псевдослучайных чисел, устанавливая количество генерируемых слов в строке равным 1.

## Генерация строк универсальным методом

| Метод | Аргументы | Возвращает | Описание |
|-------|-----------|------------|----------|
| `GetString()` | — | String | Случайное слово на русском языке, длина 4–25 символов. |
| `GetString(arg0)` | arg0: Integer | String | Слово фиксированной длины на русском языке. |
| `GetString(arg0)` | arg0: String | String | Строка указанного типа, длина по умолчанию 4–25 символов. |
| `GetString(arg0, arg1)` | arg0: Integer, arg1: Integer | String | Строка заданной длины (min–max) на русском языке, тип «Слова». |
| `GetString(arg0, arg1, arg2)` | arg0: Integer, arg1: Integer, arg2: String | String | Строка заданной длины и типа на русском языке. |
| `GetString(arg0, arg1, arg2, arg3)` | + arg3: String (язык) | String | Строка заданной длины, типа и языка. |

Параметры `arg0` и `arg1` поддерживают двойную семантику: целое число интерпретируется как длина строки, строка — как тип формируемых данных. Если длина не задана ни одним из целочисленных параметров, используется диапазон 4–25 символов. Язык строки применяется только на время данного вызова, после чего внутренний генератор возвращается к русскому языку.

Допустимые значения типа строки (`arg2` или переданного через `arg0`/`arg1`):

| Значение | Что генерируется |
|----------|------------------|
| `"Слова"` | Последовательность букв (по умолчанию) |
| `"Буквы+Цифры"` | Буквенно-цифровая комбинация |
| `"Буквы"` | Только буквы |
| `"Цифры"` | Только цифры |
| `"E-mail"` | Адрес электронной почты |
| `"WWW"` | Интернет-адрес |
| `"Адрес"` | Почтовый адрес |
| `"ФИО"` | Фамилия, имя и отчество |
| `"Фамилия"` | Фамилия |
| `"Имя"` | Имя |
| `"Отчество"` | Отчество |
| `"Фирма"` | Название компании |
| `"Телефон"` | Номер телефона |
| `"Double '%.3f'"` | Вещественное число с форматированием |
| `"Int '%d'"` | Целое число с форматированием |

Допустимые значения языка (`arg3`):

| Значение | Язык |
|----------|------|
| `"rus"` | Русский (по умолчанию) |
| `"eng"` | Английский |

## Генерация строк специализированных типов

Каждый метод ниже — это сокращение для вызова `GetString` с предустановленным семантическим типом. Все методы принимают до двух аргументов: длину и переопределение типа. Если длина меньше минимального порога для данного типа, используется диапазон по умолчанию.

| Метод | Аргументы | Диапазон по умолчанию | Минимум для точной длины | Описание |
|-------|-----------|----------------------|--------------------------|----------|
| `GetWWWString()` | — | 10–25 | 10 | Случайный интернет-адрес. |
| `GetWWWString(arg0)` | arg0: Integer | 10–25 | 10 | Интернет-адрес заданной длины. |
| `GetWWWString(arg0, arg1)` | + arg1: String/Integer | 10–25 | 10 | С переопределением типа. |
| `GetAlfaNumericString()` | — | 4–25 | 4 | Случайная буквенно-цифровая строка. |
| `GetAlfaNumericString(arg0)` | arg0: Integer | 4–25 | 4 | Буквенно-цифровая строка заданной длины. |
| `GetAlfaNumericString(arg0, arg1)` | + arg1: String/Integer | 4–25 | 4 | С переопределением типа. |
| `GetNumericString()` | — | 4–25 | — | Случайная строка из цифр. |
| `GetNumericString(arg0)` | arg0: Integer | 4–25 | — | Цифровая строка фиксированной длины. |
| `GetNumericString(arg0, arg1)` | arg0: Integer, arg1: Integer | 4–25 | — | Цифровая строка длины min–max. |
| `GetAddressString()` | — | 15–25 | 15 | Случайный почтовый адрес. |
| `GetAddressString(arg0)` | arg0: Integer | 15–25 | 15 | Адрес заданной длины. |
| `GetAddressString(arg0, arg1)` | + arg1: String/Integer | 15–25 | 15 | С переопределением типа. |
| `GetEmailString()` | — | 8–25 | 8 | Случайный адрес электронной почты. |
| `GetEmailString(arg0)` | arg0: Integer | 8–25 | 8 | E-mail заданной длины. |
| `GetEmailString(arg0, arg1)` | + arg1: String/Integer | 8–25 | 8 | С переопределением типа. |
| `GetNameString()` | — | 5–25 | 5 | Случайное имя. |
| `GetNameString(arg0)` | arg0: Integer | 5–25 | 5 | Имя заданной длины. |
| `GetNameString(arg0, arg1)` | + arg1: String/Integer | 5–25 | 5 | С переопределением типа. |
| `GetSurnameString()` | — | 5–25 | 5 | Случайная фамилия. |
| `GetSurnameString(arg0)` | arg0: Integer | 5–25 | 5 | Фамилия заданной длины. |
| `GetSurnameString(arg0, arg1)` | + arg1: String/Integer | 5–25 | 5 | С переопределением типа. |
| `GetPatronymicNameString()` | — | 5–25 | 5 | Случайное отчество. |
| `GetPatronymicNameString(arg0)` | arg0: Integer | 5–25 | 5 | Отчество заданной длины. |
| `GetPatronymicNameString(arg0, arg1)` | + arg1: String/Integer | 5–25 | 5 | С переопределением типа. |
| `GetFullnameString()` | — | 15–25 | 15 | Случайное полное ФИО. |
| `GetFullnameString(arg0)` | arg0: Integer | 15–25 | 15 | ФИО заданной длины. |
| `GetFullnameString(arg0, arg1)` | + arg1: String/Integer | 15–25 | 15 | С переопределением типа. |
| `GetCompanyString()` | — | 8–25 | 8 | Случайное название компании. |
| `GetCompanyString(arg0)` | arg0: Integer | 8–25 | 8 | Название компании заданной длины. |
| `GetCompanyString(arg0, arg1)` | + arg1: String/Integer | 8–25 | 8 | С переопределением типа. |
| `GetPhoneString()` | — | 8–25 | 8 | Случайный номер телефона. |
| `GetPhoneString(arg0)` | arg0: Integer | 8–25 | 8 | Номер телефона заданной длины. |
| `GetPhoneString(arg0, arg1)` | + arg1: String/Integer | 8–25 | 8 | С переопределением типа. |

## Генерация чисел и дат

| Метод | Аргументы | Возвращает | Описание |
|-------|-----------|------------|----------|
| `GetDouble()` | — | Real | Случайное вещественное число в диапазоне [0.0, 1.0]. |
| `GetDouble(arg0)` | arg0: Real | Real | Случайное число в диапазоне [0.0, arg0]. |
| `GetDouble(arg0, arg1)` | arg0: Real (min), arg1: Real (max) | Real | Случайное число в диапазоне [min, max]. Если min > max, используется [0.0, 1.0]. |
| `GetInt()` | — | Integer | Случайное целое число в диапазоне [0, 255]. |
| `GetInt(arg0)` | arg0: Integer | Integer | Случайное число в диапазоне [0, arg0]. |
| `GetInt(arg0, arg1)` | arg0: Integer (min), arg1: Integer (max) | Integer | Случайное число в диапазоне [min, max]. Если min > max, используется [0, 255]. |
| `GetDate(arg0, arg1)` | arg0: DateTime (min), arg1: DateTime (max) | DateTime | Случайная дата в заданном диапазоне. Если min > max, возвращается min. |

## Примеры

Генерация штрих-кода PDF417 для алкогольной марки (из `TerminalTester.txt`):

```EME-L
'Создаём генератор один раз в конструкторе класса'
Random = Object("Random");

'Цифровая часть кода фиксированной длины 20 символов'
numericPart = Random.GetNumericString(20);

'Буквенно-цифровая часть фиксированной длины 129 на английском языке'
alphaPart = is_upper(Random.GetString(129, 129, "Буквы+Цифры", "eng"));

'Сборка кода марки и запись в PDF417'
StampsCode = "1" + numericPart + alphaPart;
r_Stamps.PutPDF417(StampsCode);
```

Подбор уникального случайного кода для карточки сотрудника (из `Staff.txt`):

```EME-L
randoml = Object("Random");
dsStaff = Object("dsDB", "Staff");
value = 0;
While (true)
    'Случайный код в диапазоне 1000–99999'
    value = randoml.GetInt(1000, 99999);
    dsStaff.SetSkipMode();
    dsStaff.GetCodeFld().MustBeEQ(value);
    dsStaff.MustBeValid();
    dsStaff.SetFirstLine();
    If (~dsStaff.IsValidLine())
        Break;
    End If
End Loop
```

Генерация разнообразных данных через универсальный и специализированные методы:

```EME-L
random = Object("Random");

'Случайное ФИО на русском языке'
fullName = random.GetFullnameString();

'Случайный e-mail длиной 12 символов'
email = random.GetEmailString(12);

'Случайное вещественное число от 10.0 до 100.0'
price = random.GetDouble(10.0, 100.0);

'Случайная дата в диапазоне 2024–2025'
minDate = is_date(1, 1, 2024);
maxDate = is_date(31, 12, 2025);
randDate = random.GetDate(minDate, maxDate);
```

## См. также

- [Класс File](./file.md) — самостоятельный класс, также создаваемый через `Object()`.
- [Класс Archive](./archive.md) — бинарная сериализация данных.
