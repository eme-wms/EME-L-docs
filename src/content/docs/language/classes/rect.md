---
title: "Класс Rect"
sidebar:
  order: 94
---

# Класс Rect

Класс `Rect` в языке EME-L — класс для работы с прямоугольной областью в двумерной плоскости.

Представляет прямоугольник, заданный четырьмя целочисленными координатами: левой, верхней, правой и нижней. Поддерживает создание пустого прямоугольника, копирование из другого объекта `Rect` и инициализацию координатами.

Предоставляет методы для получения и изменения каждой стороны, вычисления ширины и высоты, а также проверки принадлежности точки области.

Используется в прикладных скриптах EME-L для работы с координатами, размерами и областями экрана или элементов интерфейса.

## Создание объекта

Класс `Rect` имеет три конструктора — по числу аргументов.

```EME-L
'Пустой прямоугольник, все координаты равны 0'
rect = Object("Rect");
```

```EME-L
'Копия существующего прямоугольника'
rectCopy = Object("Rect", rect);
```

```EME-L
'Прямоугольник с явными координатами границ'
rect = Object("Rect", 100, 100, 500, 500);
```

Конструктор без аргументов инициализирует все границы нулём. Конструктор с одним аргументом копирует координаты из переданного объекта `Rect`. Конструктор с четырьмя аргументами задаёт левую, верхнюю, правую и нижнюю координаты в указанном порядке.

## Границы прямоугольника

| Метод | Аргументы | Возвращает | Описание |
|-------|-----------|------------|----------|
| `GetLeft()` | — | Integer | Левая граница прямоугольника. |
| `SetLeft(arg0)` | arg0: Integer | Integer | Устанавливает левую границу. Возвращает предыдущее значение. |
| `GetTop()` | — | Integer | Верхняя граница прямоугольника. |
| `SetTop(arg0)` | arg0: Integer | Integer | Устанавливает верхнюю границу. Возвращает предыдущее значение. |
| `GetRight()` | — | Integer | Правая граница прямоугольника. |
| `SetRight(arg0)` | arg0: Integer | Integer | Устанавливает правую границу. Возвращает предыдущее значение. |
| `GetBottom()` | — | Integer | Нижняя граница прямоугольника. |
| `SetBottom(arg0)` | arg0: Integer | Integer | Устанавливает нижнюю границу. Возвращает предыдущее значение. |

## Размеры и проверка точки

| Метод | Аргументы | Возвращает | Описание |
|-------|-----------|------------|----------|
| `GetWidth()` | — | Integer | Ширина прямоугольника (`right - left`). |
| `GetHeight()` | — | Integer | Высота прямоугольника (`bottom - top`). |
| `IsInside(arg0, arg1)` | arg0: Integer (x), arg1: Integer (y) | Boolean | `TRUE`, если точка находится внутри прямоугольника. Левая и верхняя границы включаются, правая и нижняя — исключаются. |

## Примеры

Создание прямоугольника и получение его размеров:

```EME-L
rect = Object("Rect", 100, 100, 500, 500);
width = rect.GetWidth();
height = rect.GetHeight();
```

Настройка области окна консоли:

```EME-L
rect = Object("Rect");
rect.SetLeft(100);
rect.SetTop(100);
rect.SetRight(600);
rect.SetBottom(400);
Console.SetRect(rect);
```

Проверка попадания точки в область:

```EME-L
rect = Object("Rect", 0, 0, 200, 100);
If (rect.IsInside(50, 50))
    'Точка внутри прямоугольника'
End If
```

Передача прямоугольника в метод `AddSprite` класса `Irrlicht` (реальный пример из компонента 3D-курсора WMS):

```EME-L
'Спрайт-прицел создаётся с прямоугольником 16×16'
CursorSpritePoint = m_Irrlicht.AddSprite("Cursor#point", Object("Rect", 0, 0, 16, 16));
```

## См. также

- [Класс Console](./console.md) — метод `SetRect` принимает объект `Rect` для задания области окна консоли.
- [Класс Irrlicht](./irrdevice.md) — методы `AddSprite` и другие принимают `Rect` для позиционирования спрайтов.
